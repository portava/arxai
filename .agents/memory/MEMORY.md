- [Mission Planner timeframe unit model](mission-planner-timeframe.md) — minutes-first domain; short-tf daily-equiv huge (1%/30min≈61%/day→Unrealistic scalp); unitAwareMissionClass zones; NL parser fully wired (parse-intent route + UI); extractTimeframe scans all tokens.
- [HTF Trend FVG Pullback wiring pattern](fvg-trend-pullback-wiring.md) — pure engine in lib/fvg/; Ruby adapter in assistant/; scanner decorator uses runWithScannerBudget + mapWithConcurrency; route wires after timing, before finalRead sort; ChartOverlay needs symbol field + marker:{side} not top-level side.
- [Instrument Passport (D3b)](instrument-passport.md) — canonical per-market unit facts w/ provenance (DECLARED vs BROKER_REPORTED), exact unit-conversion closure (never rounds, typed refusals), forexPair/instrumentSpec/brain-registry/derivProvider are now views; USOIL/UKOIL exclusion ruled deliberate.
- ARX Focus — [36-market lock](arx-focus-market-lock.md) (@workspace/domain/market = sole truth; live backstop SYMBOL_NOT_IN_ARX_FOCUS is NEW-ENTRY only, close/modify exempt; contract test locks the 2-call-site scope) / [envelope contract](arx-focus-lock-envelope-contract.md) (display surfaces return BOTH shared envelopes — blocked→no-fetch, approved→nested arxFocus key; multi-symbol explicit per-symbol entries never silent-drop; hidden-not-deleted).
- [Entry data-sufficiency gate](entry-data-sufficiency-gate.md) — Phase-2 COMPOSES pure engine (never re-derives), block-only/fail-closed, lockstep preflight+dispatch; entry freshness MUST come from buildChartIntelligenceState (mt5_broker-aware) NOT the Deriv-tick resolver or owner EURUSD live testing falsely blocks; lazy-import inside try.
- Chart feed-status honesty — [shared resolver/badge](chart-display-status-shared.md) (/api/chart/candles carries feedStatus in MT5 tf names, /api/data/candles is a bare array; gate live affordances on isLivePriceDisplay) / [embedded in candles](chart-feedstatus-embedded.md) (drive feed-confidence UI off data.feedStatus never a 2nd /feed-status poll; feedConfidence() centralizes verdict, aiUsable only when quality=clean).
- lightweight-charts — [v5 API](lightweight-charts-v5-api.md) (v4 methods removed; never cast a charting call) / [RO disposal guard](lwc-resize-observer-disposal.md) (every consumer's RO callback needs disposed-flag+try/catch or post-remove callbacks throw "Object is disposed" empty-stack→Vite overlay; setTimeout→rAF loops need stopped flag) / [marker anchoring](lwc-marker-bar-anchoring.md) (markers must anchor to in-series bar or paint crashes; adapter re-snaps inside setCandles).
- [Measuring production timing in dev](measuring-production-timing-in-dev.md) — artifact workflow locked + harness can't hit non-proxied ports; use bundle + loopback-transport evidence, or publish.
- [Narrow env parser for ARX_LIVE_BROKER_EXECUTION_ENABLED](narrow-env-parser.md) — accept ONLY trimmed lowercased "true"; never broaden to 1/yes/on.
- Account-mode resolution — [unified resolver shape](unified-account-mode-resolver.md) (single source at /api/me/account-mode; armed user stays LIVE_SHARED + cleanBlockedReason) / [preview-as-user loading race](preview-as-user-loading-race.md) (async role/mode flags default false during load; unresolved mode = preview-locked for safety gates).
- [Live trade confirmation UX rule](live-trade-confirmation-ux.md) — typed operator phrases stay backend-only; user gesture = one Confirm click; raw codes only under shouldShowAdminDiagnostics.
- Perf — [instrumentation](perf-instrumentation-foundation.md)/[action naming+leaks](perf-action-naming-and-leaks.md)/[orval bridge](perf-orval-observer-bridge.md) wire perf once at the mutator (every hook timed), transport admin-gated default-off, close perf row on every SSE early-return; [poll pause](perf-poll-pause-on-hidden.md)/[invalidation](perf-poll-and-invalidation-rules.md)/[route cache](short-ttl-route-cache.md) RQ pauses polling on hidden tabs (raw fetch loops need own guard+invalidate-on-mutation), cache slow GETs at ROUTE layer evict-on-reject; [lazy panels](perf-lazy-collapsed-heavy-panels.md) lazy collapsed heavy comps; [alerts badge](perf-alerts-badge-payload.md) SQL unread-count endpoint never client-count; [sweep verdict](perf-sweep-verdict-semantics.md) multi-sample probes FAIL on mixed-status.
- Test-assertion design — [audit tables](audit-table-test-baselines.md) (baseline-delta + max(id), never "count==0"; rows are safety evidence, never auto-delete) / [expiry days-left seeds](expiry-floor-test-seeding.md) (floored daysUntilExpiry + ms elapsed before endpoint reads its own `now` floors an "exactly N days" seed to N-1; seed x.5 days).
- [Import-boundary guard escape ladder](import-boundary-guard-escape-ladder.md) — text-scan "never import Y" guard: ban spec-literal outside quoted `from` (backtick≠import) + require identifier + `["require"]`; reflection/split-string out-of-scope (review). Companion [line-preserving strip](ci-guard-line-preserving-strip.md): blank comments in place (block→spaces, //-line→"" KEPT) never delete lines or reported line drifts; report :line:col via rx.exec + exact m[0] token.
- [Integration CI lane](integration-ci-lane.md) — DB-backed safety tests can't live in offline `ci` (db import throws); they run in `ci:integration` (registered `safety-integration` validation, INTEGRATION_LANE_KEYS = single source of truth), missing-DB hard-fails ONLY in CI context, tests self-boot in-process.
- [Live-intent vs live-shared paths](live-intent-vs-live-shared-paths.md) — /live-intent/submit audit-only (lot≤0.01, accepted=false); LIVE_SHARED uses LiveSharedTradeTicket via canTrade.
- [Preflight SL sanity is physics not policy](preflight-sl-sanity.md) — BUY SL<entry / SELL SL>entry / |SL−entry|≤50% apply to ALL profiles incl owner; best-effort quote, skip on quote-fail.
- Market providers — [Polygon free-tier forex](polygon-free-tier-forex.md) (only D1 + /prev; sort desc+reverse or stale bars; tag /prev DELAYED) / [composite design](composite-market-provider.md) (fall-through on EVERY method; first connected+non-empty wins; never fabricate).
- [Chart symbol shared bus](chart-symbol-bus.md) — every symbol-aware UI reads useChartSymbol; async work keyed by symbol invalidates stale; modal frozen at open; see also [mount rebroadcast](tradingview-mount-broadcasts-bus.md) (TradingViewLiveChart pushes defaultSymbol on every (re)mount; feed it the live bus symbol or it resets to EURUSD) and [pick writes brokerSymbol](symbol-bus-vs-broker-resolution.md) (concrete pick = exact brokerSymbol incl. recents; typed SYMBOL_AMBIGUOUS only at LiveSharedTradeTicket).
- [Per-row data quality from per-row evidence](per-row-quality-from-per-row-evidence.md) — multi-row freshness/state from per-row cache lookups, not a global "data flowing" boolean.
- [Operator/diagnostic page gating](operator-diagnostics-gating.md) / [effective-vs-real role](admin-gate-effective-vs-real-role.md) — wrap operator pages in AdminDiagnosticsGate AND gate endpoints on EFFECTIVE normalizeProductRole(req.authUser.role), NEVER resolveProductRole (reads realRole → preview-as-user leaks); admin-previewing-as-user blocked on both; resolveProductRole only for /me projection.
- [Scanner router-first, no sim leak](scanner-router-first-no-sim-leak.md) — synthetics route via marketDataRouter; AWAITING_FEED over sim OHLC; LIVE_SHARED modal uses LiveSharedTradeTicket.
- [LIVE_SHARED data isolation](live-shared-data-isolation.md) — every read (list AND by-id) mode-routes via getUserModeScope; live_intents has no userId so all reads must admin-gate.
- [OWNER safety-bypass pattern](owner-safety-bypass-pattern.md) — double identity guard + soft-reason allowlist + global anchor + read-only + audit; downstream gates still run.
- Live commands — [projection/redaction](live-shared-command-redaction.md) (user-facing GETs use allowlist DTO projection; redact account/bridge/payload/idempotency/gate-snapshot) / [exactly-once CAS](live-command-exactly-once-cas.md) (terminal arx_live_commands writes use UPDATE...WHERE status='SENT_TO_MT5_LIVE' + rowcount check; unconditional update races).
- [MT5 retcode 10027 three switches](mt5-retcode-10027-three-switches.md) — three AutoTrading switches gate OrderSend; EA Common-tab one isn't in heartbeat so a clean precheck can still 10027.
- Live Test Cycle — [dispatch path](live-test-cycle-design.md) (OWNER single-shot reuses createLiveDraft→confirm→dispatch; single-flight via DB partial unique index; auto-close once) / [no one-trade limiter on manual](manual-live-no-onetrade-limit.md) (the automated cycle is a one-shot OPEN+CLOSE *verification* only; owner/admin manual live testing is uncapped — don't conflate).
- [EA / domain pre-trade guard parity](ea-domain-guard-parity.md) — mirrored MQL5 guard must match the TS contract check-for-check; MQL5 untestable here so drift is silent (caught only by review).
- [New schema tables absent from old dev DB](ea-update-tables-dev-db.md) — read endpoint 500 on "relation X does not exist"? merged tables don't auto-migrate old dev DBs; run db push.
- [Fail-closed admin audit pattern](fail-closed-audit-pattern.md) — DB mutations wrap mutation+writeAudit(tx) in one tx; dispatch actions audit INTENT before queueing then best-effort result.
- [Audit actor role integrity](audit-actor-role-integrity.md) — a script/handler recording an audit actor must use the actor's REAL privileged role; never coerce non-ADMIN→OWNER (hard-fail); role is plain text so re-ternary after the guard for the literal union.
- Dev/QA auth & sessions — [e2e invite gate](e2e-auth-invite-gate.md) (registration invite-gated; seed beta_invites, log in via /login UI) / [dev-owner-login≠per-user](dev-owner-login-not-peruser.md) (legacy role cookie only; authUser needs /auth/login, env QA pw stale) / [QA shell ephemeral session](qa-shell-ephemeral-session.md) (mint auth_user_sessions→sha256 cookie arx_user_session, delete after, read-only) / [signed hr_session](genuine-signed-session-cookie-tests.md) (mint via encodeSession w/o knowing SESSION_SECRET, mint without uid) / [dev anon=OWNER](dev-anon-default-owner-runtime-smoke.md) (no hr_session⇒OWNER dev/VIEWER prod; faithful non-admin smoke = x-security-role:VIEWER + minted session; SESSION_SECRET set⇒can't forge; restart api-server to verify route edits) / [playwright no env secrets](playwright-subagent-no-env-secrets.md) (runTest notebook lacks QA_OWNER_*; real-auth QA server-side).
- [Scanner chart trade actions](scanner-chart-trade-actions.md) — chart place/close/modify/reverse route via executeInstantTrade(source:"chart"); paper→no buttons; pending-cancel = DELETE draft.
- [QA test-user cleanup: dynamic + fail-closed on evidence](qa-test-user-cleanup.md) — discover all user_id tables to delete, but protect evidence (name-pattern + denylist) and ABORT if a protected table holds test rows; never half-clean.
- [Normal-user route containment](route-containment-allowlist.md) — nav-hiding ≠ access control; default-deny allowlist + AppLayout guard gate direct-URL; /emergency stays allowlisted.
- [Two-tier human-trader nav](two-tier-trader-nav.md) — pending vs approved driven by /api/me/account-mode (useTraderTier, loading=locked); approvedOnly flag + pending-vs-full allowlist across ALL nav surfaces + RouteAccessGuard; pending paths allowed without awaiting approval, else tierLoading→skeleton; tests must mock useTraderTier.
- [Workflow health detection in this sandbox](workflow-health-detection.md) — ss/lsof restricted here; proxy HTTP 200 is the authoritative workflow-liveness signal, not port/PID enumeration; unauth candles ≠401 is a hard-fail auth regression.
- [rg/grep matched-content mangling](rg-output-token-mangling.md) — bash rg/grep can corrupt matched text to literal "n"; use the read tool for content, rg -l/-n for locations only; intermittent, don't trust mangled dumps.
- OOM in this env — [full typecheck](typecheck-oom-this-env.md) (use `pnpm run typecheck:ci` serial one-process-per-unit; cgroup-OOM not V8 so LOWER the heap; kill exact IDE-tsserver PID; run foreground) / [validation-orphan trap](mark-complete-validation-orphan-oom.md) (spin-looping mark_task_complete piles concurrent suites → 2 tsc at once = exit 137 OOM not a type error; kill orphans, run ONE lane at a time) / [browser-test quiet window](ci-validation-saturation-browser-test.md) (runTest "browser closed unexpectedly" = validation saturating the box; clearValidationCommand the heavy ones then setValidationCommand back — NEVER removeWorkflow+configureWorkflow).
- [Process-guard fail-safe on safety-critical server](process-guard-uncaughtexception.md) — uncaughtException must log+close+exit (clean restart), NOT keep-alive; only unhandledRejection logs-and-continues; request-path errors belong to always-JSON middleware.
- Phantom live positions — [reconcile owned](phantom-live-position-reconcile.md) (stale open arx_live_positions strand allocation; repair via audited reconcile, reconcile_state non-null, on DIRECT broker-absent evidence, never bypass a gate or fake balance) / [open-card needs broker ticket](phantom-open-card-requires-ticket.md) (render/count a live card only on a confirmed broker ticket; reconcile orphans narrowly, protect in-flight pendings).
- [Admin diagnostic must mirror real gate normalization](diagnostic-gate-readout-parity.md) — a gate-diagnostic row that re-implements a check drifts silently; case-sensitive diagnostic showed FAIL while gate #6 (lowercased) PASSed. Normalize identically; gate is source of truth.
- One-click — [standing-consent model](one-click-standing-consent.md) (toggle gesture = consent, typed phrase removed; KEEP REQUIRED_TYPED_PHRASE const as audit marker + live master-live gate; retarget CI guard in lockstep; GET emits canEnableLiveBlockedReason not liveBlockReason) / [ARX Single Confirm vs MT5 terminal](one-click-vs-mt5-terminal.md) (two unrelated "one-click"s; MT5 terminal checkbox unreadable by the EA, ARX gates only on app-side live_one_click_enabled; never conflate in copy/diagnostics).
- [Ruby chart read structure analyzer](ruby-chart-read.md) — deterministic no-LLM read-chart endpoint; committed trend stays directional unless it stalls into opposing boundary; never fabricate (insufficient candles ⇒ honest read); demo-first removed only from active live decision surfaces.
- Symbol resolution — [casing resolver](broker-symbol-casing-resolver.md) / [canonicalize](symbol-directory-resolver-canonicalize.md) / [directory fallback](shared-bridge-symbol-directory-fallback.md) — MT5 case-sensitive, resolve ARX→exact at live-poll boundary, empty dir⇒NOT_FOUND, never weaken a gate; approved no-spec users see master directory at /me/mt5/symbols* ONLY (fail-closed, metadata-only, NOT the exec path which uses resolveBrokerSymbolName).
- Live-execution honesty — [V75 tradability](v75-live-tradability-verdict.md) (synthetics EA_REJECTED_NO_DETAIL = not in user's MT5 Market Watch, not an ARX bug; EURUSD is the proven path, don't fake fills) / [dispatch≠execution](dispatch-vs-execution-honesty.md) (res.ok = sent to bridge NOT filled; say "executed" only on LIVE_FILLED+brokerTicket or LIVE_CLOSED via command-status poll reading real cols brokerTicket not orderTicket).
- [Owner-unrestricted live preflight relaxations](owner-live-preflight-relaxations.md) — margin proxy is now a governance toggle (enforceAllocationLimit, OWNER+ADMIN); Deriv-synthetic floor stays owner-only; every other gate + 16-gate dispatch still run.
- [isPrivileged vs governance-active](governance-privileged-vs-active.md) — isPrivileged (role) gates UI visibility only; relaxed behavior needs isPrivileged && ownerLiveControlMode. Using isPrivileged alone causes a control-mode lockout or silent relaxed caps.
- [Source-scan tests false-pass off comments](source-scan-test-false-pass.md) — regex copy tests match doc-comment text too; on copy changes assert NEW present AND OLD absent, and strip old strings from comments.
- [Ruby briefing honesty gates](ruby-briefing-honesty-gates.md) — state-derived assistant briefing; surface news/calendar ONLY when provider.connected; MT5 master numbers owner/admin-only; weather/location never guessed; never an execution gate.
- UI display honesty — [notification dedupe collapse](notification-dedupe-collapse.md) (alert spam = unstable dedupe entityId; key on logical entity + cooldownMs, bump repeatCount on collision WITHOUT resetting status; delete is soft/user-scoped, never hard-delete) / [single live indicator](single-live-indicator-rule.md) (live/demo shows on ONE compact LiveModeBadge chip; never re-add a mode banner/pill or persistent "Live trading ready" alert; banners = actionable problems only).
- [Shared-live account provisioning](shared-live-provisioning.md) — LIVE_SHARED needs 4 aligned rows (routing + virtual_trading_accounts + slot allocation + arx_live_arming.is_armed); force-arm honestly (fresh phrase hash, never copy another user's arming row, audit the bypass).
- [Live allocation single-source](allocation-view-single-source.md) — display surfaces (Cockpit/SharedAccountCard) must read getUserAllocationView.availableAllocation (assigned−reserved+openFloatingLoss), NEVER static virtual_balance, or card shows headroom the gate refuses; pure resolveAllocationGate splits NOT_ASSIGNED(!hasAllocation) vs EXHAUSTED, both still BLOCK.
- [Shared open-position truth join](shared-open-position-truth-join.md) — "Open positions" = attribution(open,ticket) ⋈ arx_live_positions; JOIN MUST scope user_id (ticket uniqueness is per-user, not global) or it leaks another tenant's position.
- EA POST pitfalls — [body truncation](mql5-webrequest-body-truncation.md) (EA POST 1 byte short, missing final `}` → entity.parse.failed; StringToCharArray explicit-count adds no NUL, ArraySize-1 chops real byte; use WHOLE_ARRAY then len-1) / [diagnostic payload 413](ea-diagnostic-payload-413.md) (ENUMERATE_SYMBOLS result POST 413s under default 100kb express.json; scope express.json({limit}) to /api/mt5/command-result before global parser; EA won't retry, re-dispatch fresh PENDING row).
- [Ruby manual voice only](ruby-manual-voice-only.md) — auto-listen removed (VAD reopened mic mid-TTS → cut-off); mic disabled while rubySpeaking; useAutoListen.ts unwired, don't rewire; Realtime mode kept on purpose.
- [EA heartbeat eaInputs shape evolved](ea-heartbeat-eainputs-shape.md) — v1.50 nests live-readiness flags under `eaInputs` (v1.29 sent flat); parser must read nested-first or a ready master bridge falsely blocks MASTER_BRIDGE_NOT_LIVE_CAPABLE.
- [Live command transport bridge](live-command-transport-bridge.md) — EA polls ONLY mt5_commands, never live-commands-poll; mirror dispatched arx_live_commands into mt5_commands + forward result; gate #9 = EA AllowOrderExecution (blocks pre-mailbox when off).
- Live gate copy parity — [gate-code→FE](live-gate-code-copy-parity.md) (every LivePhaseBGateKey needs exact-key entries in humanize REASON_MAP+CATEGORY AND structuredRejection CODE_META; CODE_META override hides missing REASON_MAP keys; humanize LIVE_BLOCKED branch exact-match no token-scan) / [block-banner gate-name](block-banner-gate-name.md) (liveSharedReasonCopy fallback must humanise LIVE_BLOCKED:<GATE>; never collide with FORBIDDEN_USER_COPY_TOKENS).
- Ruby broad market intelligence — [on-demand reads](scalp-rank-cache-source.md) (/me/scalp/rank + Ruby opps/radar scan via scanSymbolTimeframe live, fresh-server safe; scannerOpportunities cache is Scanner-page-only) / [unified resolver](ruby-market-intelligence-unified.md) (opps/snapshot/context/briefing from one chart-truth resolver; scanner ENRICHES never gates; single scoring path, live-only; news provider-gated; advisory-only) / [feed-not-confirmed honesty](ruby-feed-not-confirmed-honesty-fields.md) (market tools' block/early-return payloads must carry honest feed fields feedConfirmed:false/source:null/UNAVAILABLE never undefined; test open-positions need FRESH lastSyncedAt to pass the truth gate, never relax it).
- [Live fill vs position mirror](live-fill-vs-position-mirror.md) — fill = arx_live_commands (real ticket+retcode), not mirror presence; hide-on-absence needs the empty-stamped complete-snapshot marker, never row recency; non-forex margin unavailable.
- EA/retcode rejection copy — [retcode→user-copy override](retcode-copy-override.md) (retcode overrides copy ONLY when MAPPED; classify only positive ints, Number("")===0 trap; FE mirror needs #13 backend drift guard) / [rejection reason fallback](ea-rejection-reason-fallback.md) (non-FILLED + null rejectionReason ⇒ derive from brokerMessage/mt5Retcode so UI never shows "(none)").
- [Product-role model](product-role-model.md) — single active role on users.role text (no migration/enum); resolveProductRole reads realRole first; backend guards authoritative; live perms are role-independent rows.
- Post-merge push hygiene ([db push](post-merge-db-push.md) / [concurrent constraint race](concurrent-push-force-constraint-race.md)) — post-merge.sh uses push-force (stdin closed→interactive hangs) + generous timeout (default 20s too tight for schema-adding merges); never run two validation lanes at once (concurrent push-force races DROP/ADD → 42704 + dup QA-email; single non-concurrent push converges; bash timeout 124 ≠ real failure).
- [full-ci chain-order + red triage](fullci-chain-order-classification.md) — `pnpm run ci` &&-chain short-circuits at 1st fail (ran-suites≠green); security-regression is the ONLY localhost:80-dep suite (uniform 502 ⇒ restart api-server+rerun, not a regression); triage deeper reds via isolation-repro + diff scope.
- [Policy lives at entry point, not shared helper](shared-mutation-helper-policy.md) — every route into a shared write helper must re-enforce required-reason/role; legacy DELETE silently defaulted reason while new /status validated; investor portal reads = path-scoped trader deny.
- Honesty degradation — [outcome resolution fail-closed](outcome-resolution-fail-closed.md) (resolve a verdict ONLY on real evidence — closed trade / observed move; elapsed time alone stays UNRESOLVED, never NO_TRADE_CORRECT/EXPIRED-on-timeout) / [fail-soft readiness → NOT-ready](fail-soft-readiness-honesty.md) (`.catch(()=>[])` feeding `ready=empty` fabricates readiness; return null on failure, map null→not-ready + honest marker).
- [Agent Ecosystem advisory/shadow](agent-ecosystem-shadow-only.md) / [runtime involvement map](agent-governance-runtime-involvement.md) — specialists are advisory-only (fail-open, shadow=0, never the 16-gate); distinguish wired-and-behavior-changing vs computed-not-enforced vs manual-only before claiming "active".
- Agent Ecosystem [layer3 mapping](agent-ecosystem-layer3-registry-mapping.md) / [reason-gate](agenteco-reason-gate-convention.md) — registry lacks health signals (derive childCount/parentKey/speedCost, default rest 0 fail-open; DUPLICATE_AGENT≠GENERIC_REPETITION); decision mutations hard-gate reason (run/refresh default it but still audit; daily report upserts onConflictDoUpdate).
- [Scalp add-on revenge guard](scalp-manage-addon-guard.md) / [per-symbol personality feedback](scalp-personality-feedback.md) — evaluateAddOn fires revengeGuardTriggered ONLY when flame alive (maxAddOns>0), fading stages hit DO_NOT_ADD first (test LATE timing + negative PL); tightening-only personality nudge applies on Focus+Broad+Builder, fold closed trades under SELECT…FOR UPDATE or concurrent closes lose updates.
- [Factory request dedupe + CAS approval](factory-request-dedupe-and-cas-approval.md) — request-level dedupe needs service precheck AND partial-unique index (PROPOSED only); approval mints once via CAS-claim-in-transaction.
- [Global nav surfaces gate roles independently](nav-role-surfaces.md) — sidebar role-correctness ≠ MobileBottomNav/FloatingActionPanel/CommandPalette; audit ALL global nav surfaces for investor/admin leaks.
- [Engine+DB-wiring present ≠ wired into a runnable flow](wiring-vs-callsite-gap.md) — domain engine + a persist helper can both exist yet never be called by any route/runner; verify call-sites (grep usages) before trusting a feature is end-to-end.
- [Agent Ecosystem runtime wiring — [lifecycle runner](agenteco-lifecycle-runner.md), [governance applies when](agenteco-governance-applies-when.md), [traffic enforcement opt-in](agenteco-traffic-enforcement.md), [court auto-wiring](agenteco-court-autowire.md): advisory/shadow, opt-in, fail-open; influence persists only on a real event (SHADOW weight-0 ⇒ no trace; ≥2-camp conflict ⇒ disagreement; non-selected agents abstain); guard order enabled→live-defer(TTL-aware)→lock→fail-soft; never import into the live path or delete live evidence to unstick.
- [Ruby/assistant safety envelope derived-vs-forced](ruby-chart-read-envelope.md) — reporting surfaces DERIVE per-user (incl. all prose); only chart/intelligence+draft-read+draw-setup force paper_only; gates/choreography untouched.
- [Weekly report change-verifiability gate](weekly-report-honesty-gate.md) — never show numeric week-over-week change when under-review/stale; one changeVerifiable flag, math + UI in lockstep.
- News/calendar chart honesty ([news radar](news-radar-honesty-gate.md) / [event markers](economic-event-chart-markers.md)) — events only from a connected REAL calendar (honest-empty WIRED via seam, never hardcoded); a "news layer" = chart markers + preference-gated alerts; markers reuse the market-truth snapshot calendar (no new fetch), render ONLY on confirmed feed (LIVE/FALLBACK_COMPOSITE), window-FILTER never edge-clamp, pure helpers in scannerChartFormat.ts, effect dep MUST include displayStatus.
- Fund Book ([cutoff](fundbook-waterfall-cutoff-source.md) / [capital lifecycle](fundbook-capital-lifecycle.md) / [drawdown-HWM](fundbook-drawdown-reconciliation.md) / [deposit fee](fundbook-deposit-fee-nav.md)) — POOL-overlay-HWM beats NAV for cutoff (baseline always NAV, persist resolved cutoff never HWM+profit); status via pure requestLifecycle (settle atomic, no loose terminal flips); HWM recompute reads overlays in own tx AND deletes vanished-scope rows or stale drawdowns persist; deposit fee = ledger row only (issueUnits double-counts a passed feeAmount → issue on net, feeAmount=0).
- [Admin mutation conflict classification](admin-mutation-conflict-classification.md) / [drizzle wraps 23505 on .cause](drizzle-wrapped-error-code.md) — 409 ONLY for 23505 unique-violation OR a CAS zero-row sentinel; everything else 500. Under drizzle 0.45.2 a tx 23505 sits on e.cause.code NOT e.code, so isUniqueViolation must walk the cause chain or it re-throws (bridge v2 idempotency replay); test "not-fed" via sequence-duplicate.
- [ARX Native Chart L1 truth contract](arx-native-chart-l1.md) — normalized candle+feed-status built OVER routeCandles (no new source); quality precedence + trailing-interval staleness; aiUsable===clean; /data/candles untouched.
- [Chart decision-memory Slow Brain](chart-decision-memory-slow-brain.md) — receipts immutable by absence of update/delete path (prove via byte-identical snapshot); admin read double-gated; honesty floors first-class; QA uses negative synthetic userIds.
- [Chart controls role+mode awareness](chart-controls-role-awareness.md) — PAPER/frozen render NO Plan buttons (not just block placement); investor route-contained AND defence-in-depth gate menu+scan; gate each control to what backend accepts.
- [Statement file serving gates](statement-file-serving-gates.md) — each file-stream route must re-apply status/role gates; list filtering never protects the direct download route; ref-count before deleting shared objects.
- [Admin route path + anon-401 false-pass](admin-route-path-and-false-pass.md) — handlers register /admin/* (router mounted at /api); blanket guard 401s ANY /api/admin/* incl nonexistent, so only an authed ADMIN→200 test proves correct wiring.
- [ARX Handshake System (advisory/fail-open)](handshake-system.md) — cross-layer readiness/check-in backbone that WRAPS services read-only; UNKNOWN on no data, never a 17th gate; admin-only monitor.
- [Execution-cost estimator directional honesty](exec-cost-directional-honesty.md) — validate SL/TP side before pricing; null wrong-side legs + blocker, never show invalid setup as profitable/survivable.
- [Request-to-join onboarding](request-to-join-onboarding.md) — public request-access ALWAYS returns one neutral confirmation (no enumeration); approve reuses createInvite (cap only at approve, over-cap stays queued); QA owner is DB role USER so admin endpoints 403 without temp promote.
- [Registration-key pepper operational](registration-key-pepper-operational.md) — ONE REGISTRATION_KEY_PEPPER governs both key generation AND signup validation; unset = full fail-closed; set as shared env + restart api-server to activate (prod needs republish); never rotate/delete or all keys break.
- [AACI security handshake enforcement scope](aaci-security-handshake-enforcement.md) — advisory BLOCK-only; enforce at REAL chokepoints over the full catalog; gate only the sensitive direction (release kill switch, activate agent).
- [Self-Trade autonomous cycle scope+audit](selftrade-autonomous-cycle-scope.md) — scoped run must intersect fleet-wide active-exec agent set with listAgents(scope); fail-closed AUTONOMOUS_CYCLE_RUN audit before any side effect; reject USER scope w/o ownerId; dispatch≠fill.
- [ARXNativeChart real-time ticking honesty](arx-native-chart-real-time-ticking.md) — jsdom render test drives REAL SSE wiring + honesty gates; (1) updateActiveCandle avoids repaint; (2) frozen/stale/non-LIVE/malformed ticks rejected; (3) authoritative poll supersedes late tips; (4) stream closes on unmount.
- [Selected Market truth switch + CI-guard lockstep](selected-market-truth-switch.md) — Selected Market panel must use analyzeMarketFromCandles (real candles) not simulator analyzeMarket; dataAsOf from feed/candle-bucket time ≠ generatedAt; a CI guard encoding the OLD contract must be retargeted (tightened) when a task supersedes it, not treated as untouchable.
- [Scanner feed badge cap routing](scanner-feed-badge-cap-routing.md) — Scanner chart panel + scalp cards reach FeedConfidenceBadge ONLY via capped RubyChartRead (header = honest ChartFeedStatusBadge); never add a raw uncapped badge there.
- Render-proof test mocking — [mock hooks not providers](render-proof-mock-new-hook.md) (scanner/card render proofs have NO QueryClientProvider + mock every data hook; a new data hook crashes the suite until mocked) / [vitest pitfalls](vitest-render-proof-mocking.md) (never return `new Proxy` from a vi.mock factory; shared `const stub` in vi.mock = TDZ → inline it; imported-but-not-rendered siblings need NO mock, only consumed hooks + wouter).
- [ScannerChartPanel pure-helper module](scanner-chart-pure-helper-module.md) — keep pure constants/helpers in a sibling *.ts (scannerChartFormat.ts), never export non-component values from the component file (testability + Vite fast-refresh breaks otherwise).
- [Mock-never-LIVE_FEED guard](mock-provider-live-feed-guard.md) — CI: no file may both import the mock surface AND emit LIVE_FEED; containment over "shim off" (keyed alphaVantage shim still serves mock data); LIVE_FEED tagged only by router-fed scanner.
- [Opportunity Map sim direction for no-data rows](opportunity-map-sim-direction.md) — non-synthetic no-feed symbols fall back to simulator for direction/edge (tagged SIMULATOR/hasLiveData:false/non-tradeable, NOT a leak); FE shows the badge unconditionally; gate on row.hasLiveData to hide.
- [Mid-session secrets don't propagate](secret-propagation-frozen-snapshot.md) — requestEnvVar updates the store, not running processes (bash + fresh workflow both stale; sandbox has no env); only a full Repl restart reloads; apply user-set passwords via an interim value or post-restart.
- [configureWorkflow one-shots clobber .replit runButton](configureworkflow-oneshot-drops-runbutton.md) — running a one-off as a workflow drops runButton="Project" and it's unrestorable via tools; use bash for one-offs.
- [Market-closed/frozen-quote indicator honesty](market-closed-indicator-honesty.md) — "Market closed" badge needs broker-stale AND wall-fresh (ticks still arriving); broker-stale alone mislabels a dead feed as closed; reset per-stream state on symbol switch.
- Deriv feed liveness — [signal](deriv-feed-liveness-signal.md) (WS self-establishes on boot index.ts eager bootstrap; liveness = /api/market-data/deriv/status LIVE_FEED+hasRecentTick NOT the market_candles deriv mirror; otpLastResult "invalid" benign) / [winning-provider basis](synthetic-liveness-winning-provider.md) (only demand a Deriv WS tick when the WINNING provider is deriv*; mt5_broker-served synthetics judge on candle freshness — assetClass alone falsely demoted fresh broker synthetics to "analysis only"; honesty floors + buildFeedStatus unchanged).
- [Outage banner page-scope + enrichment cap](scanner-outage-banner-and-enrichment-cap.md) — degraded banner must render above PageTabs (tab-trapped = invisible on Focus); cap scanner per-symbol decorators via mapWithConcurrency(=8); no forced-502 hook here, prove structurally.
- [Shared read-verdict reason unification](shared-verdict-reason-unification.md) — unify the REASON on every surface/branch, not just the originally-buggy source; downgrade cap keys on !canShowTradeSetup (any data source); freshness/delayed copy must not outrank a higher-precedence insufficiency reason.
- [Verify merged code is running + verdict transience](verify-merged-code-is-running.md) — "merged but not showing": api-server dev = build&&start (runs dist bundle, grep dist/index.mjs + check process start time to prove freshness); Vite dev serves src (stale dist = deploy-only); LIVE_DELAYED is correctly invisible when the feed is genuinely LIVE.
- [One feed verdict (scanner/Ruby/floor)](one-feed-verdict-three-surfaces.md) / [Ruby chat footer = scanner header verdict](ruby-chat-footer-feed-verdict.md) — resolveSymbolFeedVerdict/getChartFeedStatus = ONE freshness verdict (no 2nd threshold; fail-closed); ChartFeedStatus lacks trailingIntervals so Ruby uses quality+tick; VERIFIED read downgrades FULL→STRUCTURAL_ONLY when feed unconfirmed (withheld reads use dedicated builders, never the success line); LIVE_DELAYED blocks synthetic live at preflight+dispatch; enforce in rubyStructuralReadService (route delegates, static guards target the service).
- [DB rate-limiter semantics](db-rate-limiter-semantics.md) — evaluateRateLimit returns FULL cooldownMs on violation (not remaining-window); consumeRateLimit SELECT→eval→upsert isn't per-(action,scope) serialized; non-public-auth ⇒ failOpen:false (securityPhase7 guard).
- Synthetic-floor smoke — [QA loop baseline trap](synthetic-floor-loop-baseline.md) (looping live-pipeline harness "no command row written" check must snapshot just-before-draft, not start-of-run — prior dispatch tests persist valid LIVE_BLOCKED rows) / [prod default-deny guard](synthetic-floor-prod-default-deny-guard.md) (CI comment-stripped source-scan locks the two refusals QA_ALLOW_DB_MUTATION + prod QA_ALLOW_PROD_SMOKE; build fails if either dropped).
- [Pino transport worker hangs process.exit](pino-transport-worker-hangs-exit.md) — a pure tsx test that PASSES can intermittently fail to exit if its imports reach logger.ts (pino-pretty worker = blocking thread-stream exit flush); fix = split pure logic into a logger-free core module and import that, not --test-force-exit.
- [Scores-not-placeholder backend lock](scores-not-placeholder-backend-lock.md) — "values are real not defaults" needs a pure backend-derivation test (2 distinct inputs ⇒ distinct outputs); a FE render-echo passes even if backend collapses to a constant.
- [Trade Health readiness normalizer downgrade-only](trade-health-readiness-normalizer.md) — shared display contract's tf normalizer/floor must match EXACT tokens (month=MN1 not MN\d*), unknown⇒strictest 150 never lenient; Ruby must pass per-tf minimumRequiredCandles or it defaults to 5 and over-reads vs scanner.
- [Scrub blanks UPPER_SNAKE tokens](scrub-blanks-underscore-tokens.md) — scrubUserCopyDeep blanks STRUCTURAL_ONLY/enum tokens in user-facing chartRead copy; assert state via gated/canReadStructure/canShowLiveTradeSetup/liveSetupWithheld or the raw top-level readLayer field, never the scrubbed prose.
- [Readability-vs-actionability direction leak](readability-vs-actionability-leak.md) — sufficiency engine caps only label/action, never bias; direction leaks from INSUFFICIENT/PARTIAL data across scanner/header/ruby-card/scalp/opp-map; Phase-2 contract = display-only mayShow* flags in marketDataSufficiency.ts (never an execution gate).
- [Trade-Health ceiling delivery layer](trade-health-ceiling-delivery-layer.md) — ONE readiness verdict enforced at SOURCE (scannerTruth/Ruby/resolveTradeAffordance hub mirrors it; read_only forces ceilings off); scannerActionability 3rd-arg ceiling is downgrade-only; ScalpSignalCard/BroadScan deliberately NOT wired (engine already collapses non-live; forcing it risks blind-Broad regression); never gate owner/admin Confirm on a ceiling.
- [node:test tsx unit consolidation](node-test-isolation-consolidation.md) — `node --test <files>` spawns a child PER FILE; `--test-isolation=none` shares one process (~36s→~7s); top-level mock.module must be a real-superset, keep it last.
- [Scanner header action display resolver](scanner-pending-display-state.md) — keyed truth (no keepPreviousData) means a present data-only verdict is RESOLVED: mirror it immediately; PENDING only when both null; expiry never overrides; run-key guard on publisher retract effects.
- [Scanner Action-cell bounded pending FSM](scanner-action-pending-fsm.md) — header "Checking…" must be bounded: keyed symbol|tf timeout→NO_CONFIRMATION + onError publishes CHECK_FAILED; display-only, sanitize before verdict resolvers.
- [Scanner insufficiency split](scanner-vs-ruby-insufficiency-presentation.md) / [header-card reconciliation](scanner-header-card-verdict-reconciliation.md) / [setup-level withholding](scanner-setup-withholding-projection.md) — same sufficiency engine: scanner keeps downgraded directional bias while Ruby withholds (MT5/forex closed-bar starvation EURUSD=1 vs Deriv V75=299 drives it; legacy "cannot verify chart data" leak only for status=partial); selected card LIFTS setup-aware verdict to a page store, header CONSUMES via resolveSelectedSymbolActionability=lifted??dataOnly (never a 3rd opinion, clear on symbol switch); one fail-closed projectOpportunitySetup withholds ALL scanner/Ruby/radar levels on !canShowTradeSetup so radar key/invalidation levels (derived from entry/stop) can't leak (reason=humanReason).
- [QA suspend→restore + sentinel](qa-suspend-restore-sentinel.md) — DB-backed suites blocked by pre-existing freeze/discrepancy rows: capture-by-id, suspend, restore exact in finally, sentinel-assert restore; never delete; deposits default BALANCED (CASH_RESERVE refused).
- [Offline network guard proof](offline-network-guard-proof.md) — batch-only parity flake = rate-limited provider enrichment; swap globalThis.fetch to block external (deterministic 503 fail-soft); an in-wrapper "escapedCount===0" is VACUOUS — assert attemptCount>0+blockedHosts instead, prove egress via NODE_OPTIONS netprobe.
- [Chart Pattern Truth child-input boundary](pattern-truth-child-input.md) — pattern = display/decision-support only; nudge clamped to ±MAX_CONFIDENCE_ADJUSTMENT AND the verdict ceiling; learning loop fail-open/idempotent/null-user-noop; synthetic bucketed separately; display-contract-import-boundary guard locks it.
- [Market Heat honesty surfaces + UI bar](market-heat-honesty-surfaces.md) — missing provider NEVER reads low/all-clear (per-provider banner news/calendar/both, fail-closed); "card/map" review bar.
- Economic calendar honesty — [provider-agnostic](economic-calendar-provider-agnostic.md) (enforce at EVERY read surface not just shared service; missing-config/fetch-error never all-clear) / [selected-not-configured](calendar-provider-selected-vs-configured.md) (mock fallback gates on provider-SELECTED not CONFIGURED; selected+no-key ⇒ honest-empty real provider never mock; only no-provider ⇒ mock) / [FRED wiring](fred-calendar-provider.md) (TE-then-FRED seam; FRED dates-only: null forecast/clock-time, curated release_name classifier, drop-unrecognized, MUST client-window-filter so tests need now-relative dates).
- [Per-user rename of a default-named exported registry](peruser-rename-exported-registry.md) — convert module-scope static arrays consumed in-component to name-factories resolved via the context hook; KEEP the exported const as a default-named snapshot (factory(DEFAULT)) + give consumers an optional items param so data-contract tests don't break; preserve internal identifier NAMES even when a const must become a function to inject the name.
- [Radix Tabs jsdom activation](radix-tabs-jsdom-activation.md) — shadcn/Radix Tabs default to focus-driven activation; fireEvent.click won't switch tabs in jsdom, use fireEvent.focus+click (no user-event dep here).
- Profit Mission — [Phase 1 contract](profit-mission-phase1.md) (display-only planner; feed hardcoded NOT_CONFIRMED⇒canStart false; banned-vocab/estimate honesty; isolation at route layer) / [Phase 6 gated exec + single-flight](profit-mission-execution-single-flight.md) (drafts reach REAL exec ONLY via executeInstant(source "mission"); CAS-claim approved→executed BEFORE executor — loser 0 rows; release on clean reject, keep claimed on throw; mission risk stricter-only over governor, no martingale).
- [Structure overlay shared drawing path](structure-overlay-shared-path.md) — trendline/channel overlay drawn by ONE shared structureLines.ts (adapter + ScannerChartPanel); double-gate = overlay.visible AND the chart's OWN feed verdict (native: aiUsable+affordance; scanner: isLiveDisplay); display-only.
- [LIVE_EXECUTION_ACTIVATION_GATE + approved-trader live state](live-execution-activation-gate.md) — additive precondition (re-checked at dispatch BEFORE 23-gate, never ORs); bot/agent/system = ONLY users.isSystemUser (no accountType col), investor = role INVESTOR; a code review may falsely FAIL claiming a missing account-type source — verify schema, don't invent one.
- [Global symbol picker merged resolver](symbol-picker-merged-resolver.md) — picker reads /api/me/symbols = ARX_FOCUS_MARKETS (always present) overlaid with enumerated specs; tradeable honest (enumerated+tradable+brokerSymbol only), source from real evidence (never infer "active_candles" from selection — getCandles is SIMULATOR).
- Live-action entitlement (close-can-always-reduce-risk) — [FE resolver](live-action-capabilities-resolver.md) (ONE resolver: canClose ALWAYS true so revoked/frozen reduce risk, OpenLivePositions uses NO mode hook; open-gates only, display-only) + [BE Cluster D](cluster-d-live-entitlement-close.md) (close-after-revocation at meTrades close handler not routingResolver; global DISABLED/kill-switch stay hard; ONE narrow ADMIN_EMERGENCY_CLOSE bypass relaxes only gate #5 for CLOSE; audit honest QUEUED never EXECUTED; route-deny beats phrase).
- [One-click "no gate skipped" route proof](oneclick-no-gate-skip-route-proof.md) — positive proof = approved+toggle-ON+armed HUMAN (isSystemUser:false) submit STILL refuses 409 stage:"draft" LIVE_BLOCKED:LIVE_EXECUTION_ACTIVATION_GATE (fires before master-pool gate, DB-state-independent); assert LIVE_DRAFT_REFUSED audit delta + 0 commands.
- [Chart drag-to-modify SL/TP](chart-drag-modify-sltp.md) — Scanner-only drag of OWN LIVE SL/TP via executeInstantTrade(source chart_drag, MODIFY_SL_TP); entry never draggable; BOTH feed (isLiveDisplay) AND entitlement (canTrade) re-checked at SEND time in the submitModify chokepoint (fresh refs) not just drop; no second path.
- Profit Mission — [serialization contract](profit-mission-serialize-contract.md) (list/get-by-id/pulse all carry riskProfileMismatch+required(Daily)ReturnPct+planningProjection* via ONE pure DB-free serializer, offline ci; new read surface ⇒ route through it) / [intent attribution tags](mission-intent-attribution-tag.md) (missionId/selfTradeAgentId on InstantTradeIntent are pass-through audit metadata, no gate/2nd path; lock via intent-spy asserts in DB-backed route tests not the presence-only static anchor).
- [Ruby parser percent-token boundary](ruby-parser-percent-token-boundary.md) — in the trade-command parser, a digit before `%` must not parse as a lot, and percent extractors end `(?!\w)` not `\b`; "close 50%"→full-close was a liquidation risk.
- [runTest reaped mid-run](runtest-notebook-reaped-mid-run.md) — code_execution notebook can die DURING a long runTest before it returns; write a sentinel result file to disambiguate infra-block vs test-fail; substitute = committed jsdom render-proof + DB-backed route suite cover the e2e's assertions across layers.
- [Live activation blocker copy override](live-activation-blocker-copy-override.md) — thread resolver blockingReasonCode as DISPLAY-ONLY (never a gate); structuredRejection.overrideCode drives only userMessage, technicalCode keeps canonical LIVE_BLOCKED:...envelope; ACTIVATION_BLOCK_COPY exact-match BEFORE substring chain (SERVER_LIVE_EXECUTION_OFF⊃LIVE_EXECUTION_OFF collision).
- [Dual live-execution architectures](dual-live-execution-architectures.md) — TWO guarded live pipelines (Phase B `lib/live/` + legacy `lib/adminTrading/`) both end at the mt5_commands EA mailbox; audit BOTH. adminTrading now STRUCTURALLY locked to SIMULATED-only: gate #8 reads NO env (LIVE→USE_PHASE_B, DEMO→USE_DEMO_QUEUE); keep gate name bridge_token + gate#9 literal; placeOrder callers incl assistant/tools.ts (AI-boundary blocked). `check-admin-trading-no-live-bypass` CI guard now locks the static mailbox-writer allowlist + gate-chain.
- [modeScope contract: meTrades sanctioned live-close exception](modescope-metrades-close-exception.md) — meTrades legitimately imports the live pipeline for CLOSE/ops dispatch (a close reduces risk, same 23-gate); the PART G "read-only routes import no pipeline" invariant must EXCLUDE meTrades + pin it to close/ops only (no createLiveDraft( generic open-draft), never blanket-include it.
- [Picker freshness vs dispatch feed](picker-freshness-vs-dispatch-feed.md) — /api/me/symbols `freshness/MISSING` = enumerated-broker-overlay (display-only); live-dispatch gate resolves feed independently via resolveSymbolFeedVerdictForSymbol→routeCandles; CTA/ticket never read the picker cache. Keep decoupled.
- Assistant name — [Eleanor=user-facing/Ruby=internal](user-facing-assistant-name-eleanor.md) (fix leaks via DEFAULT_ASSISTANT_NAME, never rename Ruby* identifiers, don't touch hash-pinned disclosure text) / [branding guard scope](assistant-name-branding-guard-scope.md) (user-facing "Ruby"→resolved-name source-scan guard covers FE+api-server routes+lib+lib/domain, copy composed in libraries too; domain pure/sync reword not thread; allowlist only admin/operator/internal after verifying RUNTIME projection, AACI userFacingExplanation never reads conflict.detail).
- Eleanor market-tool honesty — [feed-status contract](eleanor-market-tool-feed-status.md) (every block/error branch emits honest feed-status, never undefined→blank; some tools exempt) / [trade-options FVG withholding](eleanor-trade-options-fvg-withholding.md) (FVG staleness independent of primary feed verdict ⇒ STRUCTURAL_ONLY withholds numeric FVG levels; trade-options intent steers only turn-0 tool_choice, never execution).
- Scanner header prominence — [Action-cell](scanner-action-cell-prominence.md) (Action pill emphasis frame neutral primary + verdict-invariant; never tint success-green or strengthen on READY) / [Trade-Health readiness chip](scanner-trade-health-readiness-chip.md) (reuses truth.readiness display-only; only LIVE_CONFIRMED gets green+ring; LIVE_GATE_BLOCKED deliberately keeps green — feed IS live, label carries the gate — don't flip to warning).
- [Global broker diagnostics ownership](global-broker-diagnostics-ownership.md) — stamping userId is not ownership; operator-gate until provider lookup is owner-bound.
