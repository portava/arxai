# Truth-system audit — 2026-08-30

Owner directive: "audit the truth system across all functions and make sure
that all is accurate and giving information based on fact and not guessing."

Ten surface auditors under the honesty rubric (FABRICATED / CONFIDENT_ABSENT /
STALE_UNLABELED / GUESSED / MOCK_LEAK / DEAD_GAUGE), every HIGH finding
independently attacked by adversarial verifiers. Comments were never accepted
as proof — every finding carries a file:line chain.

**107 findings** — HIGH 26 / MEDIUM 55 / LOW 26. 
By sin: CONFIDENT_ABSENT 38, DEAD_GAUGE 22, STALE_UNLABELED 17, GUESSED 17, FABRICATED 8, MOCK_LEAK 5.

Of the 26 HIGH findings, **24 were CONFIRMED** under adversarial
re-verification and 2 REFUTED (the auditor's claim did not survive the code).
The 24 confirmed HIGHs entered the same-day fix campaign; MEDIUM/LOW below
are the standing backlog, unverified beyond the original auditor's evidence.

---

## HIGH

### H1 [MOCK_LEAK] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider — **REFUTED**

When the scanner has no live feed for a symbol it scores it from the in-memory SIMULATOR, and the results cards still show the simulator-derived direction (BUY/SELL), bias, setup type, and quality label (ELITE/STRONG left-edge green) to normal users — with enabled BUY/SELL buttons — because the server-side mask only zeroes numbers, and the frontend never reads the row's dataSource/tradeable/disabledReason fields; for ADMIN/OWNER viewers the same simulator rows render completely unmasked and indistinguishable from live rows on this card.

- **Evidence:** artifacts/api-server/src/lib/marketScanner.ts:1394-1399 (any non-LIVE analysis is tagged ds="SIMULATOR" and still enters the feed) and :1435-1440 (tradeable=false + disabledReason set but never consumed by this page) → artifacts/api-server/src/lib/honesty/feedTruthCopy.ts:102-145 (maskSimulatedOpportunity spreads ...opp, preserving recommendedAction, bias, setupType, opportunity.label; only numbers zeroed; :156 admins bypass the mask entirely) → artifacts/trading-dashboard/src/pages/market-scanner.tsx:50-63 (Opp type omits dataSource/tradeable/disabledReason), :684+689 (opportunity.label drives green edge + badge), :692-693 (recommendedAction + bias badges), :746 (setupType), :767-804 (BUY/SELL/Trade Setup buttons rendered unconditionally per row)
- **Suggested fix:** In maskSimulatedOpportunity also neutralize recommendedAction/bias to WAIT/NEUTRAL, setupType to a withheld token, and opportunity.label to a non-affirmative value; have the frontend results card consume dataSource/tradeable/disabledReason (which the engine already emits honestly) to disable trade buttons and show the disabledReason; label simulator rows explicitly for admin viewers.

### H2 [STALE_UNLABELED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx. — **CONFIRMED**

A scalp signal card keeps saying 'Ready to review' / 'Valid now' with an enabled Build Scalp Trade button indefinitely, even though the engine itself declares the read expires in 90–240 seconds — no surface renders, counts down, or acts on expiresAt/validForSeconds, so a user returning minutes later is invited to trade an expired plan whose entry/SL/TP/lot are pre-filled into the ticket from the stale read.

- **Evidence:** Engine computes validity: artifacts/api-server/src/lib/scalp/scalpEngine.ts:48-55 (MODE_PROFILES validForSeconds 90–240s) and :645-646 (validForSeconds/expiresAt in the result). Frontend discards it: grep of trading-dashboard/src shows expiresAt/validForSeconds only in ScalpSignalCard.verdict.test.tsx:80-81; ScalpSignalCard.tsx:219-225 renders SCALP_TIMING_LABEL['VALID_NOW']='Valid now' (scalpLabels.ts:103) with no timer; RubyScalpFocusCard.tsx:69-128 refetches only on symbol/mode change or manual click; RubyScalpScan.tsx:102-134 holds results until the user re-scans; Build button gate ScalpSignalCard.tsx:427-434 uses only the frozen r.canBuildTrade; market-scanner.tsx:187-217 (scalpResultToSignal) carries the stale entry/stopLoss/takeProfit/suggestedLot into the trade modal. The engine's own EXPIRED timing token is never emitted (only assignments scalpEngine.ts:349/357-380 are VALID_NOW/WAIT_FOR_ENTRY/LATE), so the 'Expired' label at scalpLabels.ts:106 is dead.
- **Suggested fix:** Drive a client-side countdown off result.expiresAt: when it passes, flip the card to an explicit 'Read expired — refresh' state, disable Build, and auto-retract the lifted header verdict. Emit timingStatus EXPIRED server-side on re-serve of an old result.

### H3 [STALE_UNLABELED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx. — **CONFIRMED**

The Position Manager can show hours-old open positions and P/L as current: the LIVE staleness filter is only relative to the user's own newest sync (when the bridge is down ALL rows are equally stale and ALL pass), the DEMO path has no age check at all, and the basket is stamped generatedAt=now with no staleness field — so positions already closed at the broker still show as open baskets with old 'Open P/L' and live-looking exit advice.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpManageService.ts:107-118 — liveFloor = newestLiveSync − 90s (relative, not vs now); :136-160 — DEMO reads mt5StateTable newest row with no lastSyncAt age gate; scalpManage.ts:443-470 (assembleBasket) stamps generatedAt = Date.now(); scalpTypes.ts:366-386 — ScalpBasket carries no lastSyncedAt/staleness field; trading-dashboard/src/components/scanner/RubyScalpBasketPanel.tsx:70-73 renders combinedFloatingPl as 'Open P/L' with no freshness signal (15s polling just re-serves the same stale DB rows).
- **Suggested fix:** Compare lastSyncedAt/lastSyncAt against now (not the newest row), and when the whole feed is stale return the baskets tagged with a syncedAt + stale flag the panel must render ('as of 14:03 — bridge disconnected') instead of bare P/L.

### H4 [CONFIDENT_ABSENT] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter — **CONFIRMED**

The Analytics Session Heatmap renders a fully-populated "$0 / 0 trades / 0% win" grid for ASIA/LONDON/NEWYORK whenever the /api/analytics/session read is pending, failed, or returns an error body — indistinguishable from a real 'you traded flat' result.

- **Evidence:** trading-dashboard/src/pages/analytics-command.tsx:88 passes `(sess.data?.session ?? {}) as any` into SessionHeatmap with no loading/error gate; the query at analytics-command.tsx:24-27 is `(await fetch("/api/analytics/session")).json()` with no res.ok check, so a 500 JSON body ({error:...}, no `session` key) resolves 'successfully'. SessionHeatmap.tsx:11 defaults every missing session to `{ trades: 0, pnl: 0, wins: 0 }` and lines 20-24 render `$${d.pnl.toFixed(0)}`, `{d.trades} trades`, and `{d.trades ? … : 0}% win` — a confident "0% win" even when zero trades means no measurement exists. Backend fallback compounds it: api-server/src/routes/analytics.ts:281 serves `?.dataset ?? {}`.
- **Suggested fix:** Gate on sess.isLoading/sess.isError (and res.ok) and render an honest empty/error state; inside SessionHeatmap render '—' (not $0 / 0% win) for sessions with no bucket or 0 trades.

### H5 [GUESSED] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter — **CONFIRMED**

With <15 candles (no market feed) the timing brain returns hard-coded buyPressure=50 / sellPressure=50 / roomToMove=50 and relabels the session's static fakeoutRisk constant as 'trap probability'; the Buy/Sell Windows and Danger Windows tabs render these as numeric market readings ('Buyers 50 / Sellers 50 — Neutral pressure', 'Trap prob. 45%') with no data-quality badge on those tabs, identical to a real measured market.

- **Evidence:** api-server/src/brain/timing/trapRoomEngine.ts:38-48 returns `{ trapProbability: fakeoutRisk, roomToMove: 50, buyPressure: 50, sellPressure: 50 }` when candles.length < 15; fakeoutRisk is a per-session constant 40-60 (api-server/src/brain/timing/sessionKillZoneEngine.ts:26-59). trading-dashboard/src/pages/market-heat-map.tsx:617-662 (BuySellWindowsTab) renders the meter, pills and 'Neutral pressure' verdict, and market-heat-map.tsx:716-719 (DangerWindowsTab) renders 'Trap prob.' — neither tab renders DataQualityBadge (it appears only on the Now/By-Symbol/Admin tabs, market-heat-map.tsx:229,470). tradeabilityEngine.ts:66 also seeds edge=50 base with no candle modifiers, and entryPermission 'GO' is reachable from edge(50)*0.6 + session bonus alone (tradeabilityEngine.ts:89,129-140).
- **Suggested fix:** Thread dataQuality into every tab (render the badge, or null-out pressure/trap/room fields in the read when hasCandleData is false so the UI's existing '—' path fires) instead of emitting midpoint numbers.

### H6 [DEAD_GAUGE] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written). — **CONFIRMED**

The Open Paper Trades 'P&L' column on the trader cockpit shows the database default 0 for every OPEN position, always — a position deep underwater reads as flat, in neutral color, for its entire open life.

- **Evidence:** trading-dashboard/src/pages/trading-cockpit.tsx:233 renders o.profitLoss → api-server/src/routes/tradingCockpit.ts:87-88 selects paperOrdersTable.profitLoss where status='OPEN' → lib/db/src/schema/paperTrading.ts:41 profitLoss real('profit_loss').notNull().default(0) → the only writers set it at close: api-server/src/lib/paperExecution/paperExecutionMonitor.ts:117,180 and api-server/src/routes/paperTrading.ts:110-113,293-296. The monitor DOES compute unrealized P&L (paperExecutionMonitor.ts:102-107) but only returns it in its own HTTP response — it is never persisted for OPEN orders and never reaches the cockpit summary.
- **Suggested fix:** In /trading-cockpit/summary, compute unrealized P/L per open order from a fresh DD quote at read time (the honest getMarketData already exists), returning {value, asOf, quality} and a typed null with reason when the quote is MISSING/stale; render '—' with the reason instead of 0. Alternatively persist the monitor's unrealizedPnl with a computedAt timestamp and show its age.

### H7 [DEAD_GAUGE] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written). — **CONFIRMED**

Active-session 'Net P&L (cents)' and 'Trades open/closed' never move when paper trades actually open or close — their only writer is a manual link endpoint nothing calls — and the session loss limit shown to the user is enforced against this same dead number, so it can never trip via real trading.

- **Evidence:** trading-cockpit.tsx:196-197 → tradingCockpit.ts:195-198 → api-server/src/lib/paperSession/manager.ts:288-290 (reads row values). Sole writer: manager.ts:518-531 (linkTradeToActiveSession), reachable only via POST /api/paper-sessions/link-trade (api-server/src/routes/paperSessions.ts:177-181). Grep proves no caller: paperExecution/, paperAutopilot/, routes/paperTrading.ts, autoDebriefService.ts and the entire trading-dashboard contain zero references to linkTradeToActiveSession or link-trade. Loss-limit enforcement reads the same dead netPnl: manager.ts:457 and :489 (sessionLossLimitBreached(cur.netPnl, ...)).
- **Suggested fix:** Call linkTradeToActiveSession (or an equivalent session-scoped aggregator) from the actual open/close paths (paperExecutionService open, paperExecutionMonitor/paperTrading close), or derive the session panel's numbers at read time from paper_orders rows joined to the session window.

### H8 [CONFIDENT_ABSENT] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written). — **CONFIRMED**

When the alert-manager read throws, the cockpit renders 'Unread: 0 / Critical: 0', shows no critical banner, and the ACK_CRITICAL safety gate silently passes — offering 'Start Paper Session' — because the failure fallback is a confident empty list, not an unknown.

- **Evidence:** tradingCockpit.ts:71-72 safeCall('criticalUnread', ..., []) and safeCall('unreadAll', ..., 0) swallow failures into all-clear values → pickNextBestAction (tradingCockpit.ts:51) gates the BLOCK action on criticalUnread.length which is now 0 → UI trading-cockpit.tsx:107-122 shows no critical alert banner, :292-293 shows the zero counts, and :300-303 even renders the 'none were written' honesty copy for a state that is actually 'read failed'. No error/unknown discriminator travels in the payload.
- **Suggested fix:** Make safeCall fallbacks typed unknowns ({status:'READ_FAILED', reason}) for the alert counts; render '?' with the reason and treat READ_FAILED as blocking for the start-session next-best-action, matching the owner's fail-closed rule.

### H9 [CONFIDENT_ABSENT] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written). — **CONFIRMED**

The dashboard 'ARX AI Cockpit' account card renders Balance $0.00 / Equity $0.00 / Open P/L $0.00 / Open Trades 0 as confident fact on a real-money surface when the summary read fails or the master EA has never reported open P/L — the card has no error state, and the shared formatPnl() turns null into '$0.00'.

- **Evidence:** trading-dashboard/src/components/dashboard/cockpit/CockpitCards.tsx:91-353 AccountSnapshotCard has no isError branch (loading gate only at :223); on a failed useGetMeSharedAccountSummary, accounts=[] so the reduces at :112-116 manufacture 0 (not null) → :146 balanceValue=0 → money(0)='$0.00' (money() at :62-63 only guards null), :147 equity likewise; :253 formatPnl(openPnl) where openPnl can be null on the master path (:148 with master.mt5OpenPnl null) and trading-dashboard/src/lib/format.ts:8-12 returns '$0.00' for null (formatPrice/formatPercent at :3-6,:14-17 have the same null→'0.00' sin); :262 Open Trades = String(openCount ?? 0). Contrast the honest sibling: position rows render '—' for absent P/L at :742.
- **Suggested fix:** Give the card an explicit error/unknown state; make the empty-read reduces yield null; change formatPnl/formatPrice/formatPercent to return '—' for null (the admin cockpit's fmtPl/fmtMoney at components/admin/cockpit/cockpitShared.tsx:39-56 already do this correctly — reuse them).

### H10 [FABRICATED] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f — **CONFIRMED**

POST /positions/:id/close (legacy Build-H live-position router, mounted unconditionally) marks a broker-mirrored position MANUALLY_CLOSED and writes trades.status=CLOSED_WIN/LOSS with pnl derived from the last-synced UNREALIZED mark, stamped pnlStatus='COMPUTED', while the 'CLOSE' it queues can never reach the broker — queueMt5CommandWithGate hardcodes every command to status=BLOCKED. The position displays closed contrary to broker truth and a fabricated realized P/L flows into the trades history surface as a trusted number.

- **Evidence:** artifacts/api-server/src/routes/livePositions.ts:483-490 (MANUALLY_CLOSED+closedAt stamped immediately after queue), :503-511 (pnl = realizedProfitLoss ?? unrealizedProfitLoss, pnlStatus:'COMPUTED'); artifacts/api-server/src/routes/mt5.ts:1376-1390 (status='BLOCKED' forced, EA poll filters PENDING so the CLOSE is never delivered); routes/index.ts:257 (router mounted); consumer UI exists but is unmounted: artifacts/trading-dashboard/src/components/positions/ClosePositionConfirmModal.tsx:14-20 (useClosePosition); display chain trusts COMPUTED: artifacts/trading-dashboard/src/components/trading/RealizedPnl.tsx:34 (only pnlStatus==='UNKNOWN' renders 'P/L unavailable'). This is the surviving remnant of the old 'Live-Trades Close is a mock' defect — the fix holds on LiveTradeCard/tradeManagement (LiveTradeCard.tsx:421-430; tradeManagement.ts:46-87 refuses LIVE rows) and on meTrades LIVE closes (meTrades.ts:434-513, real Phase-B dispatch), but NOT on this route.
- **Suggested fix:** Make /positions/:id/close (and /stop-loss, /take-profit) refuse when the queued command comes back BLOCKED (surface mode/blockedReason from queueMt5CommandWithGate), never stamp MANUALLY_CLOSED/closedAt on an undelivered command, and never write trades.pnl from unrealizedProfitLoss with pnlStatus COMPUTED — use pnlStatus UNKNOWN + dataQualityFlag, or retire the router.

### H11 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f — **CONFIRMED**

LiveOpenTradesPanel (Live Trades page, real per-user live slot positions) renders 'No open trades right now.' with a confident (0) count whenever the /api/me/live/slot-summary read fails — the queryFn never checks r.ok and the error branch is not handled, so a 500/401/network failure looks identical to being flat; this is the exact P0-3 pattern the sibling OpenLivePositions component documents and fixes.

- **Evidence:** artifacts/trading-dashboard/src/components/live/LiveOpenTradesPanel.tsx:50 (fetch(...).then(r=>r.json()), no r.ok check), :57 (positions = q.data?.positions ?? []), :82-85 ('No open trades right now.' whenever !isLoading && length===0, no isError branch); contrast the fixed pattern and its rationale at artifacts/trading-dashboard/src/components/live/OpenLivePositions.tsx:36-50 and :150-172 ('Do not assume you are flat'); mounted at artifacts/trading-dashboard/src/pages/live-trades.tsx:53.
- **Suggested fix:** Throw on !r.ok and add an isError branch mirroring OpenLivePositions ('couldn't load — do not assume you are flat'), plus '(count unavailable)' in the header while loading/errored.

### H12 [GUESSED] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f — **CONFIRMED**

When an ARX CLOSE_LIVE_POSITION command fills, the mission outcome recorder is handed realisedPnl = the position row's LAST-SYNCED floatingPl (a floating mark of unbounded age, not a broker close fill P/L) with no outcomeStatus, and the recorder defaults that to status 'RECONCILED' — the label the completeness module defines as 'closed AND carrying a broker-confirmed P/L — the figures you can trust' — directly violating the brokerCloseOutcome honesty spine ('we never derive it from the last observed floating P/L').

- **Evidence:** artifacts/api-server/src/lib/live/liveCommandPipeline.ts:5246-5262 (realisedPnl = closedRows[0].floatingPl, outcomeSource 'ARX_CLOSE_FILL', no outcomeStatus passed; the update at :5226-5233 writes only closedAt/lastSyncedAt — no fill P/L ever lands in floatingPl first); artifacts/api-server/src/lib/missionExitManager.ts:1254 (status: args.outcomeStatus ?? (isNum(realisedPnl) ? 'RECONCILED' : 'UNRECONCILED')); spine it violates: artifacts/api-server/src/lib/live/brokerCloseOutcome.ts:16-18; the trusted-label definition: artifacts/api-server/src/lib/missionOutcomeCompleteness.ts:65-66. Contrast: the parallel ledger posting honestly labels the same number LOCAL_EXECUTION (liveCommandPipeline.ts:5271-5275).
- **Suggested fix:** Pass an explicit outcomeStatus reflecting provenance (e.g. a new 'LOCAL_MARK' status, or UNRECONCILED until the broker close report/absence reconciler supplies the venue figure), or only mark RECONCILED when a broker-reported close P/L was ingested.

### H13 [DEAD_GAUGE] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines — **CONFIRMED**

The mission Risk Control card's 'Trades today', 'Consecutive losses', 'Daily budget used' meter, behavioral cooldown, and the protective mode ladder they drive are computed from the user's MANUAL paper-trade journal (paper_trades), never from the mission's own executed trades — so a mission on a real losing streak shows 'Consecutive losses: 0 / Daily budget used: 0%' and its cooldown/stop ladder never fires.

- **Evidence:** artifacts/api-server/src/lib/missionRiskService.ts:290-296 (refreshMissionRisk reads paperTradesTable only) → aggregateHistory feeds computeMissionRiskState:182-234; the ONLY writer of paper_trades is the manual journal route artifacts/api-server/src/routes/mePaperTrades.ts:150; mission trades land elsewhere (sim_* family: artifacts/api-server/src/lib/missionSimulatedFills.ts:254-269,491-511; broker closes: missionTradeDraftsTable.pnl via missionExitManager) and never enter these counters; rendered as fact at artifacts/trading-dashboard/src/pages/profit-missions.tsx:2082-2096, and the same wrong-book state gates dispatch via composeMissionGate (artifacts/api-server/src/lib/missionExecution.ts)
- **Suggested fix:** Feed aggregateHistory from the mission's own closed trades (sim family for paper/demo, broker-reconciled for live) or render the counters with an explicit 'paper-journal only' label and an honest unknown for missions

### H14 [MOCK_LEAK] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines — **CONFIRMED**

The Performance view and Testing Lab caption FORWARD results as 'Real closed trades' / 'real forward result' / 'your mission's real, closed trades', but on a paper/demo mission the FORWARD aggregate is built from SIMULATED modelled closes; the persisted evidenceBasis (SIMULATED/MIXED/BROKER_RECONCILED) is never rendered anywhere on the frontend, so simulated performance reads as real track record.

- **Evidence:** artifacts/api-server/src/lib/missionTestingLabService.ts:349-366 (forward aggregation includes simulated sim_pnl/sim_closed_at rows) and :361-366,416 (evidenceBasis recorded); grep shows zero frontend consumers of evidenceBasis; captions at artifacts/trading-dashboard/src/components/missions/MissionPerformanceView.tsx:128,131-134,166 ('Real closed trades'),176,185-186 and artifacts/trading-dashboard/src/pages/profit-missions.tsx:2376-2378,2463-2465
- **Suggested fix:** Render evidenceBasis on every FORWARD result (badge + copy: 'simulated paper/demo closes, modelled from real quotes') and correct the 'real closed trades' captions

### H15 [MOCK_LEAK] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f) — **CONFIRMED**

The Trade Command Room's Quick Trade panel shows a big SELL/BUY price strip and a dollar 'Risk approx.' computed from the in-memory market SIMULATOR, unlabeled, even while the page banner reads 'LIVE · Shared Master MT5'.

- **Evidence:** artifacts/trading-dashboard/src/pages/trade-command-room.tsx:87 fetches /api/market/quote/:symbol → artifacts/api-server/src/routes/market.ts:26-30 serves marketSimulator.quote() (only handler for that path; artifacts/api-server/src/lib/marketSimulator.ts:107-120 is a random-walk). QuickTradePanel renders quote.bid/ask/spread as the executable price strip with no simulator tag (artifacts/trading-dashboard/src/components/dashboard/trade/QuickTradePanel.tsx:55-57,112-126) and riskApprox '$N' is derived from that fake mid (trade-command-room.tsx:112-127). The payload's executionEnvironment:'SIMULATOR' field is never displayed; the page's ChartFeedConfidence chip describes the chart feed, not this quote. grep shows zero 'simulat' matches in trade-command-room.tsx.
- **Suggested fix:** Route the strip through the shared chart-truth/marketDataRouter quote (like getMarketSnapshot does), or render the quote's executionEnvironment tag next to the prices and suppress riskApprox when the source is SIMULATOR.

### H16 [CONFIDENT_ABSENT] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f) — **CONFIRMED**

When the open-trades lookup throws, Ruby's briefing tells the user 'No positions are currently open.' — a failed read degrades to a confident zero instead of an honest unknown, on an account that may hold live broker positions.

- **Evidence:** artifacts/api-server/src/lib/assistant/rubyContext.ts:319-325 (catch { account.openPositions = 0 }; the field has no null state, initialized 0 at line 287) → composeRubyBriefing artifacts/api-server/src/lib/assistant/rubyContext.ts:551-565 renders openPositions===0 as 'No positions are currently open.' → served verbatim as an assistant message via GET /me/assistant/briefing (artifacts/api-server/src/routes/meAssistant.ts:260-262) and injected into the chat panel (artifacts/trading-dashboard/src/components/help/ArxAssistantLivePanel.tsx:368-378).
- **Suggested fix:** Make openPositions nullable; on error say 'I can't verify your open positions right now' (mirror the openPL null handling two blocks below, which already does this correctly).

### H17 [CONFIDENT_ABSENT] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f) — **CONFIRMED**

With the economic-calendar provider disconnected, the pre-trade News Risk Check panel shows a green '· none' risk badge and the sentence 'No major scheduled news for <symbol> in the current window.' — asserting quiet when the system is actually blind (a real high-impact event could be minutes away).

- **Evidence:** artifacts/api-server/src/lib/news/newsIntelligenceService.ts:201-221 — calendar.connected:false ⇒ scorableEvents=[] ⇒ scoreNewsRisk yields riskLevel 'none'; summarizeWarning at lines 126-128 then emits the affirmative 'No major scheduled news…' string without distinguishing disconnected from genuinely quiet. Rendered at artifacts/trading-dashboard/src/components/trading/NewsRiskCheckPanel.tsx:87-92 (collapsed header '· none' in text-success with no caveat) and 114 (warningSummary); the honest 'calendar provider not configured' line (172-176) only appears when the panel is expanded (defaultOpen=false). Contrast: Ruby's own briefing path handles this correctly via newsRiskStatement (rubyContext.ts:489-501).
- **Suggested fix:** In summarizeWarning, branch on calendar connectivity: when disconnected say 'calendar feed unavailable — news risk unknown' and render the header badge as 'unknown' (muted), not green 'none'.

### H18 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch. — **CONFIRMED**

The System Health page renders 'Live trading: DISABLED' and 'Mode: PAPER_ONLY' badges that are compile-time constants — the health report never reads the tables that actually gate dispatch (global_trading_settings.liveBrokerExecutionArmed, arx_live_arming, ARX_LIVE_BROKER_EXECUTION_ENABLED), so when an operator arms live execution the admin health surface still swears live trading is off.

- **Evidence:** artifacts/trading-dashboard/src/pages/system-health.tsx:68-69 renders report.liveTradingStatus/report.mode → artifacts/api-server/src/lib/systemHealth/health.ts:88-89 types them as literal "DISABLED"/"PAPER_ONLY", :403 emits them as constants, and probeSafety :309-312 hard-codes canPlaceTrades:false, liveTradingAllowed:false, brokerMode:"READ_ONLY"; envelope constants also at artifacts/api-server/src/routes/systemHealth.ts:18-21. Meanwhile the real arm switch is env+DB in artifacts/api-server/src/lib/live/phaseBConfig.ts:27-50 and live dispatch really inserts MT5 commands at artifacts/api-server/src/lib/live/liveCommandPipeline.ts:4713 (wired via routes/tradesLiveShared.ts, meTrades.ts, meOneClick.ts). Also static subtitle at system-health.tsx:49.
- **Suggested fix:** Have probeSafety/runHealthCheck read resolveLiveBrokerExecutionEnabledAsync() + arx_live_arming and report the real armed/disarmed state (or rename the fields to policy assertions and drop the badges); at minimum render 'reported by legacy Build MM, does not reflect Phase B arming' next to the badges.

### H19 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch. — **CONFIRMED**

The ARX Status Command Center (header: 'Real app state') always shows 'Emergency Stop: off' — emergencyStopActive is hard-coded false in the runtime context, so when the platform kill switch in safety_core is engaged, this page, its Setup Wizard ('Emergency Stop not engaged.'), its blocker cards, and the App Doctor all report no stop.

- **Evidence:** artifacts/trading-dashboard/src/assistant/runtimeContext.ts:143 (`emergencyStopActive: false, // surfaced by bridge if/when server exposes it`) → rendered at artifacts/trading-dashboard/src/pages/status-command-center.tsx:381; downstream consumers that can therefore never fire: artifacts/trading-dashboard/src/statusCommand/setupWizard.ts:212-213, statusCommand/blockerCards.ts:63-74, assistant/appDoctor.ts:22-26, statusCommand/readinessScore.ts:214. The real state exists and is served live to emergency.tsx via /api/system/status (pages/emergency.tsx:74-91, backed by safety_core.kill_switch_engaged).
- **Suggested fix:** Fetch killSwitchEngaged from /api/system/status (or add it to /api/app/health-summary) into RuntimeContext; until then render 'unknown' rather than 'off'.

### H20 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch. — **CONFIRMED**

The Release Status page's 'Real broker locked: OK' pill is derived from a hard-coded constant (realBrokerExecutionAvailable: false in versionEnvelope), never from the actual arm switch — it will keep asserting the broker is locked even after an operator arms live execution.

- **Evidence:** artifacts/trading-dashboard/src/pages/release-status.tsx:37 (`<Pill ok={!v.realBrokerExecutionAvailable} label="Real broker locked" />`) ← artifacts/api-server/src/routes/release.ts:18-20 → artifacts/api-server/src/lib/release.ts:23-24 (constants false), :96 (readinessReport repeats it); real lock state lives in artifacts/api-server/src/lib/live/phaseBConfig.ts:36-50.
- **Suggested fix:** Derive the pill from resolveLiveBrokerExecutionEnabledAsync() or delete it; a lock indicator must read the lock.

### H21 [MOCK_LEAK] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch. — **REFUTED**

Found while tracing feed status (adjacent surface): the Live Manual page's order ticket displays BID/ASK/SPREAD from the marketSimulator and computes dollar 'Estimated max loss/gain' from the simulated mid, with no 'simulated' label — a user sizes a live intent off invented prices.

- **Evidence:** artifacts/trading-dashboard/src/pages/live-manual.tsx:55 fetches /api/market/quote/:symbol → artifacts/api-server/src/routes/market.ts:27 (`marketSimulator.quote(...)`); rendered unlabeled at live-manual.tsx:121-126 (ticket-quote) and $-derived at :73-74 → :154-155. trade-command-room.tsx:87 consumes the same simulator quote.
- **Suggested fix:** Label the ticket quote SIMULATED (the endpoint already knows: /market/symbols returns executionEnvironment:"SIMULATOR") or switch the ticket to the honest candle/quote truth used by useScannerTruth.

### H22 [DEAD_GAUGE] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts — **CONFIRMED**

My Account's entire 'My P/L' block (Today's/7d/total P/L, Trades/Wins/Losses Today) and the 'Daily Loss Remaining' risk tile are fed by performance_daily, a table with NO production writer — so every user permanently sees $0 P/L and a full daily-loss allowance, even after real live losses.

- **Evidence:** artifacts/api-server/src/routes/meAccountShell.ts:285-304 reads performanceDailyTable for closedPnlToday/tradesToday/winsToday/lossesToday; :405-407 computes dailyLossRemaining = max(0, limit − max(0, −closedPnlToday)) from that always-zero figure; repo-wide grep shows the ONLY insert into performanceDailyTable is a QA fixture (scripts/src/qaPerUserPrivateWorkspace.ts:146) — no worker, route, or seam writes it (contrast performance_daily_snapshots, written by artifacts/api-server/src/lib/performanceCC/rebuild.ts:47). Surface: artifacts/trading-dashboard/src/pages/my-account.tsx:299-306 renders the zeros as '+$0' facts and :320 renders Daily Loss Remaining as a confident dollar figure.
- **Suggested fix:** Compute the shell's pnl block from the same trusted source /performance/summary uses (tradesTable with pnlStatus='UNKNOWN' exclusion and scope labeling), or return typed nulls with a reason and render '—'; derive dailyLossRemaining from a real day-P/L read or report it unknown — never from a field pinned at zero.

### H23 [CONFIDENT_ABSENT] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts — **CONFIRMED**

When the /me/shared-account/summary read fails, the dashboard Account Snapshot renders a fully confident all-zero account — 'Available $0.00', 'Equity $0.00', 'Open P/L $0.00', 'Open Trades 0' — with no unknown state and no warning (the allocation notes are gated on allocationView != null, so they vanish too).

- **Evidence:** artifacts/trading-dashboard/src/components/dashboard/cockpit/CockpitCards.tsx:96-118 — summaryQ.isError is never consulted; accounts=[] makes the alloc reduces at :111-116 sum to 0; :133 falls back canonicalAvailable ?? alloc.balance → 0; tiles at :241-242 and :262 print money(0)/'0'; loading prop is `summaryQ.isLoading && !summary` so the error state renders the body.
- **Suggested fix:** On summaryQ.isError or absent data render '—' tiles plus a 'could not read your account' line, per the moneyBasis.ts contract already used by the Trading Permission card in the same file.

### H24 [CONFIDENT_ABSENT] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts — **CONFIRMED**

A failed positions read renders as a confident flat book: the cockpit Open Positions card shows 'No open positions / You have no open trades right now' with badge 0, and the Analytics 'Exposure by Market' card shows 'No open market exposure right now', whenever useGetMeSharedAccountPositions errors (with the SSE snapshot also absent).

- **Evidence:** artifacts/trading-dashboard/src/components/dashboard/cockpit/CockpitCards.tsx:677 (`posQ.data?.rows ?? []` — isError unused) → :715-720 empty state; artifacts/trading-dashboard/src/pages/analytics.tsx:72 same fallback → :424 'No open market exposure right now.' and the green 'Exposure: Clear' chip at :306. TodayPerformanceCard has the sibling defect: a failed /performance/summary read prints 'No trade has closed today yet.' (CockpitCards.tsx:769-786, esp. :781).
- **Suggested fix:** Distinguish error/loading from a genuinely-empty read: on isError show 'positions could not be read — this is not a statement that you are flat', matching the LedgerBasisStrip's failed-read copy.

### H25 [DEAD_GAUGE] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern) — **CONFIRMED**

The entire onboarding surface tells every user — including live-armed ones — that live trading is impossible: the API envelope hard-codes appMode:'PAPER_ONLY', liveTradingStatus:'DISABLED', canPlaceLiveTrade:false on every response, the step catalogue says 'Nothing here can place real trades' / 'this app cannot enable live trading', and the page footer renders a permanent 'DEMO_ONLY · LIVE TRADING DISABLED' badge — on a build where real broker orders dispatch.

- **Evidence:** artifacts/api-server/src/routes/onboarding.ts:22-32 (constant envelope) and :96-98 (ack result liveTradingStatus:'DISABLED'); artifacts/api-server/src/lib/onboarding/steps.ts:18 (SAFETY constant stamped on all 16 steps) and the 'welcome'/'ack-live-disabled' step copy ('Nothing here can place real trades', 'The LIVE TRADING DISABLED badge is always shown'); artifacts/api-server/src/lib/onboarding/state.ts:92,115,180-181 (alerts + audit rows asserting 'Live trading remains DISABLED'); artifacts/trading-dashboard/src/pages/onboarding.tsx:86 ('Live trading remains DISABLED') and :141 ('DEMO_ONLY · LIVE TRADING DISABLED'). Falsity proven by: artifacts/api-server/src/lib/live/liveCommandPipeline.ts:4131 (executionAdapter.deliver() dispatches real orders), lib/phase6/guidedDispatchEntry.ts, computeAccountModePrecedence.ts:17,68 (LIVE_SHARED armed mode), and the repo's own RANK-4 postmortems (lib/onboarding/whyBlocked.ts:5-41, lib/onboarding/help.ts:3-30) which fixed this identical lie on the Help/WhyBlocked surfaces but left the onboarding routes/steps/page untouched. Internally contradictory on the same page: onboarding.tsx:23-24 acknowledges 'Live Trading Active' as a possible admin-set mode.
- **Suggested fix:** Delete the hard-coded envelope constants and footer badge; have /api/onboarding/status embed the user's real mode from computeAccountModePrecedence / readLiveReadiness (the honest chain already built for whyBlocked.ts), and rewrite steps.ts copy to the default-deny truth used in help.ts (live is possible, gated, operator/admin-controlled).

### H26 [CONFIDENT_ABSENT] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern) — **CONFIRMED**

Both notification surfaces render a confident 'No notifications yet' empty state (and a clean bell with no unread badge) when the read failed — a 401/500/network error is indistinguishable from a genuinely clear alert queue, on the exact surface onboarding tells users to check for CRITICAL live-risk alerts before trading.

- **Evidence:** artifacts/trading-dashboard/src/pages/notifications.tsx:68-82 (refresh() has no res.ok/catch handling; a rejected fetch or {error} body leaves items=[] with no error state) and :252-256 (visible.length===0 renders EmptyState 'No notifications yet.'); criticalUnread at :106 is derived from that same empty list. artifacts/trading-dashboard/src/components/NotificationCenter.tsx:43 (unread = q.data?.unread ?? 0 hides the badge on q.isError) and :66-71 ('No notifications yet' rendered whenever !q.isLoading && list.length===0, including isError) — mounted in the global header (components/layout/AppLayout.tsx:645). Stakes set by the product's own copy: lib/onboarding/steps.ts 'critical-alerts' REQUIRED step and lib/onboarding/help.ts:93 (CRITICAL alerts = live-risk event, bridge failure during an open trade).
- **Suggested fix:** Branch on q.isError / failed refresh: show 'Couldn't load your alerts — treat this as unknown, not as clear' with a retry, and never render the no-notifications empty state (or a zero badge) unless a successful response returned an empty list.


## MEDIUM

### M1 [CONFIDENT_ABSENT] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

Masked simulator rows are degraded to CONFIDENT ZEROS instead of typed nulls — the results card renders "Conf 0 / Risk 0 / Sniper 0" and "<label> 0" as if they were measured scores, and clicking BUY/SELL/Trade Setup seeds the trade ticket with entry=0, stopLoss=0, takeProfit=0 (the SL/TP inputs literally show "0").

- **Evidence:** artifacts/api-server/src/lib/honesty/feedTruthCopy.ts:104-113 (signalStrength/confidenceScore/riskScore/entrySniperScore/entry/stopLoss/takeProfit all set to 0, not null) → artifacts/trading-dashboard/src/pages/market-scanner.tsx:720-735 (zeros rendered in the Conf/Risk/Sniper number cells), :689 (label + score 0 badge), :1033-1043 (zeros passed as signal into ScannerTradeModal) → artifacts/trading-dashboard/src/components/scanner/ScannerTradeModal.tsx:265-270 (entry/SL/TP seeded via String(signal.entry) etc., so 0 becomes the visible "0" value)
- **Suggested fix:** Make the masked fields nullable in the contract (or add a withheld:true flag the card checks) and render em-dashes with the existing waiting-for-feed reason; never seed the trade ticket from a masked row.

### M2 [STALE_UNLABELED] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

A fresh forming-tip tick overrides the closed-bar staleness verdict entirely: if the closed candle feed stalls (trailing ≥2-3 intervals — e.g. EA keeps pushing ticks but candle pushes stop) while live ticks still flow, buildFeedStatus returns quality=clean/stale=false/aiUsable=true and isLive=true, so the chart badge reads LIVE, scanner truth resolves "Live, confirmed candles" and actionable=true, despite a multi-interval hole between the newest closed bar and the synthesized tip (missingCandleCount only counts internal gaps, and the trailing gap that would have said delayed/stale is discarded).

- **Evidence:** artifacts/api-server/src/lib/data/freshness.ts:256-257+274-283 (formingTipPresent branch replaces the trailing-interval freshness verdict with tick-age only) ← artifacts/api-server/src/lib/data/chart/chartDataService.ts:310-322 (tip appended whenever tipOpenMs > lastClosedOpenMs, with no cap on how far the closed feed trails) and :381-383 (isLive = reachable && !verdict.stale when tip present); tail gap not counted: artifacts/api-server/src/lib/data/chart/candleNormalization.ts:554 (missing counted only between consecutive returned bars) + chartDataService.ts:285 (trailing computed then overridden)
- **Suggested fix:** Only let the forming tip upgrade freshness when the closed bars themselves are within the delayed threshold (trailing <= DELAYED_TRAILING_INTERVALS); otherwise keep the closed-bar stale/delayed verdict (tip may still render, labeled).

### M3 [STALE_UNLABELED] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

Open-position rows on the scanner chart (side, lots, entry, floating P/L rendered green/red to 2dp) carry no freshness signal, and the server's 90s stale filter is RELATIVE to the newest sync — so if the EA bridge dies entirely, hours-old floatingPnl/currentPrice keep rendering (all rows pass the relative floor) next to a chart whose candle badge can legitimately say LIVE (candles can come from Deriv/third-party while position sync is dead), with Close/50%/Break-even/Reverse buttons acting on that stale picture.

- **Evidence:** artifacts/api-server/src/routes/mePositionsUnified.ts:86-96 (liveFloor = newestLiveSync - 90_000; when the whole sync is stale every row still passes; no lastSyncedAt/age emitted to the client) → artifacts/trading-dashboard/src/components/scanner/ScannerChartPanel.tsx:2782-2805 (position row renders floatingPnl.toFixed(2) with no age/staleness affordance; trade buttons alongside), :1061-1090 (10s poll swallows failures by clearing, but a stale-yet-successful read is indistinguishable from fresh)
- **Suggested fix:** Emit lastSyncedAt (or ageMs) per position and an overall syncStale flag when newestLiveSync itself is older than the threshold; scanner chart should badge stale P/L ("as of Xm ago") and warn before Close/Reverse.

### M4 [GUESSED] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

Admin market-data diagnostics report deriv `authorized: true` whenever ticks are flowing — the flag is derived from feed health, not from an authorize — so a new-mode DERIV_PUBLIC_DATA_ONLY session (which by Ruling 15 deliberately NEVER authorizes; commit 1176d61) presents as an authorized session to the operator, and conversely a genuinely authorized session with no recent tick reports authorized:false.

- **Evidence:** artifacts/api-server/src/lib/data/marketDataRouter.ts:885 (authorized: deriv.healthSummary === "healthy") ← artifacts/api-server/src/lib/data/providers/derivProvider.ts:213-221 (healthSummary is "healthy" exactly when feedReadinessState === LIVE_FEED, i.e. hasRecentTick) ← derivWsClient.ts:275-278 (public-data-only session sets the DERIV_PUBLIC_DATA_ONLY sentinel, never authorizes); consumed by artifacts/api-server/src/routes/adminMarketDataDiagnostics.ts:48. User-facing scanner surfaces stay honest (feed-confidence.ts:201-207 labels deriv as synthetic tier, "not your broker's live bars"; scannerTruth.ts:595 brokerFeedActive only on mt5_broker; provenance environment stays "unknown" at marketDataRouter.ts:477-484).
- **Suggested fix:** Report authorized from the client's real authorize state (client.isAuthorized()), and add a distinct publicDataOnly flag so the diagnostics can say "public data, unauthorized session" truthfully.

### M5 [CONFIDENT_ABSENT] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

A basket's 'Open P/L' can be a partial sum shown as the whole: combinedFloatingPl sums only the legs that have a floatingPl (plKnown is true if ANY leg has one), so a 3-leg basket with one known leg shows that single leg's P/L as the basket total, and the exit-urgency ladder ('in profit' vs 'losing' branches) reasons from the same partial number.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpManage.ts:143-159 (summarizeBasket: pl += only when num(leg.floatingPl); plKnown = any leg) → :461 combinedFloatingPl; exit verdict uses it as basket truth at scalpManage.ts:355-357 (inProfit/losing); displayed unqualified at trading-dashboard/src/components/scanner/RubyScalpBasketPanel.tsx:35-36,70-73.
- **Suggested fix:** Track knownLegs vs entryCount in summarizeBasket; when partial, return combinedFloatingPl=null (or a {value, partial:true} shape) and have the panel show 'P/L incomplete — N of M legs reporting'.

### M6 [CONFIDENT_ABSENT] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

A journal close can be labeled plQuality 'KNOWN' (broker realised figure, shown with no caveat) from a partial sum: realized P/L is summed over only the legs found closed in arx_live_positions, so legs whose rows were dropped by the staleness filter or never marked closed silently vanish from the 'realised' number.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpJournalService.ts:186-207 (finalizeClosedEntry: closed = rows matching tickets AND isNotNull(closedAt); realizedPl = sum of whatever matched, any=true on one leg) → deriveResult marks it KNOWN (scalpJournal.ts:199-204); UI renders KNOWN with no note: trading-dashboard/src/pages/scalp-journal.tsx:67-69.
- **Suggested fix:** Mark plQuality KNOWN only when every legTicket matched a closed row; otherwise ESTIMATED with the existing 'estimated from the last reading' note.

### M7 [CONFIDENT_ABSENT] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

Non-actionable cards (including 'Waiting for live data' and 'Market closed') still show confident green risk chips — 'Spread LOW · Slippage LOW · News LOW' — because rejectResult hard-codes LOW risk bands when no data was read, instead of an honest unknown.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpEngine.ts:170-172 (rejectResult: spreadRisk 'LOW', slippageRisk 'LOW', newsRisk defaults 'LOW' when scanner absent); chips render unconditionally for every status at trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:354-369, with tooltips claiming a live reading ('right now').
- **Suggested fix:** Make the risk bands nullable on reject results and hide the chips (nullBehavior='hide', as the SetupSignalStrip already does) when the engine never evaluated them.

### M8 [DEAD_GAUGE] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

The scalp-type classifier and the momentum-mode scoring are wired to a dead input: oppToScannerInput never maps trendStrength, so the engine's trend is always the constant 50 — in ANY mode the card badge always reads 'Sniper Scalp' (Momentum/Reversal labels are unreachable), the FAST and MOMENTUM mode tilts are always exactly 0, and REVERSAL always gets a flat +1.5, while the UI presents these as distinct live styles.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpService.ts:154-174 (oppToScannerInput — the only production producer of ScalpScannerInput — omits trendStrength; the field is optional at scalpTypes.ts:415); scalpEngine.ts:385 (trend = 50 fallback), :388-392 (mode tilts on trend), :783-788 (scalpTypeLabel: trend 50 → always 'Sniper Scalp'); badge displayed at trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:213; style picker at RubyScalpScan.tsx:163-172.
- **Suggested fix:** Map the scanner's real trend strength (marketScanner analysis a.trendStrength) into oppToScannerInput, or derive trend from the flame's candle metrics; until then drop the ANY-mode Momentum/Reversal branches so the label matches what actually runs.

### M9 [GUESSED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

The 'Entry zone X – Y' shown as a precise price range is synthesized from constants (entry ± 2·spread + 5·points), not market structure — the analyzer's genuine structural entry zone is dropped upstream — and the READY vs WAIT_FOR_ENTRY verdict plus pullback re-entry timing key off this invented band, with no 'estimated' labeling.

- **Evidence:** artifacts/api-server/src/lib/marketScanner.ts:1372 collapses a.entryZone to a single entry (ScannerOpportunity has no zone, :1545-1572); scalpService.ts:154-174 passes no entryZone; scalpEngine.ts:339-346 fabricates zoneFrom/zoneTo from spreadPrice*2 + point*5 and derives inZone/status from it; displayed as a hard range at trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:176-179 and 283-291.
- **Suggested fix:** Carry the analyzer's entryZone.low/high through ScannerOpportunity into the engine; if unavailable, label the band as an approximate buffer ('≈ entry ± spread buffer').

### M10 [STALE_UNLABELED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

The 'Spread' chip (tooltip: 'how wide the gap is right now') and the SPREAD_TOO_WIDE safety gate run on arx_symbol_specs.spreadPoints — a 'last known spread snapshot' from the EA report — read with no freshness check on reportedAt, so a stale LOW snapshot during a live spread spike shows 'Spread LOW' and lets the gate pass; the flame's live-spread-spike downgrade can never fire because loadExecutionInput never populates liveSpreadPoints/latencyMs.

- **Evidence:** Snapshot semantics: artifacts/api-server/src/lib/mt5/brokerSymbolSpec.ts:28-29 ('Last known spread snapshot… when reported'); read without age check: scalpServiceInputs.ts:33-68 (loadSpecInput, spreadPoints at :65, ignores reportedAt); used for the gate/bands/buffers at scalpEngine.ts:290,295,307-314,328; dead spike-guard: flameRead.ts:133-140 requires exec.liveSpreadPoints which loadExecutionInput (scalpServiceInputs.ts:117-137) never sets; 'right now' tooltip at trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:355-357.
- **Suggested fix:** Gate spreadPoints on reportedAt age (treat old snapshots as unknown → spreadRisk null/‘unknown’), and feed the router's live quote bid/ask spread into the engine and execution read.

### M11 [GUESSED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

The scalp read's timeframe basis is mis-stated across surfaces: the engine reads M5, but the Focus card claims 'the scalp engine reads M1', evaluates its readability contract at M1, publishes its verdict on the '1m' header bus key (so the header's Action cell shows an M5-derived verdict as the 1m verdict and shows nothing on the 5m view), and 'Ask Ruby' validates the M5-built draft against M1 candles — while the trade ticket stamps the same read as M5.

- **Evidence:** Engine timeframe: artifacts/api-server/src/lib/scalp/scalpServiceInputs.ts:24 (SCALP_TIMEFRAME='M5'); contradiction: trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:91-97 (SCALP_SIGNAL_BUS_TIMEFRAME='1m', comment 'the scalp engine reads M1'), :133-139 (sufficiency evaluated at 'M1'), :169-171 (publishes under '1m'), :456-470 (RubyChartRead timeframe='M1' on the M5 draft); header adopts by key at ScannerHeaderSummary.tsx:181-215; ticket stamps M5 at market-scanner.tsx:174-196 (the 'Theme D1' fix, acknowledging M1 was wrong).
- **Suggested fix:** Publish the lifted verdict under the 5m bus key and run the RubyChartRead at M5, matching SCALP_TICKET_TIMEFRAME; delete the stale M1 comments.

### M12 [GUESSED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

For open positions with no stop or take-profit set, the manage-side flame invents the trade geometry from constants (stop = max(100 points, 10 spreads), target = max(1.5×stop, 150 points)); the resulting chase/runway/lateFraction feed exit-urgency rungs like 'Trim your last add — the move is overstretched' and the add-on tiers, and the flame verdict is banded from a constant base score of 60 — none of which discloses that the target/stop basis was synthesized.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpManageService.ts:200-207 (fabricated stopDist/mainTpDist/invalidationPrice/quickTpPrice when geo.stopLoss/takeProfit null), :244 (qualityScore = 60 + scoreAdjust); those drive chaseRiskFor/runwayFor/timingFor in flameRead.ts:421-466 and the exit ladder scalpManage.ts:392-431; advice displayed as-is at trading-dashboard/src/components/scanner/RubyScalpBasketPanel.tsx:82-121.
- **Suggested fix:** When a basket has no SL/TP, compute chase/runway relative to ATR only and say so ('no stop/target set — judged against recent volatility'), or suppress the distance-based rungs.

### M13 [CONFIDENT_ABSENT] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

A basket that merely vanishes from the (staleness-filtered) open-positions feed is finalized as 'Closed <time>' in the journal — so a bridge outage or the relative staleness floor closes entries for positions still open at the broker, deriving a WIN/LOSS from the last floating P/L; the P/L is honestly marked 'estimated' but the Closed state itself is inferred and displayed as fact.

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpJournalService.ts:84-93 (finalize any DB-OPEN entry absent from the live snapshot) fed by the filterable feed at scalpManageService.ts:107-160; closedAt = observation time (:233-234); rendered as 'Closed {date}' at trading-dashboard/src/pages/scalp-journal.tsx:202,238.
- **Suggested fix:** Only finalize when the feed itself is fresh (bridge heartbeat current) or when a broker-closed row confirms the close; otherwise mark the entry 'lost sight of' rather than Closed.

### M14 [STALE_UNLABELED] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

The Global Market Heat card's Grid view and 'Top hot' chips render stale/delayed-source verdicts in full intensity color (including green Calm/Low) with no staleness marker, bypassing the honesty-mute the Map view applies to exactly the same data.

- **Evidence:** A verdict can carry sourceStatus 'stale'/'delayed' with a real computed intensity (lib/domain/src/market-heat/heatVerdict.ts:389-397 with intensity from magnitude at :432; price freshness STALE when age > 6× timeframe, api-server/src/lib/heat/marketHeatProviderStatus.ts:293). The map view mutes these to gray via isHonestyMuted/mapTone (trading-dashboard/src/components/scanner/GlobalMarketHeatCard.tsx:111-130, applied at :469-471), but the Grid view uses raw `tone(v.intensity)`/`tileTone(v.intensity)` (GlobalMarketHeatCard.tsx:727,734) and the topHot ranking filters only `intensity !== "unavailable"` (GlobalMarketHeatCard.tsx:590-595), then renders intensity-colored chips at :657-673 — contradicting the file's own rule at :107-110 that degraded sources must never render in intensity colors.
- **Suggested fix:** Route grid tiles and topHot chips through mapTone/isHonestyMuted (and exclude honesty-muted verdicts from the 'Top hot' ranking) the same way the map view does.

### M15 [DEAD_GAUGE] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

The timing brain's 'unavailable' data-quality label is unreachable, so three honesty mechanisms keyed on it are dead: the heat-alert skip-guard (alerts asserting 'spread, trap risk, or structural breakdown active' can be pushed from clock-only estimates), the non-admin status card (always says 'Market timing intelligence is active … Scores update automatically' even with zero market providers), and the UI's 'Unavailable' badge branch.

- **Evidence:** api-server/src/brain/timing/marketTimingBrainService.ts:307-314 (resolveDataQuality) emits only 'basic_timing_estimate' | 'real' | 'partial' — never 'unavailable'. api-server/src/lib/heat/heatAlertEmitter.ts:390 guards `if (read.dataQuality.label === "unavailable") return;` (documented at :14 and :387 as the honest-absence guard) — dead code; the heat_danger_window alert at heatAlertEmitter.ts:99-110 fires on dangerScore >= 70, reachable with no candles/quotes (base 20 + fakeoutRisk*0.4 + AT_EVENT 40, tradeabilityEngine.ts:102-115), with text claiming measured 'spread, trap risk, or structural breakdown'. trading-dashboard/src/pages/market-heat-map.tsx:1235 computes `isActive = !!data && data.dataQuality.label !== "unavailable"` — always true when a read exists — so :1244-1248 claims active intelligence during a session-clock-only estimate; the badge's 'unavailable' branch at market-heat-map.tsx:186-198 can never render.
- **Suggested fix:** Either emit label 'unavailable' when hasCandleData && hasQuoteData are both false, or re-key the emitter guard and status card on 'basic_timing_estimate', and soften the danger-alert copy to name what was actually measured.

### M16 [FABRICATED] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

Session Heatmap (and the surrounding Analytics stats) present paper P/L as bare dollars ('$-12', 'Net P&L $340') when the number is a synthetic `(exit-entry)*lots*100` unit — not account currency — which collapses typical FX results to '$0' and systematically misranks sessions/symbols against each other (XAUUSD/index moves dwarf FX pip moves).

- **Evidence:** api-server/src/routes/analytics.ts:50-54 `pnlOf` = `(o.exitPrice - o.entryPrice) * dir * o.lotSize * 100; // synthetic $/lot`, feeding SESSION_PNL buckets at analytics.ts:204-213; SessionHeatmap.tsx:21 renders `$${d.pnl.toFixed(0)}` and analytics-command.tsx:72,75,77 render `$` Net P&L/expectancy/Max DD with no synthetic-units label. The unit matches the paper engine's own convention (api-server/src/lib/paperExecution/paperExecutionMonitor.ts:26-29) so values are internally consistent, but a 10-pip EURUSD win at 0.01 lots computes to 0.001 → displayed '$0', and cross-symbol comparisons (which the heatmap tint at SessionHeatmap.tsx:12-15 normalizes on) are distorted by instrument scale, not real money.
- **Suggested fix:** Label the unit ('synthetic units — not account currency') or compute real per-instrument pip/contract value; at minimum stop rendering the '$' sign on synthetic units.

### M17 [STALE_UNLABELED] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

The stat tiles surrounding the Session Heatmap (Trades, Net P&L, Win rate, Max DD) come from the latest STORED analytics snapshot — refreshed only on first visit or a manual 'Recompute snapshot' press — with no generated-at timestamp shown, so days-old P/L sits unlabeled next to the live-computed heatmap.

- **Evidence:** trading-dashboard/src/pages/analytics-command.tsx:16-19 fetches GET /api/analytics/snapshot; api-server/src/routes/analytics.ts:167-172 returns the newest stored analyticsSnapshotsTable row (written only by POST at analytics.ts:152-164); analytics-command.tsx:43-45 auto-generates only when none exists; the tiles at analytics-command.tsx:70-78 render snapshot values with no createdAt/freshness label anywhere on the page. Also unlabeled window: the heatmap itself aggregates the most recent 1000 paper orders (analytics.ts:194-196) with no time-window caption on SessionHeatmap.tsx:8.
- **Suggested fix:** Render the snapshot's createdAt next to the tiles ('as of …') and caption the heatmap's data window (e.g. 'last N closed paper trades').

### M18 [DEAD_GAUGE] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

Active-session 'Win rate' has no writer anywhere — the column is created at 0 and no code path ever updates it, so the cockpit shows 'Win rate 0%' as a live stat forever.

- **Evidence:** trading-cockpit.tsx:198 renders a.winRate → manager.ts:291 reads row.winRate → lib/db/src/schema/paperSessions.ts:33 winRate integer default(0). Every db.update(paperSessionsTable) in the codebase is at manager.ts:397, 420, 431, 519, 524 — none sets winRate (even the link-trade CLOSE branch at 524-531 omits it). The correct win rate is computed only inside generateSessionReport (manager.ts:597) and written to the reports table, never back to the session row the cockpit reads.
- **Suggested fix:** Either compute winRate at read time in getActiveSession from the linked closes, or drop the tile until a real producer exists (honest limited surface).

### M19 [CONFIDENT_ABSENT] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

A failed paper_orders read renders as the confident empty states 'No open paper trades' and 'No closed paper trades yet today' (with dayRating NO_TRADES) — a DB outage is indistinguishable from being flat.

- **Evidence:** tradingCockpit.ts:89 openTrades fallback [] and :104 todayPerformance fallback {totalTrades:0,...,netPnl:0,dayRating:'NO_TRADES'} on any thrown read → UI trading-cockpit.tsx:217-218 ('No open paper trades. They will appear here...') and :253-254 ('No closed paper trades yet today...') render the same copy for failure as for genuinely-zero.
- **Suggested fix:** Return {status:'READ_FAILED', reason} from safeCall for these two blocks and render an explicit 'couldn't read paper trades' state distinct from the true empty state.

### M20 [FABRICATED] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

'Open paper trades' and 'Today's performance' on the per-user cockpit are computed over EVERY user's and every account's paper orders — another user's trades and P/L are presented as this trader's, unlabeled (unlike the alerts panel, which does label its system-wide scope).

- **Evidence:** paper_orders has user_id (lib/db/src/schema/paperTrading.ts:27), and the route claims per-user isolation (tradingCockpit.ts:60-63), yet the open-trades query filters only status='OPEN' with no user scope (tradingCockpit.ts:88) and todayPerformance selects ALL rows (tradingCockpit.ts:94: db.select().from(paperOrdersTable)). Contrast: notifications carry notificationScope:'SYSTEM_WIDE_UNSCOPED' (tradingCockpit.ts:220) and the UI says so (trading-cockpit.tsx:317-319); the trades/performance panels carry no scope label at all.
- **Suggested fix:** Add eq(paperOrdersTable.userId, userId) to both reads (rows with NULL user_id need a backfill decision), or until then label both panels instance-wide the way the alerts panel is labeled.

### M21 [GUESSED] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

All paper P/L is derived from a fixed ×100 multiplier — dir*(exit−entry)*lotSize*100 — which corresponds to no real currency amount for any symbol (no contract size, pip value, or per-symbol scaling), yet it is displayed as money: bare 'P&L'/'Net P&L' numbers on two cockpit panels, '(cents)' on a third, and '$' in event text.

- **Evidence:** Formula: paperExecutionMonitor.ts:26-29 and paperTrading.ts:65-68 (identical, symbol-blind ×100). Display: trading-cockpit.tsx:233 ('P&L', unitless) and :259 ('Net P&L', unitless) vs :197 ('Net P&L (cents)') — while the session 'cents' store is documented at manager.ts:86-92 and only ever fed by caller-supplied pnl with no unit validation (manager.ts:503,515,523). Event text implies dollars: paperTrading.ts:114 'P&L ${pnl.toFixed(2)}'.
- **Suggested fix:** Pick one honest unit (e.g. per-symbol USD using contract specs) or, until then, label the figure as 'sim points' everywhere it renders, and validate/convert the unit at the link-trade boundary.

### M22 [MOCK_LEAK] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

Non-EE paper orders are marked, SL/TP-settled, and their realized P/L written from a seeded deterministic candle generator — not market data — and those mock-settled P/L rows flow unlabeled into the cockpit's 'Today's performance' totals; a trader believes the market hit their stop when a PRNG did.

- **Evidence:** paperTrading.ts:52-59 getMockPrice → generateDeterministicCandles(seed 'paper-mtm|<bucket>') → :87 px = getMockPrice(...) → :97-113 SL/TP hit detection and db.update setting profitLoss from that mock price (entry can also be mock: :214; manual close uses mock px: :288). The cockpit's todayPerformance then sums those closed rows' profitLoss (tradingCockpit.ts:94-99) with no source/synthetic label on the panel (trading-cockpit.tsx:256-262). Note the code is honest at one edge — px==null skips rather than settling (paperTrading.ts:88-91) — but a returned mock price settles trades as if real.
- **Suggested fix:** Stamp orders settled via the mock model (e.g. priceSource:'SYNTHETIC') and either exclude them from performance totals or badge the total 'includes synthetic-priced closes'; long-term route Build-Q orders through the same honest DD path Build EE uses.

### M23 [CONFIDENT_ABSENT] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

On failed reads, the dashboard cockpit's Critical Market Events card says 'No high-impact events in the next 3 days.' and the Alerts Summary card says 'No major alerts right now.' — asserting all-clear from no signal.

- **Evidence:** CockpitCards.tsx:582-583 (eventsQ.data ?? []) with the empty branch at :603-604 rendering the confident copy regardless of eventsQ.isError; :836-840 (alertsQ.data ?? []) with :865-866 'No major alerts right now.' regardless of alertsQ.isError.
- **Suggested fix:** Branch on isError to render 'Couldn't load events/alerts' before the empty-state copy.

### M24 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

The slot-summary API collapses a null floatingPl to 0 into the wire's non-nullable grossProfit/netProfit, so a live position whose P/L has never synced renders as 'Current P/L 0.00' / '$0.00' instead of '—' — the frontend's fmtMoney handles null honestly but never receives one.

- **Evidence:** artifacts/api-server/src/routes/meLiveAccount.ts:200 (const floating = p.floatingPl != null ? Number(p.floatingPl) : 0), :225-230 (grossProfit/netProfit: floating); display: artifacts/trading-dashboard/src/components/live/LiveOpenTradesPanel.tsx:34-40 (fmtMoney null→'—' path unreachable), :104-106, :154.
- **Suggested fix:** Type grossProfit/netProfit as number|null and pass the null through; exclude null rows from openPnL or flag the total incomplete.

### M25 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

Live Trades page: a failed useGetOpenTrades read renders the confident empty state 'No open trades.' (no isError branch anywhere on the page), and a failed mt5-state read silently removes the 'MT5 Positions' block entirely, so both failure modes are indistinguishable from genuinely-flat.

- **Evidence:** artifacts/trading-dashboard/src/pages/live-trades.tsx:17 (only isLoading destructured), :55-57 ('No open trades.' when !trades), :67 (mt5 && mt5.positions guard hides the block on undefined data with no error surface).
- **Suggested fix:** Handle isError for both queries with an explicit could-not-read state.

### M26 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

In the same page's raw MT5 positions block, a broker position whose profit the EA omitted (schema allows profit nullish) renders as a green '0.00' P/L — a confident flat claim for an unreported number.

- **Evidence:** artifacts/trading-dashboard/src/pages/live-trades.tsx:75 ((p.profit ?? 0) >= 0 tone + (p.profit ?? 0).toFixed(2)); profit is optional on the ingest schema: lib/api-zod/src/generated/api.ts:4835 (profit: zod.number().nullish()); passed through raw by artifacts/api-server/src/routes/mt5.ts:1769.
- **Suggested fix:** Render '—' with muted tone when p.profit is nullish.

### M27 [STALE_UNLABELED] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

The legacy /positions/sync reconciler marks any mirrored row that is missing from a single mt5_state.positions read as MANUALLY_CLOSED with closedAt — no snapshot-reliability guard — so one partial/incomplete EA push closes rows still open at the broker, and they thereafter display 'Closed' with confidence; this contradicts the canonical positionFreshness rule the newer surfaces enforce ('a stale/missing row is broker-confirmed-absent ONLY with a reliable recent snapshot').

- **Evidence:** artifacts/api-server/src/routes/livePositions.ts:298-313 (vanished-from-feed → MANUALLY_CLOSED + closedAt, event text admits 'treated as manual close'); canonical rule it violates: artifacts/api-server/src/routes/meLive.ts:340-348 and artifacts/api-server/src/lib/live/positionFreshness.ts:16.
- **Suggested fix:** Gate the vanish→closed transition on the same isSnapshotReliable marker (last_positions_snapshot_at) used by meLive/meLiveAccount, or mark rows SYNC_PENDING instead of MANUALLY_CLOSED.

### M28 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

The unified positions feed (/api/me/positions/all, chart-side 'My open positions' picker) silently drops live rows whose lastSyncedAt lags the newest row by >90s — a relative stale-floor with no snapshot-reliability check and no warning — so the same open broker position can be listed by OpenLivePositions (with a 'confirming…' pill) yet be absent from the picker, reading as closed/gone.

- **Evidence:** artifacts/api-server/src/routes/mePositionsUnified.ts:86-96 (liveFloor filter hides on staleness alone); the rule it diverges from, on the surface it claims to share truth with: artifacts/api-server/src/routes/meLive.ts:340-373 (stale rows kept visible + flagged, hidden only when a reliable snapshot excludes them); consumer: artifacts/trading-dashboard/src/components/positions/PositionPickerPanel.tsx:49.
- **Suggested fix:** Reuse classifyRow/isSnapshotReliable from positionFreshness.ts here and emit the shared snapshotWarning instead of the ad-hoc floor.

### M29 [FABRICATED] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

POST /execute-trade, when the operator has set LIVE_TRADING mode and a confirmationId is supplied via API, inserts a mode='LIVE' OPEN trades row and replies 'LIVE trade executed.' without any broker placement existing anywhere in the route — a phantom LIVE position that then renders on the Live Trades surface; unreachable from the shipped UI only because the pre-trade-checklist layer is documented NOT MOUNTED (default-deny).

- **Evidence:** artifacts/api-server/src/routes/trades.ts:246-260 (insert mode LIVE) and :281-283 ('${gate.decisionMode} trade executed.' message — no broker call in the route); decisionMode LIVE is reachable: artifacts/api-server/src/lib/safetyCore.ts:545-548; UI-unreachability is deliberate and documented: artifacts/trading-dashboard/src/components/execution/index.ts:3-19.
- **Suggested fix:** Make the LIVE branch refuse outright ('broker placement not implemented') instead of inserting a LIVE-marked row and claiming execution.

### M30 [DEAD_GAUGE] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

Every production call into the mission risk/emergency read hardcodes killSwitchActive:false, brokerConnected:true, feedStatus:'live', quoteFresh:true, so the Risk Control card's emergency alert and mode escalations can structurally never fire on a real kill switch, broker disconnect, stale feed, or stale quote — the persisted riskJson permanently asserts a live feed it never observed (real safety is only re-derived in the separate live 23-gate path).

- **Evidence:** artifacts/api-server/src/routes/profitMissions.ts:105-110 (MISSION_RISK_SIGNALS constants) used at :300-305 (pulse), :868 (emergency-stop), :973 (execute); artifacts/api-server/src/lib/missionExecution.ts:~246-252 (DEFAULT_SIGNALS, same constants); the never-matchable conditions: lib/domain/src/profit-mission/blowupRisk.ts:279-283; consumed by missionRiskService.ts:204-222; displayed at artifacts/trading-dashboard/src/pages/profit-missions.tsx:2105-2110 (alert-emergency) and :2048-2055 (mode badge); live path independently checks the real switch (artifacts/api-server/src/lib/liveTrading/guard.ts:49)
- **Suggested fix:** Resolve the real kill-switch/broker/feed state at the pulse and execute call sites (the seams exist: liveTrading/state, getBrokerHealthVerdict, evaluateEntryDataSufficiency) instead of constants

### M31 [STALE_UNLABELED] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

The page's prominent 'mode truth' copy asserts paper/demo missions produce no fill, no P/L, cannot progress or complete ('a record of intent, not a simulated account') and that 'paper and demo produce no realised trades' for promotion — all contradicted by the wired fill simulator: paper/demo dispatch produces simulated fills from real quotes, sim P/L moves the displayed currentValue, missions complete on SIMULATED basis, and the demo_performance promotion gate reads simulated closes.

- **Evidence:** Stale copy: artifacts/trading-dashboard/src/pages/profit-missions.tsx:2823-2840 (text-mission-mode-truth + ladder-road), :1756-1771 ('Execute (live only)' + 'produces no fill, no profit or loss'), :1821-1828, :2169-2177 ('Demo auto (records intent only)'), :2503-2505; contradicted by artifacts/api-server/src/lib/missionExecution.ts:446-448,557-564,647 (default simulated executor = simulateMissionFill), missionSimulatedFills.ts:179-307,447-526 (sim fills + sim P/L), missionExitManager.ts:584-599,687-699 (currentValue update + SIMULATED-basis completion), missionPromotionService.ts:149-157 (simulated demo evidence); the stale claims are even test-locked (profit-missions.copy-truth.test.ts:441,484,502)
- **Suggested fix:** Rewrite the mode-truth copy (and its copy-truth test anchors) to describe the simulator: real-quote-priced simulated fills, SIMULATED accounting, simulated completion, and simulated demo evidence feeding demo_performance

### M32 [CONFIDENT_ABSENT] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

Daily briefing / EOD review / mission report aggregate only broker-family closes (pnl+closedAt), so on a paper/demo mission whose simulated trades closed today the EOD headline confidently reads '+0 across 0 trade(s)' and the report 'Net realised: 0' while the same page's currentValue/progress moved on those very closes — a confident zero over the wrong book, with no basis label.

- **Evidence:** artifacts/api-server/src/lib/missionBriefingService.ts:71-98 (readClosedDrafts filters pnl/closedAt, which stay NULL forever for simulated rows per missionSimulatedFills.ts:254,489-511), :144-159 (EOD), :161-175 (report); headline copy lib/domain/src/profit-mission/missionBriefing.ts:114,145; rendered at artifacts/trading-dashboard/src/pages/profit-missions.tsx:2742-2757
- **Suggested fix:** Aggregate the mission's accounting-basis family (sim for paper/demo, broker for live) and state the basis in the headline, mirroring resolveMissionRealisedStats' basis switch in missionExitManager.ts:584-599

### M33 [STALE_UNLABELED] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

The surface tells the user a mission 'cannot start until broker feed is confirmed' (canStart is always false in Phase 1), but POST /profit-missions/:id/start enforces no feed or canStart check at all — it walks draft→pending_approval→running unconditionally, making the mission eligible for the driver's automated ticks despite the displayed hard block.

- **Evidence:** Displayed claim: artifacts/trading-dashboard/src/pages/profit-missions.tsx:226-235 (FeedNotConfirmedBanner 'cannot start until broker feed is confirmed'), :645-649 ('starting held'), artifacts/trading-dashboard/src/pages/profitMissionPlanner.ts:28-35; code: artifacts/api-server/src/routes/profitMissions.ts:1245-1297 (start route — state-machine legality only, never consults feasibility.canStart or resolveFeedReadiness, whose only value is ready:false per profitMissionSerialize.ts:41-43)
- **Suggested fix:** Either enforce canStart in the /start route (409 with startBlockReason) or change the copy to 'starting is not offered here' rather than claiming it is blocked

### M34 [GUESSED] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

With the candle/quote feed fully down, the Timing Brain still emits a timing grade, heat/tradeability/danger gauges, and can emit a green 'GO' entry permission derived from the wall clock alone — the service can never produce the 'unavailable' label the frontend's honest collapse branch requires, and the shared read gate fails open when the truth snapshot is null.

- **Evidence:** artifacts/api-server/src/brain/timing/marketTimingBrainService.ts:301-314 — resolveDataQuality returns only basic_timing_estimate|real|partial, never 'unavailable'; heatEngine.ts:51-68 builds heatScore from sessionHeatBonus+killZone with zero candles; tradeabilityEngine.ts:65,89,133-139 — edge base 50 with all-null inputs, trade = 30 + sessionTradeabilityBonus can cross the ≥50 'GO' threshold clock-only; TimingIntelligenceCard.tsx:211 collapses only when label==='unavailable' (unreachable for feed-down), and useScannerReadGate.ts:37 sets downgraded=false whenever truth is null (fetch failure), letting the GO pill/grade/score gauges render with only a small amber 'Timing estimate' chip (TimingIntelligenceCard.tsx:74-79). ScannerTimingBadges.tsx:157-159 shrinks the disclosure to a 9px 'timing est.'.
- **Suggested fix:** Return label 'unavailable' from resolveDataQuality when !hasCandleData && !hasQuoteData; make useScannerReadGate treat truth==null as downgraded (fail-closed).

### M35 [GUESSED] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

trapProbability — an additive rule-points score (35+25+20+20+15…, clamped 0-100, or the raw session fakeoutRisk when <15 candles) — is displayed to users with a percent sign as 'Trap probability N%', presenting a hand-weighted heuristic as a measured probability.

- **Evidence:** artifacts/api-server/src/brain/timing/trapRoomEngine.ts:38-49 (fallback = fakeoutRisk) and 52-113 (additive points) → rendered as `Trap probability ${r.trapProbability}%` at artifacts/trading-dashboard/src/components/dashboard/trade/PreTradeHeatChecklist.tsx:160 and in the timing actionReason string `High trap probability (${trapProbability}%)` at artifacts/api-server/src/brain/timing/marketTimingBrainService.ts:270. The codebase's own rule (liveScanner.ts:66-70) says uncalibrated scores must never be rendered with '%'.
- **Suggested fix:** Render as 'Trap score N/100' (matching the other pills) or bucket to low/elevated/high; drop the % from the actionReason copy.

### M36 [DEAD_GAUGE] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

The Manual Trade Ticket's 'Risk governor' badge is a hard-coded green 'active' — it renders 'active' regardless of the governor's real state, on the page that submits live intents.

- **Evidence:** artifacts/trading-dashboard/src/pages/live-manual.tsx:160 — `<span>Risk governor</span><Badge className="bg-success/20 text-success…">active</Badge>` with no data binding (contrast the adjacent kill-switch badge at line 161, which binds perm/defStatus, and the master-bridge badge which has a 'checking…' state).
- **Suggested fix:** Bind the badge to a real governor/permission status endpoint or remove the row.

### M37 [FABRICATED] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

The Manual Trade Ticket's 'Estimated max loss $X / Estimated gain $Y / Risk/Reward' are computed from the simulator's random-walk mid price with a blanket ×10000 pip multiplier — off by ~100x for JPY and gold pairs — and shown as dollar facts beside a 'Submit Live Intent' button (the SIMULATOR environment badge sits lower in the panel, not on the numbers).

- **Evidence:** artifacts/trading-dashboard/src/pages/live-manual.tsx:50-61 polls /api/market/quote (simulator, routes/market.ts:26-30) every 2s; lines 72-74 `Math.abs(quote.mid - stopLoss) * lotSize * 10000` for every symbol; displayed at lines 154-156. The backend explicitly banned this blanket ×10000 guess as 'wrong for JPY, gold and synthetics' (artifacts/api-server/src/lib/assistant/liveScanner.ts:55-58).
- **Suggested fix:** Use per-symbol pip math (instrumentSpec) and the routed real quote, or degrade to '—' as the page already does for Risk % when the input is not real.

### M38 [CONFIDENT_ABSENT] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

In the health check, a failed safety_core read is swallowed and reported as zero hard blocks — the System Health page then shows 'Hard blocks: none' while the kill switch may be engaged and unreadable.

- **Evidence:** artifacts/api-server/src/lib/systemHealth/health.ts:299 (`db.select().from(safetyCoreTable).limit(1).catch(() => [])`) → hardBlocks stays [] → rendered as 'none' at artifacts/trading-dashboard/src/pages/system-health.tsx:115 and drives overallStatus at health.ts:398-399. Contrast the live pipeline which treats a failed read as engaged: lib/live/liveCommandPipeline.ts:546 (`engaged = args.safetyCoreKillSwitchEngaged !== false`).
- **Suggested fix:** On read failure push a SAFETY_STATE_UNKNOWN hard block (fail-closed reporting, matching the pipeline's semantics).

### M39 [CONFIDENT_ABSENT] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

Secret-redaction status reports 'Working: true' with 0 leaks when the probe queries themselves fail — the catch handlers default both counts to 0 and redactionWorking is computed from those zeros.

- **Evidence:** artifacts/api-server/src/lib/systemHealth/health.ts:320-324 (`.catch(() => ({ rows: [{ c: 0 }] ... }))` twice) → :331 (`redactionWorking: ff === 0 && lf === 0`) → rendered at artifacts/trading-dashboard/src/pages/system-health.tsx:123-125.
- **Suggested fix:** Return redactionWorking: null / 'unknown' with a warning when either query throws.

### M40 [CONFIDENT_ABSENT] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The System Health 'Critical unread' card shows 0 when the notifications count query fails, and the admin runtime-health bridge section reports total:0/healthy:0 (indistinguishable from 'no bridges') when the MT5 aggregate query throws.

- **Evidence:** artifacts/api-server/src/lib/systemHealth/health.ts:343-344 (crit unread `.catch(() => rows c:0)`) → system-health.tsx:70; artifacts/api-server/src/routes/adminRuntimeHealth.ts:73-96 (bridge zeros initialized, catch only logs) → rendered by artifacts/trading-dashboard/src/components/admin/WorkflowHealthCard.tsx (bridge counts). Same pattern: health.ts:197 paperOrders count, :251 criticalTotal.
- **Suggested fix:** Carry an explicit ok/error flag per sub-probe and render 'unavailable' instead of 0.

### M41 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

Six of the fourteen Release Status readiness gates are hard-coded pass:true (API health, Route health 'All routes registered', Simulator trade, AI simulator, Live intent 'Queue endpoints reachable', MT5 deferred honesty) and warnings is a literal [] — the page renders them as green PASS pills and an inflated Readiness Score that can never drop below ~43 even with the server half-broken.

- **Evidence:** artifacts/api-server/src/lib/release.ts:36-37,42-43,46,63 (literal true, untested detail strings), :94 (`warnings: []`), plus truthiness-only gates :41,51,54-56; rendered as PASS/FAIL pills and score badge at artifacts/trading-dashboard/src/pages/release-status.tsx:48,54-58.
- **Suggested fix:** Probe what each gate names (as lib/systemHealth/health.ts does) or delete the un-probeable gates from the list.

### M42 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The SafetyHeader's green 'Live Chart' pill lights whenever the TradingView library object exists on window — it indicates script presence, not a live feed, so it stays green with a dead or stale feed.

- **Evidence:** artifacts/trading-dashboard/src/components/ss/SafetyHeader.tsx:34 (`setChartLive(... !!(window as any).TradingView)`) polled every 4s at :36, rendered as a success pill at :87-91. Contrast the honest per-symbol verdict in useScannerTruth consumed by market-health.tsx:126.
- **Suggested fix:** Drive the pill from feedStatus/useScannerTruth or rename it 'Chart library loaded'.

### M43 [CONFIDENT_ABSENT] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The Topbar renders 'Mode: Mock' and 'MT5: OFF' when the MT5 status query has failed or never loaded — an unknown state shown as a confident mock/disconnected claim, on the same header used in live-shared sessions.

- **Evidence:** artifacts/trading-dashboard/src/components/layout/Topbar.tsx:56 (`modeMeta = (mt5 && MODE_META[mt5.mode]) || MODE_META.MOCK`), :176-178 (`value: p.mt5?.connected ? "ON" : "OFF"`), :193; no isError/isLoading branch distinguishes 'unknown' from 'Mock/OFF'.
- **Suggested fix:** Add an 'unknown' pill state for query error/undefined, as NotificationBell does.

### M44 [STALE_UNLABELED] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

useRuntimeContext keeps the last successful health/bridge snapshot forever when refreshes start failing (`if (h) setHealth(h)` drops failures silently), so the Status Command Center's 'Real app state' — including 'Heartbeat present (Ns)' — freezes at stale values with no staleness signal.

- **Evidence:** artifacts/trading-dashboard/src/assistant/useRuntimeContext.ts:9-15 (safeJson swallows errors to null), :35-36 (only truthy responses update state); frozen values rendered at artifacts/trading-dashboard/src/pages/status-command-center.tsx:378-385.
- **Suggested fix:** Track lastSuccessfulRefreshAt; when it exceeds ~2 intervals, flag the Runtime Status card as stale/unreachable.

### M45 [DEAD_GAUGE] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The Release Notes page's 'Known issues' section is fed by a hard-coded empty array while the same file's readiness code queries a real feedback table with open P0 bugs — known issues can exist in the DB and this surface will always say there are none.

- **Evidence:** artifacts/api-server/src/routes/release.ts:82 (`knownIssues: []` literal) rendered at artifacts/trading-dashboard/src/pages/release-notes.tsx:62; the real open-P0 query exists at artifacts/api-server/src/lib/release.ts:66-75.
- **Suggested fix:** Populate knownIssues from the same feedbackTable query (open P0/P1), or drop the section.

### M46 [STALE_UNLABELED] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The Admin Security Status page's 'Protected mutations' card is static prose claiming mutation endpoints require the x-security-role: ADMIN header — a description of a superseded auth model (current code treats that header as forbidden outside the auditable resolver), and its 'Session: OK' pill goes green on any truthy JSON, including an error body.

- **Evidence:** artifacts/trading-dashboard/src/pages/admin-security-status.tsx:42 (static x-security-role claim), :31 (`<Pill ok={!!s} label="Session" />` — truthy error envelope passes); the current model per artifacts/api-server/src/routes/adminRuntimeHealth.ts:36-39 ("We do NOT read the x-security-role header... CI guard forbids it").
- **Suggested fix:** Replace the prose with a probed fact (e.g. call one gated endpoint unauthenticated and show the 401/403) and gate the pills on response shape, not truthiness.

### M47 [FABRICATED] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

The trading-cockpit 'Today's performance' win-rate/net-P/L and 'open paper trades' list are computed over EVERY user's paper orders (queries have no userId scope), so the panel presents other accounts' trades as the caller's own performance — directly under a comment claiming per-user isolation.

- **Evidence:** artifacts/api-server/src/routes/tradingCockpit.ts:76-89 (open trades: `.where(eq(paperOrdersTable.status, "OPEN"))` only) and :92-104 (`db.select().from(paperOrdersTable)` with no user filter, then wins/losses/netPnl/winRate from it); the isolation claim is at :60-63; paper_orders has a user_id column (lib/db/src/schema/paperTrading.ts:27). Bonus in the same route: safeCall fallbacks render failed reads as confident absence — todayPerformance error → {totalTrades:0, dayRating:"NO_TRADES"} (:104) and criticalUnread error → [] (:71), which suppresses the ACK_CRITICAL next-best-action (:51).
- **Suggested fix:** Add eq(paperOrdersTable.userId, userId) to both queries; make safeCall fallbacks carry an explicit unavailable marker the client must render, not a zero-shaped default.

### M48 [CONFIDENT_ABSENT] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

The performance calendar counts a closed trade with NULL P/L as a $0 'breakeven' trade — folded into totalPnl, winRate, and even bestTrade/worstTrade as 0 — with no exclusion marker, on both the paper branch and the live shared_trade_attribution branch (unlike /performance/summary, which excludes UNKNOWN rows and reports the count).

- **Evidence:** artifacts/api-server/src/routes/mePerformanceCalendar.ts:115-121 (`const pnl = r.pnl ?? 0; ... else day.breakeven += 1; day.bestTrade = ... pnl`), :203-207 (/me/performance-summary repeats `(r.pnl ?? 0)`); the live branch maps `pnl: r.pnl ?? null` at :77, admitting nulls. Contrast artifacts/api-server/src/routes/performance.ts:77-78,149 which excludes and discloses.
- **Suggested fix:** Skip null-P/L rows from money aggregates and return an excludedUnknown count per day/summary, mirroring the performance.ts contract.

### M49 [CONFIDENT_ABSENT] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

The shared frontend formatters convert honest nulls into confident values at every call site: formatPnl(null)→'$0.00', formatPercent(null)→'0.00%', formatPrice(null)→'0.00', formatConfidence(null)→'0%' — a systemic engine for rendering failed reads as zeros (13 formatPnl call sites), inconsistent even with its sibling pnlClass(null) which treats null as unknown.

- **Evidence:** artifacts/trading-dashboard/src/lib/format.ts:3-31 (all four null→zero branches); example consumer artifacts/trading-dashboard/src/components/dashboard/cockpit/CockpitCards.tsx:253 formatPnl(openPnl) vs :65-66 pnlClass treating null as neutral.
- **Suggested fix:** Make the null branch return '—' (or force callers to pass a number) so 'no data' can never print as '$0.00'.

### M50 [DEAD_GAUGE] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

The Analytics page reads top-level balance/equity/pnl fields that do not exist on MeSharedAccountSummaryResp, so its Equity / Balance / Open P/L readouts and 'Account Health' card can never populate for any user — and the always-null openPnl makes Ruby's Account Read print the fabricated sentence 'Open positions are active — keep an eye on exposure.' for every user with ≥1 all-time trade, including users with zero open positions.

- **Evidence:** artifacts/trading-dashboard/src/pages/analytics.tsx:71 casts acctQ.data to {balance?,equity?,pnl?}; the actual response shape has no such fields (lib/api-client-react/src/generated/api.schemas.ts:12801-12814 — ok/userId/accounts/allocationView/masterMt5/masterAccess only), so :75-77 are permanently null and hasAcct (:219) permanently false; :291 then always takes the 'Open positions are active' branch whenever the card renders (totalTrades>0), contradicting the 'Exposure: Clear' chip at :306 built from a different source.
- **Suggested fix:** Read the real fields (accounts[].virtualBalance/virtualEquity/virtualPnl or allocationView), and make the Ruby sentence condition on the actual positions read instead of a field that cannot exist.

### M51 [GUESSED] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

'My Equity' on My Account is not an equity read: for every allocated user it is defined as exactly equal to My Balance, so floating losses on open positions can never appear in the equity tile a user would check before adding risk.

- **Evidence:** artifacts/api-server/src/routes/meAccountShell.ts:261-263 (`allocatedEquity = hasAlloc ? allocatedBalance : ...`) and :456-457 (`equity: allocatedEquity || allocatedBalance`); rendered as an independent 'My Equity' stat at artifacts/trading-dashboard/src/pages/my-account.tsx:206.
- **Suggested fix:** Either source equity from a real marked-to-market read (virtualEquity / live snapshot) or drop the tile — a duplicate of balance wearing an equity label claims information that was never read.

### M52 [STALE_UNLABELED] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

The onboarding step catalogue routes users to pages that were removed or that their tier can never open: two REQUIRED steps link 'Open /trading-cockpit →' and 'Open /paper-testing-launch →' (routes deleted in Phase 3), REQUIRED steps point at /readiness-checklist and /risk-settings (on no trader allowlist, so RouteAccessGuard silently bounces the click home), and the completion alert says 'Use the Trading Cockpit as your home base' about the removed page.

- **Evidence:** artifacts/api-server/src/lib/onboarding/steps.ts page_route values (safety-header + monitor-trades → /trading-cockpit; preflight + start-session → /paper-testing-launch; readiness-check → /readiness-checklist; risk-governor → /risk-settings; end-session → /session-report; replay → /replay-simulator; data-import; system-health; coach → /trader-coach); artifacts/trading-dashboard/src/App.tsx:194-195,349,353 (cockpit + paper-testing-launch routes removed); artifacts/trading-dashboard/src/lib/routeAccess.ts:40-166 (none of /readiness-checklist, /risk-settings, /session-report, /replay-simulator, /data-import, /system-health, /trader-coach on either trader allowlist) with RouteAccessGuard.tsx:93-107 silently redirecting to '/'; links rendered at pages/onboarding.tsx:131 and cockpit-home copy at :86. The identical defect was documented and fixed for the Help Center (lib/onboarding/help.ts:32-42, 'RANK 51: 100% of the links were un-followable') but the step catalogue was left stale.
- **Suggested fix:** Re-point every step's page_route at a real, tier-reachable route (as help.ts now does), render links via the tier-filtered pattern WhyBlockedDrawer uses (isHumanTraderAllowedPath), and replace 'Trading Cockpit' copy with the real home '/'.

### M53 [DEAD_GAUGE] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

Every 'Ask Ruby to Explain' / 'Ask {name} to explain more' button (Trading School home, RubyTeacherCard, and six other pages) silently no-ops: askRuby() writes a sessionStorage key and dispatches a synthetic StorageEvent, but the mounted assistant panel reads that key only in its useState initializer and registers no 'storage' listener, so nothing opens — the panel instead pops open unexpectedly on the user's next full page reload.

- **Evidence:** artifacts/trading-dashboard/src/features/trading-school/components/SchoolUI.tsx:13-19 (askRuby) and :50-53; TradingSchoolHome.tsx:42-44; same pattern in pages/performance-scorecard.tsx:54-58, economic-calendar.tsx:102-106, alerts.tsx:46-50, my-trades.tsx:39-43, analytics.tsx:38-42, mt5-bridge.tsx:20-24. Receiver: components/help/ArxAssistantLivePanel.tsx:161-164 (only read of STORAGE_OPEN_KEY, in the initial-state closure) with no addEventListener('storage') anywhere in the file (grep confirms; contrast working listeners in hooks/useFeatureUnlock.ts:63, hooks/useScannerTimeframe.ts:25); the panel mounts once, persistently, in AppLayout.tsx:664 wrapping the whole route Switch (App.tsx:262-485), so it never re-runs the initializer on navigation.
- **Suggested fix:** Expose an open() function from the panel (context or a custom window event it subscribes to) and have askRuby call that; keep the sessionStorage key only as reload persistence.

### M54 [CONFIDENT_ABSENT] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

The first-run welcome modal's 'Risk Controls' chip shows a green 'Active' whenever the account-mode envelope is unloaded or failed to load — the other three chips honestly degrade to 'Checking', but the risk chip lacks that guard, asserting a safety control is running when its state is unknown.

- **Evidence:** artifacts/trading-dashboard/src/components/onboarding/OnboardingProvider.tsx:220-222 (riskChip = isFrozen ? 'Lock active' : 'Active' with no !env branch, violating its own comment at :204 'only truthful state'), versus :206-219 where approvalChip and bridgeChip both start with !env → {label:'Checking', tone:'muted'}; hooks/useTradingMode.ts:143 (isFrozen = env?.userFrozenStatus.isFrozen === true, i.e. false when env is null/errored), so a failed /api/me/account-mode read renders 'Risk Controls: Active'.
- **Suggested fix:** Give riskChip the same !env guard: { label: 'Checking', tone: 'muted' } until the envelope has actually loaded.

### M55 [CONFIDENT_ABSENT] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

Trading School progress renders local-cache numbers as the user's progress with no failed-sync signal: on a new device (or any 401/offline read) the server fetch fails silently and the UI confidently shows '0% of 10 steps', 'Not attempted' per lesson, and re-locks steps the user already passed elsewhere.

- **Evidence:** artifacts/trading-dashboard/src/features/trading-school/lib/progress.ts:186-206 (syncFromServer catch returns the local cache with no error surfaced) and :305-319 (useSchoolProgress exposes no sync status); rendered as bare fact at features/trading-school/pages/TradingSchoolHome.tsx:65-70 ('{pct}% of 10 steps'), TradingSchoolProgress.tsx:33-38,61 ('Not attempted'), and TradingSchoolProgram.tsx:22-26,44 (isUnlocked derived from the unsynced passedLessonIds re-locks passed steps).
- **Suggested fix:** Have syncFromServer report success/failure to the hook and render a small 'progress may be out of date — couldn't reach the server' notice (and avoid re-locking steps) until a sync has succeeded at least once this session.


## LOW

### L1 [DEAD_GAUGE] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

The ARX native chart's Mirror Layer maps quality "delayed" and "partial" to the green success "Mirrored" dot, so a degraded feed shows a green status indicator in the mirror strip (the separate warning banner is honest, but the strip's green dot contradicts it).

- **Evidence:** artifacts/trading-dashboard/src/components/charts/ARXNativeChart.tsx:127 (quality === "delayed" || "partial" → "Mirrored") + :133-138 (MIRROR_STYLE.Mirrored = bg-success/text-success); simultaneous honest banner at :864-865+1118-1150 keeps this cosmetic
- **Suggested fix:** Map delayed/partial to a caution-toned "Mirrored (delayed)" state or reuse the Syncing style.

### L2 [STALE_UNLABELED] Market Scanner + scanner chart (market-scanner.tsx, ScannerChartPanel, ARXNativeChart, scannerTruth/scannerActionability/chart-display-status/feed-confidence, scanner components) → /api/chart/candles + /api/chart/tick-stream + /api/market-scanner/* → chartDataService/formingBarComposer/marketDataRouter/derivProvider

The Scanner chart's SSE tick-stream has no stall detection — es.onerror is a no-op and there is no silence watchdog — so a connection that dies without a browser error (proxy timeout, network change) leaves the scanner chart looking tick-live (LIVE badge from the poll) while actual price motion degrades silently to the 15s poll cadence; the ARX native chart got exactly this watchdog + an honest "Reconnecting — prices delayed" badge (Theme C3.4/C3.5) but the scanner panel was never retrofitted.

- **Evidence:** artifacts/trading-dashboard/src/components/scanner/ScannerChartPanel.tsx:1401-1404 (onerror: "nothing to do", no watchdog, no stalled badge) vs artifacts/trading-dashboard/src/components/charts/ARXNativeChart.tsx:355+560-564+939-948 and components/charts/useTickStreamWatchdog.ts:4-17 (the documented failure mode: a nominally-open stream that silently stops delivering)
- **Suggested fix:** Wire useTickStreamWatchdog into ScannerChartPanel's SSE effect and render the same reconnecting badge.

### L3 [GUESSED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

Per-symbol win rates are quoted from samples as small as one closed trade in the scanner-side Journal & Lessons panel ('1 closed · 100% won'), and the denominator counts UNKNOWN-outcome closes as non-wins — violating the app's own stated bar (the journal page refuses to print a rate below 5 decided because 'the number is noise').

- **Evidence:** artifacts/api-server/src/lib/scalp/scalpJournalService.ts:514 (winRatePct for any tradesClosed>0); UNKNOWN inflates the denominator via scalpJournal.ts:404-415 (applyPersonalityDelta counts every close) and the '(X% wins so far)' notes at scalpJournal.ts:461-469 (from 3 samples); displayed bare at trading-dashboard/src/components/scanner/RubyScalpReviewPanel.tsx:131-134, while the sibling page enforces ≥5 at src/pages/scalp-journal.tsx:271-279,490-501.
- **Suggested fix:** Apply the journal page's min-sample rule in the panel and backend (null winRatePct below 5 decided; exclude UNKNOWN from the denominator).

### L4 [STALE_UNLABELED] SCALP system ('Ruby Scalp' / 'Ruby Flame Scalp') @ repo /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (branch phase6/guided-mode). Backend: artifacts/api-server/src/routes/meScalp.ts → lib/scalp/{scalpService,scalpEngine,scalpServiceInputs,flameRead,scalpManage,scalpManageService,scalpJournal,scalpJournalService}.ts, fed by lib/marketScanner.ts scanSymbolTimeframe + lib/data/marketDataRouter.ts. Frontend: artifacts/trading-dashboard/src/components/scanner/{ScalpSignalCard,RubyScalpScan,RubyScalpFocusCard,RubyScalpBasketPanel,RubyScalpReviewPanel,scalpLabels}.tsx/ts, components/live/LiveScalpAddOnPanel.tsx, pages/scalp-journal.tsx, and the scalp wiring + trade-ticket bridge in pages/market-scanner.tsx.

The flame 'freshness' tooltip tells the user it measures 'how recent the live candle read behind this signal is', but the value is derived purely from the momentum stage (IGNITING→Fresh, WEAKENING→Going stale), not data recency — a user can read 'Fresh' as a data-freshness guarantee; relatedly, basket 'Break-even' is just the volume-weighted average entry with no spread/commission, and the FLAME_FRESHNESS 'Still good' label shows even on stage NONE.

- **Evidence:** Tooltip text at artifacts/trading-dashboard/src/components/scanner/ScalpSignalCard.tsx:248-254 vs derivation freshnessFor(stage) at artifacts/api-server/src/lib/scalp/flameRead.ts:407-419; break-even = averageEntry at scalpManage.ts:153-160, labeled 'Break-even' at RubyScalpBasketPanel.tsx:66-69.
- **Suggested fix:** Reword the tooltip to 'how far along the momentum burst is', and label break-even 'avg entry (excl. costs)'.

### L5 [GUESSED] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

SessionCard's 'Fakeout risk NN%', 'Heat bonus +N' and the 'Best this session' / 'Caution this session' symbol badges are hard-coded per-session constants and static symbol lists rendered like live per-session intelligence, with no 'typical/static' label.

- **Evidence:** api-server/src/brain/timing/sessionKillZoneEngine.ts:26-59 hard-codes heatBonus/tradeabilityBonus/fakeoutRisk (e.g. 45/55/60/40) and bestSymbols/dangerSymbols arrays per session; rendered at trading-dashboard/src/pages/market-heat-map.tsx:542-568 as ScorePills and green/red recommendation badges alongside genuinely live scores.
- **Suggested fix:** Label these as session-typical characteristics ('typical for London session') to distinguish them from computed readings.

### L6 [CONFIDENT_ABSENT] HEAT MAPS — Market Heat Map page (timing-brain tabs + Replay), Global Market Heat card (world map + grid + top-hot), Analytics Command Center Session Heatmap, backend heatmap builders (/analytics/heatmaps, replay-lab risk heatmap) and the heat alert emitter

hasNewsData conflates 'no current event' with 'no news feed', so when a calendar provider IS connected but quiet, the News Heat tab wrongly asserts 'No live news feed connected — showing basic timing estimate only', discrediting a real reading (mislabel in the safe direction).

- **Evidence:** api-server/src/brain/timing/marketTimingBrainService.ts:104 `hasNewsData = newsOverlay.phase !== "NONE" || newsOverlay.blocksTrade` — computeNewsHeat returns phase NONE both when disconnected and when connected-but-quiet (newsHeatEngine.ts:60-66 sets calendarConnected but it is dropped from the overlay); trading-dashboard/src/pages/market-heat-map.tsx:813-819 renders the 'No live news feed connected' claim, and marketTimingBrainService.ts:319 folds 'no scheduled news events' into the 'Partial data' note.
- **Suggested fix:** Propagate the provider's connected flag into the overlay/dataQuality and word the empty state as 'feed connected — no events' vs 'feed not connected'.

### L7 [FABRICATED] Trader Cockpit — pages/trading-cockpit.tsx (route /trading-cockpit) backed by GET /api/trading-cockpit/summary (api-server/src/routes/tradingCockpit.ts), traced through lib/paperSession/manager.ts, lib/paperExecution/*, routes/paperTrading.ts and lib/marketData/marketDataService.ts; plus the cockpit-named dashboard components that carry the app's actual equity/balance/open-P&L readouts (components/dashboard/cockpit/CockpitCards.tsx → hooks/useLiveAccountSnapshot.ts → routes/meLive.ts → lib/live/liveAccountSnapshot.ts) and pages/admin/cockpit.tsx checked for contrast. Repo root: /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh (evidence paths are relative to <root>/artifacts/trading-dashboard/src, <root>/artifacts/api-server/src, and <root>/lib/db/src as written).

SL/TP closures always fill at exactly the stop/target price even when the observed price has already gapped beyond it, so realized P/L is derived from an assumed perfect fill that systematically flatters paper results feeding the cockpit's performance panel.

- **Evidence:** paperExecutionMonitor.ts:101 detects the hit with currentPrice, but :111-112 sets exitPrice = order.stopLoss ?? order.takeProfit (never currentPrice) and computes realized from it; same pattern at paperTrading.ts:106-108. The 'Simulated' tag lives only in event text (paperExecutionMonitor.ts:122), not on the cockpit numbers built from these rows (tradingCockpit.ts:94-99).
- **Suggested fix:** Fill at min/max(currentPrice, level) per direction (gap-aware), or disclose 'fills assume no gap/slippage' wherever paper P/L totals render.

### L8 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

livePositions close-confirmation summary renders a null unrealized P/L as a confident 0 and substitutes entryPrice where currentPrice is unknown ('Close BUY X @ <entry> (uPnL 0)') in the text the user is asked to confirm against (consumer modal currently unmounted).

- **Evidence:** artifacts/api-server/src/routes/livePositions.ts:460 (`uPnL ${row.unrealizedProfitLoss ?? 0}`, `@ ${row.currentPrice ?? row.entryPrice}`); rendered verbatim by artifacts/trading-dashboard/src/components/positions/ClosePositionConfirmModal.tsx:36-38.
- **Suggested fix:** Say 'uPnL unavailable' when null and label the price as entry when current is missing.

### L9 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

UnrealizedPnLCard and OpenPositionsPanel (exported but currently unmounted) render a confident green '0.00' / '0 open' on a failed or absent read and collapse per-position null uPnL to 0.00; OpenPositionsPanel additionally shows perpetual 'Loading positions…' on error, indistinguishable from loading.

- **Evidence:** artifacts/trading-dashboard/src/components/positions/UnrealizedPnLCard.tsx:4-11 (reduce over data?.positions ?? [] with ?? 0, no error state); artifacts/trading-dashboard/src/components/positions/OpenPositionsPanel.tsx:12 (isLoading || !data → loading forever), :32,:41 (unrealizedProfitLoss ?? 0 → '0.00'); unmounted: only live-chart.tsx imports from @/components/positions and takes PositionPickerPanel alone (pages/live-chart.tsx:14).
- **Suggested fix:** Add error states and null-preserving rendering before these components are ever mounted.

### L10 [GUESSED] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

LiveOpenTradesPanel's 'Net P/L' column header overstates the figure: the server structurally has no swap/commission (hard-coded null) and sets netProfit identical to gross floating P/L, so 'Net' implies costs included that are never known.

- **Evidence:** artifacts/api-server/src/routes/meLiveAccount.ts:226-230 (swap/commission null 'honestly rather than zero', netProfit = grossProfit = floating); artifacts/trading-dashboard/src/components/live/LiveOpenTradesPanel.tsx:138 ('Net P/L' column), :154.
- **Suggested fix:** Label the column 'Floating P/L' (as the row label already does elsewhere) until swap/commission are actually reported.

### L11 [CONFIDENT_ABSENT] LIVE POSITIONS / OPEN TRADES / trade history (pages live-trades, live-trading, positions, my-trades, position-control, live-chart picker; routes livePositions.ts, meLive.ts, meLiveAccount.ts, mePositionsUnified.ts, meTrades.ts, trades.ts, mt5.ts, meGuidedPositions.ts) — checkout root /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f

My Trades aggregates (page 'Open P/L' summary and /me/trades/summary openPnl/closedPnl) sum null P/L rows as 0 with no incompleteness marker, even while the same rows are individually flagged 'syncing…' — the total reads complete when it is not.

- **Evidence:** artifacts/trading-dashboard/src/pages/my-trades.tsx:221 (reduce (c.unrealizedPnl ?? 0)) vs :516 (per-row 'syncing…'); artifacts/api-server/src/routes/meTrades.ts:274,:285-286 (openPnl += r.unrealizedProfitLoss ?? 0 / r.pnl ?? 0).
- **Suggested fix:** Carry an includedCount/excludedNullCount alongside the sums and show 'incomplete' when any counted row lacks a P/L.

### L12 [CONFIDENT_ABSENT] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

ExecutionQualityDetail renders green 'Execution OK' / 'Net profit OK' / 'Exposure OK' badges whenever the verdict field is null or missing (only an explicit === false shows blocked), violating the same file's 'honest unknown is rendered as Unknown, never good' rule for legacy or partially-populated executionQualityJson rows.

- **Evidence:** artifacts/trading-dashboard/src/pages/profit-missions.tsx:1237-1241 (execAllowed === false ? blocked : green OK — null falls to OK), :1258-1262 (netAllowed same pattern), :1283-1285 (expoAllowed); the stated rule at :1178-1179,1199; normally-full payload written at artifacts/api-server/src/lib/missionAgents.ts:527
- **Suggested fix:** Three-state the badges: null → neutral 'Unverified', true → OK, false → blocked

### L13 [GUESSED] PROFIT MISSIONS — trading-dashboard pages/profit-missions.tsx (+ profitMissionPlanner.ts, components/missions/*) → api-server routes/profitMissions.ts → lib/mission* services → lib/domain/src/profit-mission engines

A proposal's scanner heuristic confidenceScore is shown as a bare 'Confidence NN%' on the proposal row and is silently reinterpreted as the win probability driving the Mission-impact 'Expected' scenario (winProbability = confidence/100, defaulting to 0.5), with no basis label on either number (the impact table does carry a generic 'Estimate only' footer).

- **Evidence:** artifacts/trading-dashboard/src/pages/profit-missions.tsx:1070-1071 (bare %); artifacts/api-server/src/lib/missionAgents.ts:472 (winProbability: confidence/100); lib/domain/src/profit-mission/missionImpact.ts:96-101 (default 0.5, clamped); footer at profit-missions.tsx:1168-1171
- **Suggested fix:** Label the row 'Scanner confidence (heuristic)' and state the expected-case basis ('weighted by scanner confidence, not a measured win rate') in the impact footer

### L14 [GUESSED] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

The Ruby Market Read card's no-trade panel shows '{N}% sure' where N is a hand-coded constant per rule branch (90/92/78/74/66/60/45), presenting a fixed heuristic as a measured confidence percentage.

- **Evidence:** lib/domain/src/signal-intelligence/explainMarketRead.ts:282-319 (computeNoTrade constants) → rendered as `{Math.round(explanation.noTrade.confidence)}% sure` at artifacts/trading-dashboard/src/components/scanner/RubyMarketReadCard.tsx:446. Mitigating: it always argues for sitting out, so the lie points toward caution.
- **Suggested fix:** Replace '% sure' with a qualitative band ('high confidence in waiting') or label it as a rule-based score.

### L15 [CONFIDENT_ABSENT] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

TwelveData's quote adapter coerces a present-but-unparseable close to price 0 while stamping freshness REALTIME and stale:false, so a malformed provider payload can surface as a confident real-time price of 0 when TwelveData is the sole quote provider.

- **Evidence:** artifacts/api-server/src/lib/assistant/marketProvider.ts:450-461 — `price: num(j.close) ?? 0` inside a branch that already passed the j.close==null check, then `freshness: "REALTIME", stale: false` and markSuccess. The composite chain filters price>0 (line 983) but a single-provider chain (only TWELVEDATA_API_KEY set) returns it to getMarketSnapshot's quote enrichment and refresh probes.
- **Suggested fix:** Return emptyQuote(...,'UNAVAILABLE') when num(j.close) is null instead of defaulting to 0.

### L16 [CONFIDENT_ABSENT] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

getRoutingContext's error fallback answers with a typed accountRoutingMode of 'USER_OWNED_MT5' / connectionType 'user_owned', so when the envelope module fails, a shared-master user asking Ruby 'where do my trades go' can be told a definite wrong routing mode (the system prompt instructs Ruby to quote it verbatim).

- **Evidence:** artifacts/api-server/src/lib/assistant/tools.ts:234-243 — catch returns accountRoutingMode:'USER_OWNED_MT5', connectionType:'user_owned' as concrete enum values; only the free-text explanation says 'temporarily unavailable'.
- **Suggested fix:** Return accountRoutingMode:null / connectionType:'unknown' in the fallback so the model cannot quote a guessed mode.

### L17 [STALE_UNLABELED] RUBY / AI assistant market-fact outputs — Ruby Chart Read, Ruby Market Read, Timing Intelligence, news-risk panel, assistant briefing/chat/voice routes + lib/assistant/* (repo checkout at /private/tmp/claude-501/-Users-areyouok/a1620dc1-92f1-4ae2-97bc-c424ed64b487/scratchpad/arxai-fresh, branch phase6/guided-mode @ 726d09f)

The Ruby Chart Read panel replays a previously fetched read from the page-level store with no timestamp or age indicator, so a read taken minutes earlier renders identically to a fresh one until the user presses 'Re-read'.

- **Evidence:** artifacts/trading-dashboard/src/components/scanner/RubyChartRead.tsx:94-101 (store.get(symbol,timeframe) keyed cache, no generatedAt/age rendered anywhere in the body at 239-297; the live feed badge reflects current feed state but not the read's age). Contrast RubyMarketReadCard which ticks 'updated Xs ago' from data timestamps (RubyMarketReadCard.tsx:84-100).
- **Suggested fix:** Stamp the read with its fetch time and render the same 'updated Xs ago' label the Market Read card uses.

### L18 [STALE_UNLABELED] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

The footer version badge shows the compile-time literal '0.9.0-beta' from Build UU while the health endpoints report the env-derived build version — the badge never tracks the deployed build.

- **Evidence:** artifacts/trading-dashboard/src/components/layout/Footer.tsx:22 ← artifacts/api-server/src/routes/release.ts:18-20 ← artifacts/api-server/src/lib/release.ts:15 (`APP_VERSION = "0.9.0-beta"`); the env-derived version is at artifacts/api-server/src/routes/health.ts:28-31 and adminRuntimeHealth.ts:25-27.
- **Suggested fix:** Serve versionEnvelope from the same env-derived APP_VERSION used by /healthz.

### L19 [GUESSED] STATUS and health readouts: ARX Status Command Center, System Health (Build MM report + page), Release Status/Notes/version+beta badges, Admin Security Status, admin runtime/EA/provider/Deriv health endpoints, Topbar/SafetyHeader connection pills, dashboard cards, notification counts, and kill-switch/arming displays vs the tables that gate dispatch.

Subsystem LL's health note asserts 'critical alerts work' from mere HTTP reachability of /api/notifications — the claim of working alert delivery is never tested.

- **Evidence:** artifacts/api-server/src/lib/systemHealth/health.ts:253 (notes: ["notifications reachable", ..., "critical alerts work"] gated only on r1.ok) rendered in the Subsystems grid at artifacts/trading-dashboard/src/pages/system-health.tsx:77-85.
- **Suggested fix:** Say what was tested: 'notifications endpoint reachable'.

### L20 [GUESSED] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

Every money render hard-codes '$' while the platform's own accounting layer treats account currency as unknowable and refuses to guess it — trades.pnl has no currency column, so a non-USD MT5 account's P/L is displayed with a dollar sign as fact.

- **Evidence:** artifacts/trading-dashboard/src/pages/analytics.tsx:48-54 and artifacts/trading-dashboard/src/lib/format.ts:8-11 (literal '$'); contrast artifacts/api-server/src/lib/accounting/economicSeams.ts:32-45 (bridgeAccountCurrency returns honest null) and economicReconciliationWorker.ts:181-190 (refuses MIXED currencies); lib/db/src/schema/trades.ts:18 (pnl carries no currency).
- **Suggested fix:** Carry accountCurrency through the performance/summary responses and format with it, falling back to an explicit 'account currency' label rather than '$'.

### L21 [FABRICATED] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

Profit factor is a fabricated 9.99 sentinel whenever there are wins and zero losses, and the UI renders it as a real ratio '9.99'.

- **Evidence:** artifacts/api-server/src/routes/performance.ts:88 (`grossLoss > 0 ? ... : grossProfit > 0 ? 9.99 : 0`); rendered numerically at artifacts/trading-dashboard/src/pages/analytics.tsx:272.
- **Suggested fix:** Return null (or an explicit '∞ / no losses yet' marker) and label it, instead of a plausible-looking 9.99.

### L22 [CONFIDENT_ABSENT] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

PaperPerformanceSummary renders confident zeros — 'Closed 0', a red '0.0%' win rate, 'Net P&L 0.00' — while loading and permanently after a failed fetch (mitigated by its prominent 'Simulated' banner).

- **Evidence:** artifacts/trading-dashboard/src/components/paperTrading/PaperPerformanceSummary.tsx:29-33 (`data?.winRate ?? 0` etc., isError/isLoading unused).
- **Suggested fix:** Render '—' until data resolves; show an error line on failure.

### L23 [DEAD_GAUGE] ACCOUNTING and balance truth — lib/accounting (economicPostings, truthHierarchy), balance/equity displays, equity curves, session stats, win-rate/analytics readouts

Two cosmetic misstatements on Analytics: the equity-curve empty state says history appears 'after account snapshots are recorded' though the series is built from closed trades, and the 'Open Exposure' bar is a meter that fills to 100% the moment any single position exists (no exposure quantity behind the fill).

- **Evidence:** artifacts/trading-dashboard/src/pages/analytics.tsx:334-335 (snapshot claim; data source is /performance/daily built from tradesTable, artifacts/api-server/src/routes/performance.ts:184-218) and :376 (`pct={positions.length ? 100 : 0}`).
- **Suggested fix:** Fix the empty-state copy; scale the bar by lots/exposure or drop the fill and keep the count.

### L24 [DEAD_GAUGE] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

The final onboarding step promises 'You can re-run onboarding any time from the Help Center', but the Help Center page contains no onboarding/tour affordance at all — the only 'Start app tour' control lives in the retired FloatingHelpWidget, which a guard test pins as having zero importers, so showTour() is unreachable in the mounted app.

- **Evidence:** artifacts/api-server/src/lib/onboarding/steps.ts 'complete' step help_text; pages/help-center.tsx has no match for onboarding/tour (grep empty); components/help/FloatingHelpWidget.tsx:531 (the only 'Start app tour' row) is pinned unmounted by components/help/mountedAssistantTrigger.test.ts:73-83; components/onboarding/OnboardingProvider.tsx:236-238 comment claims the tour is 'reachable from the FloatingHelpWidget', which never renders.
- **Suggested fix:** Either add a 'Restart onboarding / app tour' action to help-center.tsx wired to useOnboarding().showTour, or change the step copy to point at the onboarding page's own Reset button.

### L25 [CONFIDENT_ABSENT] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

Onboarding and Help drawers have no failure path: a failed /api/onboarding/status read leaves the page on 'Loading onboarding…' forever, a failed why-blocked fetch leaves the drawer body blank after 'Loading…' clears, and a failed help-topics fetch renders the confident empty 'No help topics for this page yet'.

- **Evidence:** artifacts/trading-dashboard/src/pages/onboarding.tsx:30-33 (api() ignores r.ok; error envelope leaves status null → :49 permanent 'Loading onboarding…'); components/help/WhyBlockedDrawer.tsx:50-56 (no res.ok/catch; d.explanation undefined → setExp(null) → blank body); components/help/HelpDrawer.tsx:20 (.then with no catch) and :42 ('No help topics for this page yet' whenever topics.length===0, including after a failed read).
- **Suggested fix:** On !res.ok or a thrown fetch, set an explicit error state ('Couldn't load — retry') instead of eternal loading, a blank drawer, or the no-topics empty state.

### L26 [DEAD_GAUGE] Onboarding (page, provider/first-run modal, backend routes/steps/state), Trading School (all pages, progress layer, meTradingSchool route), permission banners (WhyBlockedDrawer, ApprovalPathCard, RouteAccessGuard, AdminOnlyGate, onboarding acks), and empty states (notifications page + header bell, help drawers, EmptyState pattern)

Latent, currently unreachable copies of the paper-only lie survive in dead code that the stale onboarding catalogue still points toward: HelpDrawer's header hard-codes contradictory 'LIVE' + 'LIVE TRADING DISABLED' badges on every open, and the unmounted trading-cockpit page's HelperCopy states 'This system is currently PAPER_ONLY. Live trading is disabled.' — one re-added route re-arms both.

- **Evidence:** artifacts/trading-dashboard/src/components/help/HelpDrawer.tsx:35-37 (static badges, never reading state; sole importer is pages/trading-cockpit.tsx, whose route was removed at App.tsx:194,349); pages/trading-cockpit.tsx HelperCopy (~line 417, 'This system is currently PAPER_ONLY. Live trading is disabled.') and its OnboardingWidget still polling /api/onboarding/status every 15s at :372-376.
- **Suggested fix:** Delete the retired trading-cockpit page or strip its PAPER_ONLY copy, and make HelpDrawer's badges read the real trading-mode envelope (or remove them) before the drawer is ever remounted elsewhere.
