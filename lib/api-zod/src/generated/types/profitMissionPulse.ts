/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * ARX AI — Analyze. Risk. eXecute. — API
 * OpenAPI spec version: 0.1.0
 */
import type { MissionExecutionHealth } from "./missionExecutionHealth";
import type { MissionExposureAggregates } from "./missionExposureAggregates";
import type { MissionProtectionSnapshot } from "./missionProtectionSnapshot";
import type { ProfitMissionFeasibility } from "./profitMissionFeasibility";
import type { ProfitMissionMath } from "./profitMissionMath";
import type { ProfitMissionProbability } from "./profitMissionProbability";
import type { ProfitMissionRiskState } from "./profitMissionRiskState";

/**
 * Live recompute of a mission's pace / feasibility / probability against the current account value. Display-only.

 */
export interface ProfitMissionPulse {
  id: number;
  currentValue: number;
  math: ProfitMissionMath;
  feasibility: ProfitMissionFeasibility;
  probability: ProfitMissionProbability;
  /** Phase 6 additive, stricter-only risk read (drawdown / blow-up / daily budget / protective mode). Display-only; can never relax a gate.
   */
  risk?: ProfitMissionRiskState | null;
  /** Phase 7 advisory broker/feed execution-health verdict (symbol-independent). Analyze/watch is always allowed; execution-block only. Honest unknown never reads "good". Display-only; can never relax a gate.
   */
  executionHealth?: MissionExecutionHealth | null;
  /** Phase 7 aggregate of this user's OPEN live exposure (per-user isolation). Display-only.
   */
  exposure?: MissionExposureAggregates | null;
  /** Phase 8 protection surface — milestones / locked profit / protection ladder / giveback guard + controlled-compounding state. Recomputed + persisted (merged into progressJson) on every pulse. Display-only; a target stop+lock flips the mission to completed but never relaxes a gate.
   */
  protection?: MissionProtectionSnapshot | null;
  /** ISO 8601 server time of this recompute. */
  asOf: string;
}
