/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * ARX AI — Analyze. Risk. eXecute. — API
 * OpenAPI spec version: 0.1.0
 */
import type { Candle } from "./candle";
import type { ChartHistoryResponseAssetClass } from "./chartHistoryResponseAssetClass";
import type { ChartHistoryResponseStatus } from "./chartHistoryResponseStatus";
import type { ChartHistoryResponseTimeframe } from "./chartHistoryResponseTimeframe";

/**
 * A page of candle history with honest, non-fabricated metadata. Served from one coherent source; historical/stale data is never labelled live.

 */
export interface ChartHistoryResponse {
  ok: boolean;
  symbol: string;
  timeframe: ChartHistoryResponseTimeframe;
  assetClass: ChartHistoryResponseAssetClass;
  /**
   * The single source this page was served from (provenance).
   * @nullable
   */
  source: string | null;
  /** Ascending by time. */
  candles: Candle[];
  /** live/stale apply only to the newest window; a page reached via the `before` cursor is always historical_only; unavailable = no source/candles.
   */
  status: ChartHistoryResponseStatus;
  returnedCount: number;
  /**
   * Oldest bar time in this page.
   * @nullable
   */
  oldest: string | null;
  /**
   * Newest bar time in this page.
   * @nullable
   */
  newest: string | null;
  /** Older bars exist (in cache or pageable from a deep-cursor provider). */
  hasMoreHistory: boolean;
  /**
   * Cursor to pass as `before` for the next (older) page; null when exhausted.
   * @nullable
   */
  nextBefore: string | null;
  /** The provider could not reach the requested depth (honest ceiling, not truncation). */
  providerLimitReached: boolean;
  /** @nullable */
  providerMessage: string | null;
  /** Served from cache without a fresh provider fetch this call. */
  cacheHit: boolean;
  fetchedAt: string;
  /** Per-timeframe deep-history target in days. */
  depthTargetDays: number;
  /**
   * Days between oldest and newest cached bar for this source.
   * @nullable
   */
  coverageDays: number | null;
  depthTargetMet: boolean;
  /** The history provider chain (deepest-capable first) consulted for this asset class. */
  sourcePriorityUsed?: string[];
  /**
   * Why deeper history is unavailable when a real provider ceiling was hit (null otherwise).
   * @nullable
   */
  limitationReason?: string | null;
  userMessage: string;
  adminDetail?: string;
}
