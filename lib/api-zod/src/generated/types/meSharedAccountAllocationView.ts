/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * ARX AI — Analyze. Risk. eXecute. — API
 * OpenAPI spec version: 0.1.0
 */
import type { MeSharedAccountAllocationViewBridgeAvailability } from "./meSharedAccountAllocationViewBridgeAvailability";

/**
 * Canonical per-user live allocation — the SAME source the live dispatch gate, chart/scanner/assistant preflight, and /me/master-live/access use (getUserAllocationView). Strictly the caller's own allocation; never master or cross-user data. Display surfaces must read availableAllocation from here instead of the static virtual_balance, which drifts from the headroom the gate actually enforces.

 */
export interface MeSharedAccountAllocationView {
  assignedAllocation: number;
  /** assigned − reserved + open floating loss, floored at 0. */
  availableAllocation: number;
  reservedRisk: number;
  /** Sum of the caller's own open floating losses (<= 0). */
  openFloatingLoss: number;
  bridgeAvailability: MeSharedAccountAllocationViewBridgeAvailability;
  bridgeMessage: string;
  /** True only when an allocation row exists AND assigns > 0. Lets the UI distinguish "no allocation assigned yet" from "fully consumed"; both still block live dispatch but need different honest copy.
   */
  hasAllocation: boolean;
  isOverAllocated: boolean;
}
