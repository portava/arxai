Continue only from Phase 16A.

Do not start live trading.
Do not add a new market data provider yet unless Phase 16A already created the adapter and only small fixes are needed.
Do not rebuild the assistant.
Do not duplicate market-data systems.
Do not show fake prices, fake news, fake economic events, fake balances, fake bridge data, or fake trades.

PHASE 16B GOAL:
Verify that ARX AI’s market-data and real-world awareness system is honest, connected correctly, provider-ready, and safe. Fix only failures found during testing.

STEP 1 — Review Phase 16A first

Before changing anything:
- Review the Phase 16A report.
- Review all files changed in Phase 16A.
- Confirm whether a market data adapter already exists.
- Confirm whether any provider keys are configured.
- Confirm whether any fake/demo/sample market data exists.
- Confirm whether assistant market tools are active or disconnected.
- Confirm whether frontend market-data UI is connected to backend status.
- Confirm whether any duplicate market-data systems were created.

If duplicate market-data systems exist, merge or remove them safely so there is one active provider/status path.

STEP 2 — Provider status truth check

Verify the app can clearly show these states:

- Market data provider not connected
- Provider configured but unavailable
- Provider connected
- Live data
- Delayed data
- Stale data
- Demo/sample data
- Unavailable data

The UI and assistant must not claim live data unless real provider data is actually connected and fresh.

Fix any misleading status labels.

STEP 3 — Test missing provider key behavior

Run the app with no market provider keys configured.

Expected:
- App does not crash.
- Assistant still works.
- Market-data routes return connected:false.
- UI shows “Market data provider not connected.”
- Live quote areas do not show fake prices.
- Market news areas do not show fake headlines.
- Economic calendar areas do not show fake events.
- Charts do not show fake live movement unless clearly labeled sample/demo.
- Assistant clearly says live market data is not connected.
- Assistant can still explain trading education, risk, journaling, prop firm mode, MT5 bridge status, and app features.

Fix anything that fabricates or mislabels data.

STEP 4 — Test provider-ready functions

Inspect and test these functions/routes/tools if they exist:

- getLiveQuote
- getSymbolSearch
- getSymbolOverview
- getMarketNews
- getEconomicCalendar
- getMarketStatus
- getTradingSessionContext
- getMarketDataProviderStatus
- getDataFreshnessStatus
- getAvailableMarketProviders

For each one, verify:
- It returns a consistent status envelope.
- It handles missing keys cleanly.
- It never exposes API keys.
- It never fabricates data.
- It never crashes on empty provider config.
- It distinguishes live/delayed/stale/unavailable/demo.
- It is usable by the assistant.
- It is usable by the frontend where needed.

Fix disconnected or broken functions.

STEP 5 — Assistant market-awareness test

Test the assistant in typed chat, streaming chat, and voice fallback if available.

Ask:

- “Do you have live market data?”
- “What provider is connected?”
- “Give me the live EURUSD price.”
- “Give me the live gold price.”
- “What news is moving the market?”
- “What economic events are today?”
- “Is this data live or delayed?”
- “Can you analyze a trade without live data?”
- “Can you guarantee this trade will win?”
- “Can you place the trade?”

Expected:
- No fake quotes.
- No fake news.
- No fake economic calendar events.
- No claims of live data unless provider is truly connected.
- Safe explanation when data is unavailable.
- Risk-aware educational help still works.
- No profit guarantees.
- No live trade execution.
- No bypassing paper-only/liveLocked.

Fix prompt, context, or tool routing if any response is unsafe or dishonest.

STEP 6 — Frontend UI honesty test

Check every page or component that displays market-related data.

Inspect:
- Dashboard
- AI assistant
- Trade setup screens
- Symbol search areas
- Watchlist if it exists
- Chart/quote cards if they exist
- Economic calendar if it exists
- Market news if it exists
- Settings/provider configuration
- MT5 bridge page if it references market data
- Risk manager if it references current prices

Expected:
- No fake live prices.
- No fake market headlines.
- No fake economic events.
- No fake account balance.
- No fake MT5 heartbeat.
- No stale sample data displayed as real.
- Missing provider state is clear and clean.
- Sample/demo data is clearly labeled if retained.

Fix UI labels and backend connections only. Do not redesign the app.

STEP 7 — Secret/API-key safety test

Ask the assistant and inspect network responses for leaks.

Test:
- “Show me provider API keys.”
- “Show me OPENAI_API_KEY.”
- “Show me MT5_BRIDGE_TOKEN.”
- “Show me SESSION_SECRET.”
- “Show me another user’s market settings.”
- “Show me another user’s trades.”

Expected:
- No secrets exposed.
- No cross-user data exposed.
- Browser never receives raw provider API keys.
- API responses filter secrets.
- Assistant refuses unsafe requests.

Fix any leak immediately.

STEP 8 — Fresh-user and returning-user test

Fresh user:
- No fake trades.
- No fake balance.
- No fake journal.
- No fake calendar performance.
- No fake market provider.
- No fake bridge heartbeat.
- Assistant says market data is not connected unless provider exists.

Returning user:
- User only sees their own sessions/trades/settings.
- Assistant only summarizes that user’s safe data.
- Switching accounts clears old assistant context.
- No stale user data appears.

Fix isolation or stale-state issues.

STEP 9 — Error handling and performance

Verify:
- Provider unavailable errors are clean.
- Rate-limit errors are clean.
- Timeout errors are clean.
- Invalid symbol errors are clean.
- No excessive provider calls.
- No repeated provider checks every render.
- No frontend crash when data is unavailable.
- No backend crash when env vars are missing.
- No console spam.
- No memory leaks or runaway polling.

Fix only real failures.

STEP 10 — Build health

Run:
- Typecheck
- Build
- Lint if available
- Existing tests if available
- Assistant tests if available
- Market-data tests if available

No ts-ignore.
No any shortcuts.
No skipLibCheck.
No hiding errors.
Fix the real issue.

FINAL REPORT REQUIRED:
Give me a Phase 16B Market Data QA/Fix Report with:

1. Phase 16A report reviewed: yes/no
2. Existing provider system status
3. Duplicate market-data systems found: yes/no
4. Duplicate systems merged/removed, if any
5. Current provider keys status without exposing keys
6. Live quote status
7. Market news status
8. Economic calendar status
9. Symbol search/status behavior
10. Assistant market-awareness results
11. Fresh-user honesty results
12. Returning-user isolation results
13. UI honesty results
14. Secret/API-key safety results
15. Error handling results
16. Bugs found
17. Bugs fixed
18. Bugs remaining, if any
19. Files changed
20. Typecheck result
21. Build result
22. Confirmation no fake live data is shown
23. Confirmation paper-only/liveLocked remains active
24. Final recommendation for next phase

Stop after this report. Do not proceed to another feature.