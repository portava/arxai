This is MQL5 — your EA code — and it looks like a diff you're sanity-checking. Two issues in it, one real bug and one cosmetic:
1. Missing comma before brokerTimeEpochMs (real bug — produces invalid JSON). Look at the line order: the currency line ends with , but the line before brokerTimeEpochMs is marginLevel, which already has its comma. The problem is the brokerTimeEpochMs line itself doesn't need a trailing comma (it's last before }) — that part's correct. But check the currency line: it ends with \", — good. So the chain is balance, ... freeMargin, marginLevel, currency, brokerTimeEpochMs }. That's valid as long as every line except the last has a comma. The last line (brokerTimeEpochMs) correctly has none. So the comma structure is actually fine.
The real thing to flag: that -1+1 on its own line in the diff. That's not valid MQL5 — if it literally landed in the source, it won't compile. It looks like a diff artifact (a malformed hunk marker, or a line that got mangled). Whatever was originally on that line (probably payload += "\"equity\":" + SafeF(eq, 2) + ","; or "margin") has been replaced by garbage. Check what that line should be — you have a freeMargin line right after but I don't see equity or margin emitted anywhere, and mg/ml (margin, marginLevel) are referenced, so a margin line is likely missing too.
2. The brokerMsField change itself is correct and good. Replacing (string)((long)TimeTradeServer() * 1000) with the guarded brokerMsField is the right call: when the EA is disconnected, TimeTradeServer() returns 0, and the old code would emit "brokerTimeEpochMs":0 — a misleading "epoch 1970" timestamp. The new version emits null when brokerMs <= 0, which is honest (matches the int>0|null contract in your comment). Same pattern as the marginLevel guard above it. This is exactly the kind of "don't emit a false value, emit null" discipline that's consistent with everything else we've done. Keep it.
So my read: the brokerTimeEpochMs guard change is good and should ship. But that -1+1 line is a corrupted hunk — before you compile, restore whatever real payload += line belonged there. If you paste the surrounding 5–6 lines of the actual file (not the diff), I'll tell you exactly what's missing and whether the comma sequence holds with the real line in place.
One caveat: I can read MQL5 logic, but I can't compile it here — so treat this as a code review, not a verified build. Want to paste the full AccountSnapshot function so I can check the whole payload's comma/field integrity at once?Opus 4.8 High