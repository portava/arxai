Read-only diagnosis. On the dev preview (latest code, Phase 1 merged), EURUSD (MT5 broker feed, price LIVE-ticking) shows the scanner reading "bearish" while Ruby Chart Read says "Chart intelligence unavailable — cannot verify chart data / candles syncing." This is the SAME scanner-vs-Ruby contradiction Phase 1 was supposed to fix, but on the MT5/forex path. Find why Phase 1 didn't cover it. Do NOT fix yet — report with evidence.

1. Ruby's "cannot verify chart data" wording is the OLD chartIntelligence failure, NOT the Phase-1 shared humanReason ("Not enough closed candles (n/5)" / "Waiting for live feed"). Confirm: which exact code path/line emits "cannot verify chart data" / "candles syncing" for Ruby, and does it fire BEFORE or AFTER evaluateMarketDataSufficiency is called? Paste the call order in rubyChartContext.ts / the read-chart endpoint / chartIntelligence.ts.

2. THE KEY QUESTION: does Ruby's read path for an MT5/forex symbol actually call the Phase-1 sufficiency engine, or does it short-circuit to the legacy "cannot verify" state first? chartIntelligence returns null "when nothing is cached" — is Ruby hitting that null/empty-cache path (→ legacy unavailable message) BEFORE the sufficiency verdict governs the output? Show the branch.

3. CACHE vs DIRECT FETCH: the scanner appears to fetch candles directly (reads them fine), while chartIntelligence reads from a pre-built cache (returns null if unpopulated). For EURUSD 15m right now, mint a temp session and check: (a) does the scanner/candle path return candles? (b) is the chartIntelligence cache populated for EURUSD 15m, or empty/stale? (c) what does evaluateMarketDataSufficiency return for EURUSD 15m directly — sufficient, or insufficient? Paste all three.

4. So which is it: (A) Ruby's MT5 path never reaches the sufficiency engine because chartIntelligence returns null first (a wiring gap — engine not consulted on the cache-miss path), or (B) the engine IS consulted and correctly says insufficient because EURUSD genuinely has <5 closed cached bars (a cache-population problem, not a verdict problem)? The fix differs completely. Determine which with evidence.

5. Is this MT5-specific? Does the same Ruby read path work for a Deriv synthetic right now (V75) but fail for MT5 (EURUSD)? If V75 reads fine and EURUSD doesn't on the same code, the gap is in the MT5/forex candle→intelligence path specifically.

Report: the exact emitter of the "cannot verify" message and whether it precedes the sufficiency engine; whether Ruby's MT5 path consults the engine at all; the three EURUSD 15m data points (scanner candles / cache state / engine verdict); and the definitive answer to #4 (A wiring gap vs B cache-population). State the ONE root cause and the targeted fix — but do NOT apply it.