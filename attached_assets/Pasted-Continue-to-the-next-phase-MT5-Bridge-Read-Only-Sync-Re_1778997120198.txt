Continue to the next phase: MT5 Bridge Read-Only Sync + Reconciliation + Command Queue Safety.

Do NOT unlock live trading.
Do NOT enable real auto-close execution.
Do NOT enable shared MT5 routing.
Do NOT send real MT5 execution, modification, or close commands.
Do NOT weaken paper-only, live-trading, MT5 bridge, ALERT_ONLY, user confirmation, user ownership, duplicate-action, risk-limit, attribution, or activityStatus gates.
Check what already exists first, then adjust only what is needed.

Before coding:
1. Read the latest Per-User Account Ledger + Portfolio/P&L + Shared MT5 Account Safety QA report.
2. Confirm final status is READY FOR NEXT PHASE.
3. Confirm P0/P1 issues are None.
4. Confirm typecheck, guard suite, ledger isolation, shared MT5 attribution-block, risk-limit, P&L fallback, AI portfolio, audit trail, and frontend smoke tests were green.
5. If anything is not green, stop and fix only confirmed P0/P1 issues first.

Goal:
Build the bridge sync/control layer so the app can safely read MT5 state, reconcile paper/app trades against broker state, and prepare a future command queue without allowing real execution yet.

A. MT5 Bridge Health + Read-Only Status
Create or verify a bridge health model that tracks:
- bridgeConnected
- lastHeartbeatAt
- bridgeMode: OFFLINE, READ_ONLY, PAPER_ONLY, LIVE_LOCKED
- accountLogin if safely available
- serverName if safely available
- terminalConnected if available
- eaVersion if available
- readOnlyMode
- allowOrderExecution
- allowModification
- allowClose
- tokenValid
- latencyMs if available
- lastError
- updatedAt

Rules:
- Default/fallback must be OFFLINE.
- If heartbeat is stale, status must become OFFLINE.
- If bridge data is unavailable, say unavailable instead of inventing.
- Live command permissions must remain false/locked.
- Frontend must reflect real bridge state.

B. Read-Only Broker Snapshot Ingestion
Add or verify safe read-only ingestion for:
- broker account snapshot if available
- balance
- equity
- margin
- free margin
- open broker positions
- pending broker orders
- symbol prices
- symbol specs/pip value if available
- spreads if available
- timestamps for all broker data

Rules:
- Do not overwrite per-user paper/demo ledger with broker values.
- Broker values must be labeled “Broker Read-Only” or “Broker Unavailable.”
- If symbol specs are missing, P&L and risk calculations must show partial unavailable reasons.
- If price data is stale, mark it stale and block live-like decisions.

C. Broker/App Reconciliation
Create or verify reconciliation logic comparing:
- app paper trades/orders
- app virtual trades/orders
- broker read-only positions/orders
- tradeId/orderId attribution
- virtualSubAccountId attribution
- symbol
- direction
- entry price
- lot size
- SL/TP
- status

Reconciliation statuses:
- MATCHED
- APP_ONLY
- BROKER_ONLY
- MISMATCHED
- ATTRIBUTION_MISSING
- STALE_BROKER_DATA
- BRIDGE_OFFLINE
- RECONCILIATION_BLOCKED

Rules:
- Reconciliation must not execute changes.
- Broker-only positions without attribution must be flagged, not assigned randomly.
- Mismatches must be audit logged.
- Missing attribution must block future command routing.
- Per-user isolation must remain intact.

D. Command Queue Safety Shell
Prepare a command queue model for future MT5 commands, but keep execution disabled.

Support command types:
- PLACE_ORDER
- MODIFY_SLTP
- CLOSE_TRADE
- CANCEL_PENDING_ORDER
- SYNC_REQUEST
- RECONCILE_REQUEST

Each command record should include:
- commandId
- userId
- virtualSubAccountId if relevant
- tradeId/orderId if relevant
- auditId
- commandType
- payload
- safetyGateSnapshot
- status: DRAFT, BLOCKED, PAPER_ONLY, QUEUED_LOCKED, REJECTED, EXECUTION_DISABLED
- blockReason
- createdAt
- updatedAt

Rules:
- All real MT5 command statuses must resolve to BLOCKED or EXECUTION_DISABLED today.
- Missing userId, virtualSubAccountId, tradeId/orderId, or auditId must block the command.
- Duplicate command IDs must be blocked.
- Commands cannot cross users.
- Auto-close command generation must remain ALERT_ONLY and EXECUTION_DISABLED.

E. Safety Gate Snapshot
Centralize a reusable safety gate snapshot for every trade-related action:
- userAuthenticated
- userOwnsTrade
- bridgeConnected
- bridgeReadOnly
- tokenValid
- liveTradingUnlocked
- sharedRoutingUnlocked
- paperMode
- activityStatus
- userOptIn
- inactivityConfirmed
- highConfidenceConfirmed
- duplicateActionClear
- riskLimitsPassed
- attributionComplete
- currentPriceAvailable
- brokerDataFresh
- commandExecutionAllowed

Rules:
- This snapshot must be saved into audit logs and command queue records.
- Any false required gate must produce a clear block reason.
- UNKNOWN activity remains a hard block for auto-close execution.

F. Frontend Bridge + Reconciliation Visibility
Add or upgrade UI areas showing:
- Bridge status
- Read-only broker status
- Last heartbeat
- Broker data freshness
- Broker balance unavailable state
- Reconciliation status
- App-only trades
- Broker-only unattributed positions
- Mismatched SL/TP or lot size
- Command queue locked/disabled state

UI must clearly say:
- “MT5 Bridge Read-Only”
- “Live execution locked”
- “Shared MT5 routing locked”
- “Commands are being audited but not executed”

No fake connected status.

G. AI Bridge/Reconciliation Awareness
Upgrade AI so users can ask:
- Is my MT5 connected?
- Is the bridge live?
- Why can’t this trade be closed?
- Is this trade synced with MT5?
- Why is broker balance unavailable?
- Are any trades missing attribution?
- Is this app trade matched to broker state?

AI must:
- Use real bridge/read-only/reconciliation state.
- Explain missing data honestly.
- Never say MT5 is connected unless heartbeat is fresh.
- Never imply live execution is enabled.
- Never execute or queue real commands from chat.

H. Audit Trail Expansion
Add or verify audit logs for:
- Bridge heartbeat received
- Bridge heartbeat stale
- Broker snapshot received
- Broker snapshot unavailable
- Broker data stale
- Reconciliation started
- Reconciliation completed
- Reconciliation mismatch found
- Broker-only unattributed position found
- Command draft created
- Command blocked
- Execution disabled
- Duplicate command blocked
- Safety gate snapshot created

Each audit entry must include:
- userId if applicable
- virtualSubAccountId if applicable
- tradeId/orderId if relevant
- commandId if relevant
- action
- previous value if relevant
- new value if relevant
- safety gate status
- reason if blocked
- timestamp

I. Preserve All Existing Safety Rules
Confirm all remain true:
- Auto-close remains ALERT_ONLY unless every required gate passes.
- Live trading remains BLOCKED unless explicitly unlocked and every required gate passes.
- Shared MT5 routing remains BLOCKED unless explicitly unlocked and every required gate passes.
- MT5 commands remain blocked while bridge/token/live gates are missing.
- No AI open/add/widen trade tools.
- No fake candles.
- No fake scanner opportunities.
- No fake news.
- No fake current events.
- No fake TP targets.
- No fake P&L.
- No fake bridge status.
- UNKNOWN activity hard-blocks auto-close.
- Per-user SQL/session filters stay intact.

After implementation, run:
1. Typecheck
2. Full guard suite
3. Trading intelligence regression
4. Stop-limit/order-type QA
5. SL/TP create/edit tests
6. Manual close confirmation/blocking tests
7. Per-user trade isolation tests
8. Ledger ownership/isolation tests
9. Shared MT5 attribution-block tests
10. Bridge heartbeat fresh/stale tests
11. Broker snapshot unavailable fallback tests
12. Reconciliation matched/mismatched/unattributed tests
13. Command queue blocked/execution-disabled tests
14. Safety gate snapshot tests
15. Duplicate command block tests
16. AI bridge/reconciliation question tests
17. Audit trail tests
18. Frontend smoke test for bridge status, reconciliation UI, trade cards, portfolio dashboard, and command locked state

If any test fails:
- Fix only confirmed P0/P1 issues.
- Do not redesign unrelated UI.
- Do not touch unrelated pages.
- Rerun all checks after fixes.

End with a report showing:
- Files inspected
- Files changed
- New UI areas added
- Backend endpoints/types touched
- Bridge health behavior confirmed
- Broker read-only sync behavior confirmed
- Reconciliation behavior confirmed
- Command queue safety behavior confirmed
- Safety gate snapshot behavior confirmed
- AI bridge awareness confirmed
- Tests run
- Pass/fail results
- Remaining gated/deferred items
- Auto-close status
- MT5/live trading status
- Shared MT5 routing status
- Final answer: READY FOR NEXT PHASE or NOT READY, with reasons.