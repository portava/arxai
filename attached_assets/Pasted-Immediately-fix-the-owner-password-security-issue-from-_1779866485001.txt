Immediately fix the owner password/security issue from the real-browser smoke test.

Critical issue:
The test changed user 4 / owner account password_hash to a known smoke-test password so the browser test could log in. The report says the seeded password is still active. That must not remain true.

Do not do feature work until this is fixed.
Do not place trades.
Do not insert arx_live_commands.
Do not type operator phrases.
Do not weaken safety gates.
Do not expose password hashes, reset tokens, sessions, or secrets.

Create checkpoint first:
checkpoint: pre-owner-password-reset-security-fix

Task 1 — Lock down the owner account immediately:

1. Invalidate all auth sessions for user 4 / owner.
2. Mark the account as requiring password reset on next login.
3. Ensure the known smoke-test password no longer works.
4. If the app has password reset flow, force user 4 through it.
5. If the app does not have a password reset flow, add a safe owner-only password reset path that does not expose the new password, hash, reset token, or session token in logs/UI.

Task 2 — Remove unsafe seeded-password behavior:

1. Find the test code that wrote a known password hash to the real owner account.
2. Change it so seeded E2E tests never modify real users again.
3. Tests must create isolated test-only users with unique emails/session tokens.
4. Cleanup must delete only seeded test users by session token.
5. Test harness must refuse to run against real owner/admin users unless explicitly using a safe impersonation fixture that cannot change credentials.

Task 3 — Add guardrails:
Add regression/CI protection that fails if any test attempts to:

* update password_hash for a real user
* update user 4 password_hash
* use a known shared smoke-test password on a real account
* clear real owner sessions outside an explicit admin/security flow
* seed test auth into production-like environments

Task 4 — Verify:

1. Login with the known smoke-test password fails.
2. Existing sessions for user 4 are invalidated.
3. Owner must reset password.
4. Test-only users can still be created safely.
5. No real users are modified by seeded E2E tests.
6. arx_live_commands count remains 0.
7. No live trades are placed.
8. No safety gates are weakened.

Run:

* typecheck
* ci:guards
* auth/security regression tests
* account-mode tests
* arx_live_commands count before/after

Final report only:

* whether owner sessions were invalidated
* whether owner password reset is required
* whether known test password is rejected
* files/routes changed
* tests added
* QA results
* arx_live_commands count
* confirmation no trades placed