We are adding three UX/system upgrades to ARX AI. Do not break the current working MT5 demo bridge execution path.

Current known working state:
- MT5 demo bridge can execute demo trades.
- EA v1.26 is connected.
- Demo commands can become FILLED_DEMO with broker tickets.
- Live broker execution must remain disabled unless separately protected/enabled.

Build the following:

PART 1 — MENU SEARCH BAR

Add a search bar inside the main menu/sidebar/more menu so users can quickly find pages and features because ARX now has many options.

Requirements:
1. Add a menu search input at the top of the expanded menu.
2. Placeholder text: “Search ARX…”
3. Search should match:
   - page title
   - route name
   - menu label
   - aliases/synonyms
   - feature descriptions
4. Example searches should work:
   - “mt5” → MT5 Setup / Bridge
   - “demo” → Demo Bridge / Demo Commands / Demo Positions
   - “trade” → Trade page
   - “risk” → Risk page
   - “ai” → Ruby AI
   - “scanner” → Market Scanner
   - “positions” → Open Positions
   - “history” → Trade History
   - “settings” → Settings
5. Results should show:
   - icon
   - page/feature name
   - short description
   - route/location
6. Clicking a result must navigate to the correct page.
7. No dead links. Every searchable item must point to a real existing route.
8. Add a small “No results found” state.
9. Add recent/frequent menu shortcuts under the search bar.
10. Keep mobile layout clean and touch-friendly.

PART 2 — RENAME AI TO RUBY + SAVED CHAT MEMORY

Rename the ARX AI assistant to “Ruby.”

This must apply everywhere:
- AI chat header
- floating AI button
- AI widget
- system AI responses
- prompt-generated AI responses
- trade explanation responses
- scanner explanations
- risk explanations
- dashboard AI messages
- notifications generated by AI
- any “ARX AI says…” labels should become “Ruby says…”

Do not rename the company/app. The app remains ARX AI. The assistant/persona is Ruby.

Add persistent saved AI chat.

Requirements:
1. AI chats must stay saved unless the user clears them.
2. Chats must persist across logout/login, browser refresh, device changes, and long gaps in usage.
3. Save chat per authenticated user.
4. Add backend tables or reuse existing tables for:
   - ai_conversations
   - ai_messages
   - ai_memory_summaries
   - ai_user_preferences
   - ai_context_events
5. Every AI message should store:
   - user id
   - conversation id
   - role: user / assistant / system
   - assistant name: Ruby
   - message content
   - created_at
   - related page/feature
   - related trade id / command id when applicable
   - metadata for symbols, tickets, risk settings, and market context when applicable
6. Ruby should be able to use prior chat history and saved memory to understand the user later.
7. Add memory summarization so Ruby does not need to load every old message every time.
8. Ruby should remember useful long-term app context such as:
   - user risk preferences
   - preferred symbols
   - trading habits
   - account/demo/live mode context
   - previous questions
   - past explanations
   - preferred response style
   - open trades discussed with Ruby
9. Do not store sensitive secrets in memory:
   - broker passwords
   - bridge tokens
   - API keys
   - private keys
   - payment details
10. Add controls inside Ruby settings:
   - Clear current chat
   - Clear all Ruby chat history
   - Clear Ruby memory
   - Export chat history
   - Memory on/off toggle
11. If the user clears chat or memory, Ruby must stop using that cleared data.
12. Add a visible note in Ruby settings:
   “Ruby remembers saved conversations to help personalize ARX. You can clear chat or memory anytime.”
13. The first AI greeting should say:
   “I’m Ruby, your ARX trading assistant.”
14. All AI-generated responses should visually come from Ruby.

PART 3 — CLEANER MT5 BRIDGE PAGE

Organize the MT5 Bridge page so it is cleaner, more user-friendly, and not overwhelming.

Do not remove advanced diagnostic data. Move advanced details into collapsible sections.

Create this layout:

A. Top Status Header
Show clean cards:
- Bridge status: Connected / Disconnected
- EA version
- Account type: Demo / Live
- Live broker execution: Disabled / Enabled
- Demo trading: Enabled / Disabled
- Last heartbeat time
- Current account number
- Broker/server
- Selected symbol

B. Main Action Area
Add clear buttons:
- Run Controlled EURUSD 0.02 Demo Test
- Refresh MT5 Positions
- Close Selected Demo Position
- Close All ARX Demo Test Positions
- View Latest Result

Buttons must show loading states and prevent double taps.

C. Latest Demo Trade Result Card
Show:
- latest command id
- symbol
- side
- requested volume
- executed volume
- status
- broker ticket
- fill price
- reject reason
- elapsed time
- updated time

Only show “Success” when the trade is actually FILLED_DEMO or equivalent, with broker ticket, fill price, and executed volume.

D. Open Demo Positions Panel
This must come from MT5 position sync, not just command history.

Show every open MT5 demo position:
- ticket
- symbol
- side
- volume
- entry price
- current price
- floating P/L
- opened time
- matched ARX command id if available
- match status: MATCHED_TO_ARX_COMMAND or ORPHAN_MT5_POSITION

E. Recent Demo Commands Table
Expand from last 10 to last 50 with View More / pagination.

Columns:
- created_at
- sent_to_mt5_at
- filled_at / rejected_at
- elapsed time
- command id
- symbol
- side
- requested volume
- executed volume
- status
- broker ticket
- fill price
- MT5 retcode
- reason
- endpoint used

Normalize statuses visually:
- FILLED_DEMO / MT5_DEMO_FILLED / DEMO_FILLED → FILLED
- REJECTED / MT5_DEMO_REJECTED / DEMO_REJECTED → REJECTED
- FAILED / DEMO_FAILED → FAILED
- BLOCKED → BLOCKED
- DEMO_CANCELLED → CANCELLED
- pending states → PENDING

F. Rejected / Failed Rows
Do not show blank reasons or zero-only values.
Show:
- app guard reason
- server reason
- EA reason
- MT5 retcode
- broker message

G. Advanced Debug Section
Move noisy details here:
- raw heartbeat payload
- EA input flags
- poll outcome
- result outcome
- orphan counts
- duplicate state
- endpoint logs
- token status without exposing token
- connection id
- payload JSON preview

This section should be collapsed by default.

H. Safety Banner
At the top, show:
“Demo trading enabled. Live broker execution disabled.”

If live execution is disabled, do not make the red banner look like an error. Make it look like a protected safety state.

I. Reconciliation Warning
Compare:
- MT5 open positions count
- ARX displayed open positions count
- filled command rows with broker tickets
- Recent Demo Commands displayed rows

If mismatched, show:
“MT5 positions and ARX command history may be out of sync. Refresh or reconcile.”

J. Controlled Test Safety
The “Run Controlled EURUSD 0.02 Demo Test” button must:
1. Create exactly one EURUSD BUY 0.02 demo command.
2. Use an idempotency key.
3. Disable while pending.
4. Prevent double-taps.
5. Show command id immediately.
6. Show lifecycle state live.
7. Show final FILLED / REJECTED / FAILED result clearly.

Acceptance proof required:
- Menu search finds and navigates to MT5 Setup, Trade, Risk, Ruby AI, Scanner, Positions, History, and Settings.
- Ruby name appears everywhere AI responses appear.
- Ruby chat persists after refresh/logout/login.
- Clearing Ruby chat removes it from active memory.
- MT5 Bridge page is organized into clean panels.
- Every MT5 mobile open position appears in ARX Open Demo Positions.
- Recent Demo Commands shows last 50 with real reasons for rejected/failed rows.
- A controlled EURUSD 0.02 demo test creates only one command and clearly shows FILLED or REJECTED.