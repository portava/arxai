Two small things worth a glance, then yes to the chooser:
1. The "drop if no longer enumerated" behavior on recents — is the UX honest about it? If I picked Volatility 90 (1s) Index yesterday and today it's not in the enumeration (terminal symbol list changed, or freshness went MISSING), the recent silently disappears. That's safe (better than executing a stale symbol), but make sure it doesn't look like a bug to the user — a vanished recent with no explanation can read as "the app lost my history." Not blocking, just worth a thought: does it fail silently, or is there any "previously used, no longer available" affordance? Either is defensible; I'd just want it to be a decision rather than an accident.
2. Freshness chip + the picker gating — what happens at STALE specifically? You've got MISSING handled (honest empty state) and FRESH (normal). The middle case: if overallFreshness is STALE (EA enumerated a while ago but hasn't refreshed), does the picker still let you select, with a warning? It should — STALE symbols are probably still valid, just not freshly confirmed, so blocking selection would be too aggressive. Just confirming STALE isn't accidentally treated like MISSING (which would lock the picker when it shouldn't).
Neither needs a code change necessarily — they're "confirm it's intentional" checks, same spirit as the recents catch. If you've already reasoned through both, we're good.
On the chooser batch — yes, start it. And this is the one I care most about getting right, because it's the surface where the safety design becomes visible and load-bearing. Let me set the scope precisely so it stays contained:
The chooser is purely about turning the existing SYMBOL_AMBIGUOUS handling in LiveSharedTradeTicket from a dead-end error string into an actionable picker. The data path (resolveBrokerSymbol → returns candidates) is already built and proven. So the batch is:

When resolveBrokerSymbol(typed) returns SYMBOL_AMBIGUOUS, render the candidates as clickable buttons ("Did you mean: [Volatility 75 Index] [Volatility 75 (1s) Index]") instead of the current text-only error
Clicking a candidate sets both the display symbol and the exact brokerSymbol state, clears the error, and re-enables confirm
Crucially: after the user picks a candidate, the confirm path uses that exact brokerSymbol — it does NOT re-resolve (which would just re-trigger ambiguity). The pick is final.
SYMBOL_NOT_FOUND stays a clear error (no candidates to offer)
The "never silent pick" guarantee is the whole point: the UI makes the user choose between the two real instruments, because they're genuinely different (standard vs 1s tick)

The one design question I want to settle before writing it: when a candidate is picked, should the symbol input text update to the full broker name (e.g. typing V75, picking, and the field now shows Volatility 75 Index), so what's displayed matches what'll execute? I think yes — the field should reflect the resolved symbol so there's no mismatch between what the user sees and what gets sent. But it's worth confirming you want that visible substitution rather than keeping their typed V75 in the box with the brokerSymbol tracked invisibly. Visible-substitution is safer (no hidden state), so that's my default.
Before I write it, I'll read the current LiveSharedTradeTicket ambiguity-handling block (the setSymbolError candidate path I added in the spine) to ground the edit in what's actually there — then make the single scoped change and hand it back for a typecheck checkpoint.
Want me to proceed with the chooser on that basis — visible substitution (picked candidate fills the field with the full broker name), pick-is-final (no re-resolve), NOT_FOUND stays an error? And did you already reason through the STALE-picker and vanishing-recent behavior, or want me to fold those into the chooser batch?Opus 4.8 Max