Three UI changes to the Scanner chart, display layer only:
1. Timeframe chips: visible set exactly 1m, 5m, 15m, 30m, 1h, 4h, 8h, 1D, 1W — in that order. Remove the 1M chip and the "More" dropdown entirely; no other timeframes selectable from this UI. Server keeps the full 21-TF enum (API contract unchanged); EA streamed set untouched. Saved state or deep links referencing a hidden timeframe fall back to 15m gracefully. Verify the nine chips fit mobile without horizontal overflow (prior regression area).
2. Price scale always visible: the right-side price axis with the current-price stamp must render on every timeframe for every symbol — including fallback-provider and synthetic symbols, and in stale/delayed states (show last-known price with the existing staleness badge; never a blank axis). Fix any timeframe/symbol combination that currently renders without it.
3. Candle close countdown: show time remaining until the current bar closes, all timeframes, all symbols. Compute as (current bar's open_time + interval − now) from the actual newest bar being rendered — NOT naive calendar math — so D1 respects the broker's daily boundary and W1 the broker's weekly close. Format adaptively: m:ss intraday, h:mm on 4h/8h, d hh:mm on 1D/1W. When the feed is stale (no current bar), show NO countdown — a ticking timer is a liveness claim and must obey the same honesty rules as every badge. Place adjacent to the existing chart status area; no mobile overflow.
Coordination: check #496's (ticking candles) merge status first — if its forming-bar work has landed, key the countdown off the forming bar's open time; if still merging, build against the newest closed bar + interval and note the rebase point. Also report #496's current state: still merging, conflicted, or landed.
Separately, one diagnostic question, read-only: on the Opportunity Map, TSLA renders "BUY · Edge 78" while flagged "no live data — awaiting live data." What data produced that direction and edge score, and per the truth-cap rules, should a no-data symbol render a direction badge at all rather than only the awaiting state? Report, don't fix yet.