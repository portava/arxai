Clean up ARX AI live-mode UI so the app looks and behaves like a fully live trading app, while only showing failures/errors when they are actually true.

Important:
The app is live/armed now. The UI should not look like a broken testing/debug page. Do NOT fake live execution. Do NOT hide real safety blockers. But remove false, stale, misleading, internal, or developer-only failure labels from the user-facing app.

Current issue:
- The app is live, but the UI still shows scary failure/error states everywhere.
- Some displayed failures are not accurate or are stale/debug-only.
- The app looks unfinished even though live mode is armed.
- Normal users should not see backend diagnostic labels, raw bridge states, endpoint names, internal constants, or false errors.

Before changing code:
1. Search the whole app for live mode UI, MT5 bridge UI, broker execution UI, diagnostics panels, badges, error banners, status chips, admin/user views, scanner trade buttons, Ruby trading status, and all places showing:
   - READ_ONLY
   - Broker Execution Disabled
   - BROKER_PLACEMENT_LAYER_NOT_IMPLEMENTED
   - mt5Deferred
   - brokerExecutionAvailable
   - placementLayer
   - endpoint mounted
   - heartbeat mounted
   - debug
   - failed
   - unavailable
   - disabled
   - error
2. Identify which statuses are real backend blockers and which are stale, false, or developer-only.
3. Do not simply change labels. Make the UI reflect the real backend state accurately.

Required behavior:
1. If the app is live/armed and backend checks pass, the user-facing UI should look ready for live trading.
2. Show clean, confident live-ready statuses such as:
   - Live Mode Armed
   - Bridge Connected
   - Broker Connected
   - Trading Ready
   - Awaiting Confirmation
   - Order Sent
   - Order Filled
   - Order Rejected
3. Only show warnings/errors when they are actually true based on current backend health.
4. Remove or hide stale warnings that remain on screen after the issue is resolved.
5. Add proper state refresh so bridge/trading badges update in real time instead of staying stuck.
6. Normal users should never see raw internal states or backend constants.
7. Admins can still access detailed diagnostics, but diagnostics should be inside a clearly labeled admin-only expandable panel.
8. If a real blocker exists, show a user-friendly message instead of backend text.

Examples:
- Do not show “BROKER_PLACEMENT_LAYER_NOT_IMPLEMENTED” to users.
  User-facing version: “Broker execution is not ready.”
- Do not show “READ_ONLY” everywhere if live trading is actually armed for the approved user.
  User-facing version should match the real account mode:
  “Live Trading Ready” or “Execution Locked” depending on true backend state.
- Do not show “Broker Execution Disabled” if execution is actually enabled and health checks pass.
- If execution is disabled because of a real safety lock, show:
  “Execution locked by admin” or “Live trading approval required.”

Live UI cleanup:
1. Create a unified live status resolver that converts raw backend state into clean user-facing statuses.
2. Use this resolver across:
   - dashboard
   - MT5 bridge page
   - scanner
   - trade ticket
   - Ruby trade assistant
   - admin user approval area
   - mobile views
3. Split status display into two layers:
   - User-facing live status: clean and simple
   - Admin diagnostics: detailed technical information
4. Remove duplicate/conflicting banners.
5. Remove stale badges that contradict current state.
6. Make the UI visually feel ready for live trading:
   - no error spam
   - no debug blocks on normal pages
   - no giant warning banners unless truly required
   - clean live/trading status cards
   - clear trade readiness indicators
   - consistent colors and wording
7. Keep true safety warnings visible when they are real.
8. Do not remove kill switches, risk checks, approvals, or execution gates.

Approved-user live mode:
1. If a user is admin-approved for live trading, live armed, bridge connected, heartbeat fresh, account synced, and risk checks pass, they should see a ready/live trading interface.
2. If the user is not approved, show a clean locked state:
   “Live trading requires admin approval.”
3. If the bridge is disconnected, show:
   “Bridge disconnected.”
4. If the EA heartbeat is stale, show:
   “MT5 connection needs attention.”
5. If broker execution is truly unavailable, show:
   “Broker execution is not ready.”
6. Do not show multiple overlapping messages for the same issue.

Acceptance tests:
1. Log in as an approved live user.
2. Confirm the dashboard looks live-ready, not like a diagnostic failure page.
3. Confirm false/stale errors are gone.
4. Confirm live status matches actual backend state.
5. Confirm normal users do not see raw values like brokerExecutionAvailable, placementLayer, mt5Deferred, mounted endpoints, or internal constants.
6. Confirm admin can still open diagnostics when needed.
7. Confirm real blockers still appear clearly and accurately.
8. Confirm the scanner/trade ticket/Ruby pages all show consistent live status.
9. Confirm refreshing the app does not bring back stale false errors.
10. Confirm mobile UI is clean and not crowded with debug warnings.
11. Confirm no safety gates, admin approvals, risk limits, kill switches, or broker checks were bypassed.
12. Confirm no fake “ready” state appears if the backend is not actually ready.

After changes:
- Run full QA on live-mode UI, admin diagnostics, MT5 bridge status, scanner trade entry, Ruby trade assistant, and mobile layout.
- Fix anything that is inaccurate, duplicated, stale, or user-facing when it should be admin-only.