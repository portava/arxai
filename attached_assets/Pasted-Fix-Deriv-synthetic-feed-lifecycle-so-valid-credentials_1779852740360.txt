Fix Deriv synthetic feed lifecycle so valid credentials move from AWAITING_FEED to LIVE_FEED when ticks/candles are actually available.

Current status:
- Deriv credentials are valid.
- DERIV_APP_ID configured.
- DERIV_API_TOKEN configured.
- No INVALID_TOKEN / AUTH_FAILED.
- Symbol resolution works:
  - V25 / V75 resolve to R_25 / R_75.
  - V25 1s / V75 1s resolve.
  - Boom/Crash resolve.
- Tests pass for credential envelope and symbol mapping.
- But scanner rows still show dataSource=AWAITING_FEED and feedNote="Synthetic-index feed is connecting..."
- Reason given: live Deriv websocket has not yet completed active_symbols/ticks subscription and no streaming tick has landed in derivProvider before scanner reads it.

This is not a credential issue. Fix provider lifecycle and feed readiness.

Do not place trades.
Do not insert arx_live_commands.
Do not type operator phrases.
Do not weaken safety gates.
Do not fake ticks, prices, candles, or feed status.
Do not generate simulator candles for synthetic live symbols.
Do not expose Deriv token/app id values.

Create checkpoint first:
checkpoint: fix-deriv-persistent-feed-warmup-live-feed

Task 1 — Audit Deriv provider lifecycle:
Inspect:
- Deriv provider initialization
- websocket connect
- active_symbols call
- authorize/auth handshake if used
- tick subscription
- ticks_history/candle fetch
- reconnect logic
- provider cache
- scanner feed-readiness check
- provider health endpoint
- startup bootstrap

Find why the API server process does not have a live tick available when scanner reads synthetic symbols.

Task 2 — Make Deriv provider persistent:
The Deriv provider should be initialized in the long-running API server process, not only inside short-lived scanner/test calls.

Required:
1. On API server startup or first Deriv request, start persistent Deriv websocket connection.
2. Resolve active_symbols and cache canonical symbols.
3. Subscribe to ticks for watched synthetic symbols when scanner/watchlist/chart asks for them.
4. Keep last tick per symbol in memory/cache.
5. Keep last tick timestamp.
6. Reconnect on websocket close/error.
7. Resubscribe after reconnect.
8. Do not block the whole app if Deriv is still warming up.

Task 3 — Add feed warm-up readiness:
For synthetic/volatility symbols:
- If active_symbols loaded but no tick yet: AWAITING_FEED.
- If tick landed recently: LIVE_FEED.
- If ticks_history works but live tick not yet landed: HISTORY_READY_AWAITING_LIVE_TICK.
- If websocket/auth failed: show sanitized provider error in admin diagnostics only.
- If unsupported symbol: SYMBOL_UNAVAILABLE_FROM_DERIV_FEED.

Scanner should not stay AWAITING_FEED forever if ticks are arriving.

Task 4 — Add explicit warm-up endpoint/admin diagnostics:
Add or repair admin-only Deriv health diagnostics showing:
- credentials configured yes/no only
- websocket connected yes/no
- authorized yes/no if applicable
- active_symbols loaded yes/no
- active_symbols count
- subscribed symbols count
- last tick symbol
- last tick age
- last candle/history response
- reconnect count
- current provider state
- sanitized last error

Normal users should only see clean:
- Live synthetic feed connected
- Connecting to synthetic feed
- Historical data unavailable
- Symbol unavailable

Task 5 — Fix scanner read path:
When scanner requests V25/V75/V25 1s/V75 1s:
1. Ask Market Data Router for quote/candles.
2. If Deriv provider is warming, trigger subscription/warm-up.
3. Wait briefly for first tick/candle if this is the first request, with a safe timeout.
4. If tick arrives, return LIVE_FEED.
5. If only history arrives, return historical-ready state.
6. If timeout, return AWAITING_FEED with last attempt timestamp.
7. Do not fall back to simulator for synthetic live symbols.
8. Do not show fake price/candles.

Task 6 — Fix tests so they prove the real issue:
The short-lived tests should explicitly test:
1. Deriv provider initializes.
2. active_symbols completes.
3. tick subscription request is sent.
4. either first tick arrives within timeout, or test returns AWAITING_FEED as a warm-up state, not credential failure.
5. A long-running provider warm-up test can wait longer and should pass when a tick arrives.
6. Scanner transitions from AWAITING_FEED to LIVE_FEED after tick is cached.
7. arx_live_commands remains 0.

Add a test that does not exit before subscriptions have a chance to resolve.

Task 7 — UI behavior:
In scanner/live chart/Ruby:
- Show “Connecting to synthetic feed…” only while warming.
- When tick arrives, show “Live synthetic feed connected.”
- If history is available but live tick is pending, show “History loaded, waiting for live tick.”
- Do not show simulator language.
- Do not show credential failure when credentials are valid.
- Do not show raw websocket/admin details to normal users.

Task 8 — QA:
Run:
- typecheck
- build if available
- ci:guards
- test:deriv-scanner-feed
- test:scanner-selected-market
- test:scanner-real-feed
- qaDerivScannerFeed
- provider health check for Deriv
- scanner V25/V75/V25 1s/V75 1s feed check
- arx_live_commands count before/after

Final report only:
- whether Deriv websocket is persistent
- whether active_symbols loads
- whether ticks subscribe
- whether first tick lands
- whether scanner transitions to LIVE_FEED
- whether ticks_history/candles work
- current Deriv health state
- QA results
- arx_live_commands count
- remaining issue only if Deriv service itself is not returning ticks