The per-viewer projection was never proven to still apply for a non-admin caller at runtime. My command made that the hardest requirement, because opening a route is exactly where privileged data leaks. The agent confirmed projectOpportunitiesForViewer still exists in the handler (good), and added a route-auth test, but the QA it ran was anonymous→401 — it never actually logged in as a regular user, hit Scan, and inspected the response to confirm the simulator-derived/privileged fields are stripped. So "non-admin users can now scan" is proven; "non-admin scan results don't leak admin-only data" is asserted from code-reading, not demonstrated. Those are different, and the second one is the security-relevant half.
That's the check to do yourself: log in as a regular (non-owner) user, click Scan, and confirm results actually appear (the behavior fix) and that they're the stripped view — no raw simulator indicator values that the owner view shows. If you don't have a second non-admin account handy, the projection test the agent added (Step 3 #2) is the proxy — but I'd want to see that test actually asserts the absence of privileged fields for a non-admin caller, not just that the function is called. When you send the next archive I'll read that test and the handler together and confirm the projection genuinely fires on the opened path.
Two smaller flags, both minor:
The typecheck never passed — it cgroup-killed at every heap cap, and the agent fell back to manually verifying the symbols its code uses (req.authUser, the User export). For a ~3-edit change that compiled and ran under tsx, that's low-risk, but it's now the standard outcome on this sandbox, and it means "typechecks green" hasn't actually been true on the last several merges. At some point that environmental OOM is worth fixing properly, because right now you have no working full typecheck gate — a real type regression would sail through. Not urgent, but don't mistake "tsx ran the test" for "the package typechecks."
And the rate-limit value — the agent picked a cooldown reusing the existing limiter, but the report doesn't state the actual window it chose. Worth a glance that it's sane (a few seconds, not a few minutes — too long and the button feels broken again for legitimate re-scans).
Net: the behavior fix is real and the admin-only engine controls stayed locked. The one open item is confirming the data projection actually strips privileged fields on the now-open route — which is the thing that, if wrong, turns a UX fix into a data leak. Verify that with a real non-admin Scan, or send the archive and I'll verify the projection test asserts the right thing.
Quick close-out: do a non-admin Scan, confirm results appear and look like the regular-user view. If they do, this one's done.